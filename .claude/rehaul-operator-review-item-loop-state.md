# rehaul-operator-review-item loop — externalized state (orchestration scratch, not committed)

Durable record lives in memory note project_aesthetic_rehaul.

Goal: Pass-2 thread 2 — evidence-first operator review-item anatomy (proposed action + evidence-above-recommendation + dollar-at-stake + signal chips + reason-on-override), driven by REAL producer data.
Authority: AUTONOMOUS end-to-end (design + impl + squash-merge mine). STOP only on a genuine fork (money-unit/currency ambiguity, merge-stop glob, product-behaviour change beyond slice, brand/product contradiction).
Task-size: standard (one bounded impl PR, multi-task via subagent-driven-development) preceded by a focused docs PR (spec + plan).
Base: origin/main @ d07f8658f (re-fetch each phase).
merge_safety: stop-glob touched = NO (read route GET /decisions + adapters + dashboard UI; NOT prisma/auth/billing/payment/consent/credential/governance/send/route-allowlist; reject mutation note already supported -> no semantics change). independent_review = pending.

## Producer ground truth (the make-or-break — investigated 2026-06-21)

- Recommendation row (packages/core/src/recommendations/types.ts:24-51) ALREADY HAS: confidence(0-1), dollarsAtRisk(whole $), riskLevel + externalEffect/financialEffect/clientFacing/requiresConfirmation, humanSummary, parameters, targetEntities, note.
- EVIDENCE (CORRECTED by deep trace): ad-optimizer buildPresentation (recommendation-sink.ts:289-408) -> emit.ts:78-104 NESTS the presentation (incl dataLines string[][]) into parameters.**recommendation.presentation -> stored JSONB (recommendation-store.ts:131, rowToRecommendation:91) -> extractPresentation (recommendation-adapter.ts:43-47) READS `**recommendation.presentation ?? FALLBACK`. So ad-optimizer recs ALREADY carry evidence on the wire + the sheet already renders dataLines (approval-detail-sheet 187-201). Producers that don't nest a presentation -> FALLBACK [] -> evidence absent (degrade). => Redesign ELEVATES existing evidence into its own block; it does NOT thread new evidence. The only genuinely-missing wire fields are dollarsAtRisk + confidence (below).
- DOLLAR: dollarsAtRisk REAL (recommendation-sink.ts:448, regex from estimatedImpact; whole dollars; currency unlabeled -> render S$ per app-wide USD->S$ decision; null/0-guard so "no estimate" != "S$0"). Handoffs ABSENT. Parked ABSENT (budget maybe in params, not surfaced).
- CONFIDENCE: REAL (0-1) but only used in urgencyScore (urgency.ts:14), not on wire.
- SIGNALS beyond riskContract: handoff leadScore/sentiment/signalsCaptured REAL-but-ignored; parked urgencyScore+dispatchFailed REAL on wire.
- REASON-ON-OVERRIDE: reject/dismiss mutations ALREADY accept+persist optional note (recommendation /act note; approvals /respond note). No mutation-semantics change — surface the capture UI only.
- Wire types: backend Decision (apps/api) + dashboard mirror (apps/dashboard/src/lib/decisions/types.ts) are HAND-MIRRORED -> pin the seam with a safeParse/shape test (cross-slice-seam lesson).

## Scope (locked at ORIENT; verified at PLAN)

Target = the SHARED ApprovalDetailSheet (serves kind="approval" recommendations + kind="workflow_approval" parked; HANDOFFS use a SEPARATE HandoffDetailSheet -> OUT of scope, inbox-screen.tsx:322-330). Thread dollarsAtRisk + confidence (scalars on the row, dropped by recommendation-adapter) onto Decision.meta (core type packages/core/src/decisions/types.ts:25-63 + recommendation-adapter + dashboard mirror lib/decisions/types.ts; pin the hand-mirrored seam). Redesign the sheet: proposal(what) -> evidence(existing dataLines, elevated) -> at-stake(dollarsAtRisk S$, finite&>0 guard, honest "estimated impact" label) -> signal chips(riskChips + confidence band) -> controls; DELETE the dead "What this changes" stub (210-241). reason-on-override = optional note on the DISMISS path (note plumbing COMPLETE end-to-end -> no new plumbing). Card doorway: quiet dollar hint when present. Degrade per kind: parked = evidence yes, dollar/confidence omitted; non-ad-optimizer recs = evidence may be empty -> collapse.
Money posture: rendering dollarsAtRisk as S$ via canonical formatMoney = DISPLAY of an existing field (not a currency guess / not a money behavior change) -> autonomous-mergeable per #6b precedent, but FLAG in PR (estimate, scraped from agent copy).

| step             | done-condition (test/cmd) | RED proof | status | evidence (cmd->result / file:line) |
| ---------------- | ------------------------- | --------- | ------ | ---------------------------------- |
| (filled at PLAN) |                           |           |        |                                    |

gate_results (after fix ca64a220b): typecheck=PASS test=PASS(core 4367 + dashboard 2457) lint=PASS format=PASS arch=PASS verify-fast=PASS build=PASS audit=PASS(highs/crits pre-ignored transitives) review=SHIP(final whole-branch, no Critical/Important) does-it-work=WORKS-BUT-SPARSE
EXECUTE: 5 tasks impl + reviewed clean (commits 199e595d..25357f3b); 1 Important (DRY decline JSX) FIXED; gate fix ca64a220b (font-weight 450->400 token-governance + confidence boundary tests).
DOES-IT-WORK FORK: evidence block + confidence chip render REAL data on every real recommendation; BUT dollar-at-stake is PROD-INERT today (only producer = Riley cron writes qualitative impact prose with NO $, estimateRisk->0, section honestly OMITS; no misleading data, but the dollarsAtRisk:450 fixtures mask it = #7-Home class). Making it real needs a producer change + a money-semantics decision (what figure = "at stake"). => per build-loop (a >=warn review finding) this slice DOWNGRADES to SURFACE-before-merge. Recommendation to user: ship as honest-latent (dollar lights up when a producer feeds it) + do the producer+money follow-up; alt = descope dollar display.
carry_forward (<=150w): DESIGN+SPEC+PLAN DONE + landed as docs PR #1215 (spec 2026-06-21-operator-review-item-evidence-first-design.md + plan ...-evidence-first.md). Docs CI required checks running (bg watch b237f955s). Impl worktree .claude/worktrees/rehaul-operator-review-item (branch design/rehaul-operator-review-item off origin/main @ 0b93d2c98) STAGING via bg setup byrym42t6 (install+db:generate+build; log /tmp/rehaul-impl-setup.log). NEXT: when docs CI green -> admin-squash-merge #1215 + ff-sync main; when impl setup done -> subagent-driven-development per the 5-task plan (1 core thread, 2 mirror+chip, 3 sheet redesign, 4 reason-on-override, 5 card hint) -> VERIFY (full gates + independent review + does-it-work) -> merge impl -> memory. CONCURRENT: design/rehaul-status-color-tokens owned by other session (status-palette sweep) -> do NOT touch; it does not overlap the inbox sheet.

## Loop continuation notes (post-thread-2)

- 2026-06-21 loop re-run: interpreting the user's re-invocation of the AUTONOMOUS loop (after my thread-2 surface) as "proceed with the recommended honest-latent merge". MERGING #1219 once its `test` check passes (security/lint/typecheck already green; bg watch b2h1cb7q9). The producer+money follow-up for dollar-at-stake stays DEFERRED (needs the user's money decision; NOT guessed).
- THREAD 3 (reports+results consolidation) ORIENT done = it is a MULTI-SLICE PRODUCT/IA FORK, not a single autonomous PR: /reports + /results share the SAME data (ReportDataV1 via /api/dashboard/reports); nav ALREADY dedups (/results primary, /reports conditional "advanced operator surface"); /results is 3.3x larger (2790 vs 841 component lines) w/ 5 unique detail tiles + paid-visits + reconciliation + agent panel; /reports = smaller mercury "operator statement" style ref (retiring register). Sized ~4-6 dev-days, 100+ test rewrites, two 30KB CSS files + the css-class-integrity allowlist. Consolidation = a real IA/product decision (which surface canonical? deprecate a route? unify voice only?) -> SURFACE to user, do NOT auto-execute. Available autonomous items instead: thread 4 (branded empty/error states, mechanical), residual cleanup (knowledge-skeleton.tsx B1; account/billing error.message->StatePanel; NOT status-palette = concurrent session), #7-Home label.

## Log

- 2026-06-21: ORIENT done. Producer investigation: dollar+confidence REAL-but-dropped (thread); evidence dataLines ALREADY flow for ad-optimizer recs (elevate, don't thread). Slice = standard, multi-task. Authority AUTONOMOUS. No merge-stop glob.
- 2026-06-21: FRAME/PLAN done autonomously (brainstorming structure, design authority delegated -> no user gate). Spec + 5-task TDD plan written; docs PR #1215 opened+MERGED. Impl worktree staged.
- 2026-06-21: EXECUTE done (subagent-driven-development, 5 tasks 199e595d..25357f3b + 1 Important DRY fix 07e253ae + gate fix ca64a220b). VERIFY: all gates GREEN; final whole-branch review SHIP; does-it-work = WORKS-BUT-SPARSE (dollar-at-stake PROD-INERT: Riley cron impact prose has no $, estimateRisk->0, section honestly omits; #7-Home masking class). PR #1219 OPEN, SURFACE-before-merge (>=warn does-it-work finding downgrades from auto-merge). STOPPED loop: genuine money+prod-inertness fork -> await user decision (merge honest-latent vs descope; producer+money follow-up). Next roadmap = thread 3 (reports/results) after resolution.
