# spec1b act-leg loop — externalized state (orchestration scratch, not committed)

Durable record lives in memory note project_riley_capability_audit_2026_06_10.

Goal: finish ALL Spec-1B act-leg slices. Authority: auto-merge per slice after green CI + clean review (WTP sequencing overridden by user 2026-06-14; safety interlocks + flags-off + mocked-tests + no-real-money REMAIN). Task-size: multi-slice → adopt on-main plan #1032, drive ONE slice at a time.
Plan on main: docs/superpowers/plans/2026-06-14-spec1b-act-leg-implementation.md
Worktree: .claude/worktrees/spec1b-act-loop (reused; reset to fresh origin/main between slices).

## DECISION (locked by user 2026-06-14): Campaign-budget scale

Reallocate = scale a Meta campaign's TOTAL daily budget (matches updateCampaignBudget from 1B-1.1, which sets campaign daily_budget cents). Trigger on the existing `scale` recommendation action (Riley's "scale up 20%" semantics, recommendation-engine.ts:376-387), NOT `shift_budget_to_source` (that is source-level intra-campaign, would need ad-set edits = mismatch). Proposed budget = round(currentCents × REALLOCATE_SCALE_FACTOR=1.2). Decreases (review_budget / scale-down) DEFERRED to v2. So 1B-1.2's candidate+submit-request must be RE-KEYED from shift_budget_to_source → scale.

## FRAME finding (2026-06-14, verified) — splits 1B-1.3

- Action enum `scale` exists, verbatim "scale" (schemas/src/ad-optimizer.ts:16-31). `review_budget` also present (v2).
- The `scale` rec carries NO budget numbers (recommendation-engine.ts makeRec output: campaignId, action, estimatedImpact rationale, steps; params NOT populated).
- budgetDistribution `CampaignBudgetEntry.dailyBudget` is DOLLARS but is set to **null** at audit time (audit-v2-sections.ts:156 — no live budget flows through the v2 section yet) and is NOT passed into runRecommendationSink (call site recommendation-sink.ts ~655-672 / audit-runner ~655-672).
- ⇒ There is currently NO source of the campaign's CURRENT daily budget at sink time. Sourcing it (a live getCampaign read from 1B-1.1 MetaAdsClient, or upstream budget population) is SINK-WIRING work, not gate-plumbing. So 1B-1.3 is SPLIT: pure gate-plumbing now, sink wiring later (1B-1.6) once the real executor exists.
- HandoffCampaignContext { evidence{clicks,conversions,days}, learningPhaseActive } is keyed by campaignId in a Map (campaignEvidenceByCampaign), already threaded to the sink and consumed by pause/handoff dispatch (recommendation-sink.ts:478,510). Budget dispatch reuses it.
- adAccountId lives on audit-runner AuditConfig.accountId (audit-runner.ts:95), NOT passed to the sink today. buildRileyBudgetCandidate already requires + abstains-without adAccountId (riley-budget-dispatch.ts:75).
- gate: SPEND_KEYS=["spendAmount","amount","budgetChange","newBudget"] (spend-limits.ts:17); extractSpendAmount reads the first finite number in DOLLARS (spend-limits.ts:26-35). Sink currently omits spendAmount.

## Slice queue (revised 2026-06-14)

- [x] 1B-1.2 submitter + dispatch + seeded mandatory policy + FAIL-CLOSED placeholder executor. MERGED #1038.
- [x] 1B-1.3 re-key trigger → `scale`; pure proposeCampaignReallocationCents (×1.2); spendAmount(dollars) into submit-request + gate proof. MERGED #1043 (merge commit 0ec81734). Adversarial review []=0 findings.
- [x] 1B-1.4 ADDITIVE schema: executedAt+executionWorkUnitId cols + `MetaMutationAttempt` model/store + machine-path stamp. NO scorer gate (→1B-2). SHIPPED → PR #1047 (commit f13e2854), auto-merge armed, CI running. Adversarial review []=0 findings. Migration 20260614140000_spec1b_reallocation_marker deployed+drift-clean. Gates: typecheck/db1066/api2012/lint/format/arch/eval33/local-verify all green.
- [~] 1B-1.5 real read-modify-re-read executor. SPLIT:
  - [x] 1B-1.5a SUBSTRATE: L1 ExecutionReceiptSchema + L2 DEFAULT_BLAST_RADIUS_CONTRACT ($50/0.25) + L4 store claimLeaseAndMark(advisory-lock NS 920_002)/markApplied/markRecoveryRequired. SHIPPED → PR #1049 (commit db5f3ef3), auto-merge armed. Adversarial review []=0. Gates: typecheck21/schemas834/ao708/db1084/lint/format/arch/eval33/local-verify green. NO schema change (store extends 1B-1.4 model). Store ctor narrowed PrismaDbClient→PrismaClient (needs $transaction).
  - [x] 1B-1.5b EXECUTOR: real read-modify-re-read executor per the 10-step order; deps wired in contained-workflows.ts:249. Full §13 matrix (45 budget tests incl. shared-store end-to-end at-most-once). SHIPPED → PR #1053 (commit 63de3a83), squash auto-merge ARMED, CI running. All 10 gates green (typecheck/apiTest2044/schemasTest839/lint0err/format/arch/apiBuild/localVerify/evalRiley/build). Independent fresh-context review = SHIP (at-most-once / marker-before-write / fail-closed / cents-only / no-false-completed confirmed); 2 warns folded in (markApplied transitioned warn-log; decryptCredentials throw→NO_META_CONNECTION). NEW schema RileyBudgetExecutionInput + new workflow file; NO migration/env/route. **NEXT after 1053 merges: 1B-1.6.**
- [~] 1B-1.6 SINK WIRING (flag-off): SURFACED → PR #1056 (commit f2071ffd, rebased on main 618ee498). NOT auto-merged: touches scripts/env-allowlist.local-readiness.json = mechanical merge-stop glob → HUMAN MERGE CALL. DECISION: current-budget sourced by a getCampaign pre-read in audit-runner.run() (the IO owner), flag-off ⇒ ZERO Meta reads, per-campaign read-fail→null→abstain (no fleet-halt); sink stays pure L2 (new budget-sink-dispatch.ts helper). v1 gating ENV-ONLY (RILEY_REALLOCATE_SELF_EXECUTION_ENABLED, no per-deployment field, no migration); every move parks for approval. All 10 gates green; independent review caught a DEAD-FLAG (submitter not threaded from CronDependencies→AuditRunner in inngest-functions.ts) → FIXED + pinned by an executeWeeklyAudit seam test. Files: budget-sink-dispatch.ts(+test), recommendation-sink.ts, audit-runner.ts (getCampaign pre-read + AdsClientInterface.getCampaign optional), inngest-functions.ts (CronDeps + thread), apps/api contained-workflows/app.ts/inngest.ts (submitRileyBudget closure + flag), .env.example, env-allowlist. NEXT after #1056 merges: 1B-2.
- [~] 1B-2 SPLIT (ORIENT 2026-06-14) into:
  - [~] 1B-2a executedAt:{not:null} scorer gate (BOTH findAttributableCandidates + findOverlapsForCampaign) + applyAct stamps executedAt on toStatus="acted" (the OPERATOR-acted regression fix — else operator pause/creative recs silently un-scored) + data-only backfill (executedAt=resolvedAt WHERE acted) + inngest dispatch-prefilter mirror. PR #1059 (commit 4f5e9f17, base 30529c49) — squash auto-merge ARMED (user authorized the migration/data-backfill stop-glob merge call). All gates green (db1088/api2048/eval/build); db:check-drift skipped (no local PG, hand-validated SQL). Independent review = SHIP. L4.
  - [ ] 1B-2b queryPaidValueCentsByCampaign + prefer paid in trueRoas (dollars once); booked as labeled fallback. DEFERRED. DEPENDS-ON the **Ledger** workstream (PR #1060 spec, branch docs/receipted-booking-object): paid value is `LifecycleRevenueEvent` reconciled by the `ReceiptedBooking` aggregate. 1B-2b should read per-campaign paid value THROUGH ReceiptedBooking (which carries attribution_confidence + source_evidence), NOT raw LifecycleRevenueEvent — Riley must not reallocate toward paid $ it can't confidently attribute to a campaign. So 1B-2b ⊢ Ledger's receipted-booking object existing + populated (live deposits). NAMING: "Ledger" agent (receipted-booking) ≠ Riley "outcome ledger" (recommendation-outcome-store, 1B-2a) ≠ build-loop STATE_LEDGER (these \*-loop-state.md files). L4+L2.

## NEXT slice: 1B-1.4 FRAME (verified 2026-06-14) — schema: ledger executedAt gate + MetaMutationAttempt

Layer 4 (db schema + migration + outcome-ledger reader). Riskiest-so-far on migration correctness → stays Opus.

- schema.prisma = packages/db/prisma/schema.prisma (2417 lines).
- PendingActionRecord @1607-1642: id `@default(uuid())`, `status String` (NOT enum; values seen: pending/acted/executed), existing `@@index([organizationId, status])` (+4 more). Has NEITHER executedAt NOR executionWorkUnitId. ADD: `executionWorkUnitId String?` + `executedAt DateTime?` + `@@index([organizationId, status, executedAt])`.
- RecommendationOutcome @644-696: `executableWorkUnitId String?` ALREADY EXISTS (@651) → NO schema change there; "populate" = runtime set in 1B-1.5.
- NEW model MetaMutationAttempt (mirror AgentState/ManagedChannel org-scoped+status+timestamps style): id uuid, organizationId, adAccountId, campaignId, `executionWorkUnitId String @unique`, `status String` (pending|applied|recovery_required), `heldUntil DateTime` (lease TTL), `observedPriorCents Int`, `requestedToCents Int`, `workTraceId String?`, createdAt/updatedAt, `@@index([organizationId, adAccountId, campaignId, status, heldUntil])`. Full lease-claim raw SQL deferred to 1B-1.5; 1B-1.4 = model + thin store round-trip + unique-rejection test only.
- MIGRATION: hand-write (migrate dev needs TTY). `prisma migrate diff --from-... --to-schema-datamodel --script` → new folder `packages/db/prisma/migrations/20260614HHMM00_<descr>/migration.sql` (timestamp AFTER 20260614120000_booking_attendance; latest 3: booking_attendance, creative_job_stage_failure, creative_job_revenue_proven_promoted_at). Index names ≤63 chars must match Prisma-generated. Then `migrate deploy` (PG reachable: db:check-drift OK now) + `pnpm db:check-drift`.
- OUTCOME-LEDGER SCORER GATE → **DECISION: DEFER to 1B-2** (not 1B-1.4). The scorer = `PrismaAttributableRecommendationStore.findAttributableCandidates` (recommendation-outcome-store.ts:256-266) + sibling `findOverlapsForCampaign` @282-291; both filter {sourceAgent:"riley", status:"acted", intent startsWith "recommendation.", resolvedAt...}. Adding `executedAt:{not:null}` there is SEMANTICALLY SUBTLE + regression-prone: the scorer serves ALL Riley intents (pause/creative/reallocate) and there are TWO acted-writers — `markActedByExecution` (MACHINE, post-Meta-write, resolvedAt=exec clock, riley-pause-executor.ts:50) and `applyAct` (OPERATOR, resolvedAt=new Date(), core act.ts). Gating would change whether operator-acted rows score, and needs a backfill (`executedAt:=resolvedAt WHERE status='acted'`). The gate is NOT needed by the 1B-1.5 executor (executor only STAMPS executedAt); it matters for the LEARNING LOOP, wired in 1B-2. So the gate + backfill + operator-acted-semantics + pause-still-scores test all move to 1B-2 (attribution slice). 1B-1.4 stays additive-only ⇒ zero scoring-behavior change ⇒ low-risk schema migration. NOT dropped — resequenced within the workstream.
  NOTE distinct model: ActionRequest.executedAt EXISTS @schema:1262 (Mira, prisma-action-request-store.ts) — UNRELATED to PendingActionRecord.executedAt; don't confuse.

## 1B-1.4 SCOPE (de-risked, additive-only) — DECIDED

1. schema.prisma: PendingActionRecord += `executionWorkUnitId String?` + `executedAt DateTime?` + `@@index([organizationId, status, executedAt])`. NEW model `MetaMutationAttempt` (fields per FRAME above). NO backfill (columns nullable; nothing reads executedAt yet).
2. `markActedByExecution` (recommendation-store.ts:303): ALSO set `executedAt: args.executedAt` + `executionWorkUnitId: args.executableWorkUnitId` in the updateMany data (purely additive; already has both values; keep the params.\_\_recommendation.executedWorkUnitId JSON stash too unless a reader is safely migratable). Stamps machine executions going forward; no reader gates on it yet ⇒ no behavior change.
3. MetaMutationAttempt store (packages/db/src): thin class — create + findByExecutionWorkUnitId (+ status fields); full lease-claim raw SQL deferred to 1B-1.5. Tests: round-trip + executionWorkUnitId unique rejects dup (mocked Prisma).
4. migration: hand-write via `prisma migrate diff ... --script` → migrations/20260614HHMM00_spec1b_meta_mutation_attempt/migration.sql (after 20260614120000); `migrate deploy` + `db:check-drift`. Index names ≤63 chars.
5. Tests: markActedByExecution data-payload stamps executedAt+executionWorkUnitId (mocked Prisma); MetaMutationAttempt store. Run `--filter @switchboard/db test` (+ api if any app spy touches markActedByExecution).
   ACCEPTANCE: marker/lease table + execution-truth columns exist + machine path stamps them, before 1B-1.5. Scorer gate is 1B-2.

- STORE WRITE pattern: updateMany + `count===0` throw (StaleVersionError) — see prisma-booking-store.ts. Mocked-Prisma test pattern: prisma-workflow-store.test.ts `mockPrisma()` = per-delegate `vi.fn()`, `updateMany: vi.fn().mockResolvedValue({count:1})`, cast `as unknown as PrismaClient`. db tests run WITHOUT Postgres (mock) but I have live PG for migration validation. Run `--filter api test` too if I tighten any update→updateMany (app spies).

## 1B-1.5 FRAME + DESIGN (verified 2026-06-14) — the real executor; SPLIT into 1B-1.5a (substrate) + 1B-1.5b (executor)

Landscape (file:line):

- Placeholder: apps/api/src/bootstrap/riley-budget-executor.ts:13-32 `buildRileyBudgetExecutorHandler()` → {intent,handler}; handler `execute(workUnit, services?)`; returns failed/EXECUTOR_NOT_WIRED. SWAP to `buildRileyBudgetExecutorHandler(deps)`.
- TEMPLATE: apps/api/src/services/workflows/riley-pause-execution-workflow.ts:141-345 (buildRileyPauseExecutionWorkflow(deps)→WorkflowHandler). Deps: getDeploymentCredentials(org,dep)→{kind:ok|none|org_mismatch, credentials{accessToken,accountId}}; createAdsClient(creds)=new MetaAdsClient(creds); markRecommendationActed; getApprovalState; now. Wrapper apps/api/src/bootstrap/riley-pause-executor.ts:69-122 builds those deps from deploymentStore/connectionStore/workTraceStore/recommendationStore.
- APPROVAL: buildGetApprovalState(workTraceStore) riley-pause-executor.ts:18-31 → {approvalOutcome,approvalRespondedBy}; ORG-MISMATCH guard `read.trace.organizationId!==organizationId`→empty. APPROVED_OUTCOMES={"approved","patched"} (pause wf:115). NO explicit bindingHash in executor; integrity = WorkTrace contentHash/traceVersion verified at read (verifyAndWrap). So executor reads frozen {adAccountId,campaignId,fromCents,toCents} from workUnit.parameters and trusts post-verify. **Binding test** = mutate a param → contentHash verify fails upstream; confirm mechanism at 1B-1.5b EXECUTE.
- RECEIPT PERSIST: WorkTrace.executionOutputs (core/src/platform/work-trace.ts:35, Record<string,unknown>; prisma-work-trace-store.ts:268 write JSON / :461-463 read). Executor RETURNS outputs:{...receipt...}; PlatformIngress persists as trace.executionOutputs; replay reads getByWorkUnitId(W).trace.executionOutputs. PRIMARY replay signal = the marker (findByExecutionWorkUnitId), receipt mirrored in executionOutputs.
- BLAST-RADIUS: ad-optimizer/src/blast-radius-contract.ts:115-136 assertWithinBlastRadius(contract,deltaCents,accountDailySpendCents)→{ok}|{ok:false,reason:DELTA_CAP|SHARE_CAP}; contract {maxDeltaCents,maxAccountSpendShare,guardrails[],rollback}. NO default builder/store → 1B-1.5a adds a DEFAULT_BLAST_RADIUS_CONTRACT constant (YAGNI: no contract store).
- CLIENT creds: connectionStore.findByDeploymentAndType(deploymentId,"meta-ads") + decryptCredentials(conn.credentials)→{accessToken,accountId}. GUARD: frozen adAccountId must match conn.accountId else fail (approved-account lock).
- REGISTRATION: contained-workflows.ts:249 builds placeholder (NO deps); :352 handlers Map entry; :458-471 workflowIntents. Swap :249 to inject deps (mirror pause at :243).
- SCHEMAS: packages/schemas/src/_.ts, `export _ from "./x.js"`in index.ts (~after riley-pause-execution :166). Add execution-receipt.ts. Verify zod`.safe()` available (else use .max(Number.MAX_SAFE_INTEGER)).
- RAW SQL: prisma-booking-store.ts:37-42 `tx.$executeRaw\`SELECT pg_advisory_xact_lock(${NS}::int4, hashtext(${org}))\`` — ::int4 cast MANDATORY (Prisma sends bigint), two-key bigint+int signature doesn't exist. Booking NS=920_001; pick a NEW ns for reallocate.

DESIGN DECISIONS (mine, plan deferred the exact SQL §3.3):

- SERIALIZATION folded into the step-7 marker claim (NOT a separate step-2 lease): short tx → pg_advisory_xact_lock(reallocate_ns::int4, hashtext(org:adAccount:campaign)) → SELECT active marker (status IN pending|recovery_required AND heldUntil>now, any executionWorkUnitId) → if found LEASE_CONTENDED; else INSERT pending marker (observedPriorCents=liveCents, requestedToCents=frozenTo, heldUntil=now+TTL) → commit (advisory lock released; NO HTTP in tx). Marker existence+TTL = the cross-tx lease; drift catches "another work unit already wrote". Satisfies "clean pre-write failure leaves NO marker" (marker only inserted at step 7, after guards). Status machine: pending→applied (happy) | pending→recovery_required (META_WRITE_ERROR/POST_WRITE_MISMATCH). NO separate "leased" state needed. executionWorkUnitId @unique = same-W replay guard.
- 10-STEP ORDER (§3.4): 1 getApprovalState(approved|patched, org-guard) else REALLOCATE_NOT_APPROVED; 2 replay-first findByExecutionWorkUnitId(W): applied→return receipt(0 Meta calls); pending/recovery→MUTATION_RECOVERY_REQUIRED(0 calls); 3 getCampaign throw→CAMPAIGN_BUDGET_UNREADABLE / dailyBudgetCents null→UNSUPPORTED_BUDGET_TOPOLOGY; 4 assessBudgetDrift(frozenFrom,live)→BUDGET_DRIFTED; 5 getAccountDailySpendCents throw→ACCOUNT_SPEND_UNREADABLE; deltaSigned=frozenTo-live; assertWithinBlastRadius→DELTA_CAP/SHARE_CAP; 6 advisory-locked claim (above)→LEASE_CONTENDED; 7 updateCampaignBudget(frozenTo) throw→mark recovery_required+META_WRITE_ERROR; 8 getCampaign re-read != frozenTo→mark recovery_required+POST_WRITE_MISMATCH; 9 persist receipt+mark applied+stamp PendingActionRecord.executedAt via markActedByExecution+release(status=applied) → completed. (Replay BEFORE getCampaign so drift never mis-fires on already-applied budget.) Cents end-to-end; no dollars.
- adAccountId is FROZEN (from params), used for lease key + account-spend + receipt + must == conn.accountId.

SPLIT:

- **1B-1.5a (substrate, no behavior):** L1 ExecutionReceiptSchema (schemas/src/execution-receipt.ts, PositiveSafeCents=z.number().int().safe().positive(); fields per plan §3.2) + export; L4 extend PrismaMetaMutationAttemptStore: claimLeaseAndMark (advisory-locked conditional INSERT, returns {claimed}|{contended}), markApplied(W), markRecoveryRequired(W,reason) — raw SQL mirrors booking; DEFAULT_BLAST_RADIUS_CONTRACT constant (ad-optimizer). Tests: receipt safeParse (positive-cents reject negative/float/unsafe; signed delta ok); store claim happy + contended (mock or integration) + transitions; contract sane. NO migration UNLESS the advisory-claim needs a partial-unique (it does NOT — advisory lock provides atomicity, no schema change).
- **1B-1.5b (executor):** replace placeholder with buildRileyBudgetExecutorHandler(deps) per the 10-step order; wire deps in contained-workflows.ts:249 (deploymentStore/connectionStore/workTraceStore/recommendationStore/metaMutationAttemptStore/contract). Full §13 matrix: happy/replay-receipt(0 calls)/replay-recovery/committed-marker-before-call(throw after write→recovery, replay→MUTATION_RECOVERY_REQUIRED 0 2nd calls)/no-marker-on-clean-fail/LEASE_CONTENDED-returns-no-hang/over-cap/POST_WRITE_MISMATCH/binding-mutation/NaN-zero-spend→SHARE_CAP. Mocked client+stores (NO live Meta, NO real money).

## Current slice: 1B-1.3 (re-key + pure propose + spendAmount into gate) — MERGED #1043

Base: origin/main @ da45b923 branch: feat/spec1b-1b3-spend-delta baseline_sha: da45b923 (reset onto latest main; full `pnpm build` green baseline)

SCOPE (pure/unit, nothing fires; mirrors how 1B-1.2 shipped submitter/executor without the sink calling them):

- **A RE-KEY** (L2+L5): riley-budget-dispatch.ts guard `emitted.actionType !== "scale"` (was shift_budget_to_source) + doc; riley-budget-submit-request.ts `parameters.actionType="scale"`. Update tests: riley-budget-dispatch.test, riley-budget-submit-request.test, riley-reallocate-gate.test (any actionType/rationale assertions). Re-key is INERT at runtime (sink does not call the candidate builder yet).
- **B PRODUCER (pure, L2)**: budget-reallocation-plan.ts add `REALLOCATE_SCALE_FACTOR=1.2` + `proposeCampaignReallocationCents(currentCents, factor=REALLOCATE_SCALE_FACTOR): number|null` = guard finite + currentCents>0 + factor>0, `Math.round(current*factor)`, null if result not a safe positive int or > MAX_SANE_DAILY_BUDGET_CENTS (reuse 1B-1.1 constant, no magic-number drift). Export from index. Tests: 5000→6000; rounding (5001→6001); non-finite/≤0 → null; over-MAX → null.
- **C SPEND (L5)**: riley-budget-submit-request.ts add `spendAmount: computeBudgetDelta(fromCents,toCents).deltaCentsMagnitude/100` (DOLLARS) to parameters. Tests: 5000→8000 ⇒ 30; 8000→5000 ⇒ 30 (magnitude). NaN-guard: candidate already validates, but guard the null return.
- **GATE PROOF**: extend riley-reallocate-gate.test — build a real proposal via buildRileyBudgetSubmitRequest and assert extractSpendAmount(proposal)===dollar magnitude AND the seeded require_approval(mandatory) policy still parks. Proves "structured spend-delta reaches the gate."

DEFERRED out of this slice → 1B-1.6: sink loop building the candidate (current via getCampaign read, proposed via proposeCampaignReallocationCents), submit, flag, audit-runner threading. Reason: needs a current-budget source (FRAME finding) = real wiring, money-path-sensitive, belongs after the real executor.

| step                                                                                        | done-condition | status |
| ------------------------------------------------------------------------------------------- | -------------- | ------ |
| A re-key dispatch+submit-request → "scale" + 3 tests green                                  | unit           | todo   |
| B proposeCampaignReallocationCents + REALLOCATE_SCALE_FACTOR + index export + tests         | unit           | todo   |
| C spendAmount(dollars) into submit-request params + tests                                   | unit           | todo   |
| GATE proposal carries spendAmount; real gate parks; extractSpendAmount reads dollars        | gate test      | todo   |
| VERIFY typecheck/test(ao+api)/lint/format/arch/build/eval/local-verify + adversarial review | all green      | todo   |

carry_forward (pause family + 1B-1.2 anchors):

- 1B-1.1 MERGED #1035 (getCampaign cents-verbatim, getAccountDailySpendCents dollars×100, updateCampaignBudget safe-int+≤MAX_SANE_DAILY_BUDGET_CENTS=100_000_000). 1B-1.2 MERGED #1038.
- Reallocate `adoptimizer.campaign.reallocate` on D9-2 FINANCIAL_AUTO_APPROVE_DENYLIST (governance-gate.ts:104) — never system_auto_approved.
- 1B-1.2 files: ad-optimizer/src/riley-budget-dispatch.ts (+budget-reallocation-plan.ts computeBudgetDelta/assessBudgetDrift, index.ts); apps/api riley-budget-submit-request.ts (RILEY_REALLOCATE_INTENT, idempotencyKey `mutate:riley:<recId>:reallocate`, actor {id:"system",type:"system"}), bootstrap/riley-budget-submitter.ts (5-branch), bootstrap/riley-budget-executor.ts (fail-closed EXECUTOR_NOT_WIRED placeholder); db/seed/riley-budget-governance.ts (seedRileyReallocatePolicies both-or-neither) wired seed-riley-ad-optimizer-deployment.ts + index.ts; contained-workflows.ts (eslint-disable max-lines; import+build rileyBudgetExecutor+handlers map+workflowIntents).
- Pause mirror anchors for 1B-1.6: submitRileyPause closure contained-workflows.ts:576-586; inngest.ts:393-396 build + :472-474 flag-gate RILEY_PAUSE_SELF_EXECUTION_ENABLED; sink recommendation-sink.ts:499-524; flag .env.example:374 + env-allowlist:111.

## 1B-1.5b EXECUTE (in progress, 2026-06-14 resume)

Branch feat/spec1b-1b5b-executor reset onto fresh origin/main @ 861399f5. Plan: .claude/spec1b-1b5b-plan.md (S1..S8).
ORIENT: anchors re-verified vs main, ZERO drift (Explore brief). PLAN-GRADE: 3 opus lenses (critic/completeness/code-grounded) → all findings = plan-text corrections, design SOUND; REVISE #1 folded in: receipt field is `approvedLifecycleId` (not approvalLifecycleId) + `kind`/`deploymentId` required + executedAt ISO; markApplied/markRecoveryRequired take {executionWorkUnitId,organizationId} NO reason; wrapper→async; retire 0-arg placeholder test; budget needs OWN resolvedBy sentinel (not pause's); type vi.fn args + run `--filter api build`. At-most-once ordering / clean-fail-no-marker / NaN-fail-closed / cents-e2e all VERIFIED sound (no design change). MERGE POSTURE: money-write executor → SURFACE-before-merge (human merge call) despite act-leg auto-merge authority.

## Log

- 2026-06-14: loop start; user authorized full-act-leg auto-merge (WTP override). 1B-1.2 ORIENT→shipped, MERGED #1038.
- 2026-06-14: RESUME after shutdown. 1B-1.4 #1047 + 1B-1.5a #1049 confirmed MERGED. Drove 1B-1.5b: ORIENT (zero drift) → PLAN → 3-lens opus plan-grade → REVISE #1 → EXECUTE (TDD) → VERIFY (10 gates + review=SHIP, 2 warns folded) → CONVERGE: PR #1053 squash auto-merge → MERGED (main 32a4fb4e).
- 2026-06-14: Drove 1B-1.6 (continue): ORIENT→FRAME(current-budget=audit-runner getCampaign pre-read)→EXECUTE(sink helper+pre-read TDD; flag wiring delegated)→VERIFY: 10 gates green BUT independent review caught DEAD-FLAG (inngest-functions.ts CronDeps→AuditRunner seam unwired, sonnet subagent searched wrong package) → FIXED + seam test (lesson saved to feedback_per_slice_review_misses_cross_slice_seams). SURFACED PR #1056 → user authorized auto-merge → MERGED (main 30529c49).
- 2026-06-14: Drove 1B-2 (continue): ORIENT recommended SPLIT (paid-value half partly inert + applyAct regression risk). Did 1B-2a (scorer gate + applyAct executedAt + backfill + dispatch-prefilter mirror), TDD, all gates green, independent review=SHIP (caught + I folded the prefilter consistency nit). SURFACED PR #1059 (migration stop-glob = human merge call). 1B-2b (paid-value→trueRoas) DEFERRED. ACT-LEG nearly complete: 1B-1.1..1.6 merged; 1B-2a awaiting merge; only 1B-2b remains (gated on deposit-loop paid value).
- 2026-06-14: user resolved design fork = "Campaign-budget scale". FRAME-verified scale-rec carries no budget + budgetDistribution null at audit ⇒ SPLIT 1B-1.3 into pure gate-plumbing (now) + sink wiring 1B-1.6 (later). Reset branch onto latest main da45b923, full build green. Starting 1B-1.3 EXECUTE.
