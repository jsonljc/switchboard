# Pilot-spine loop — externalized state (orchestration scratch)

Durable per-merge record lives in memory note `project_pilot_spine_audit_2026_06_07`.
This file is the loop's flat state so context stays bounded across slices (not committed to git).

**Goal:** close the remaining pilot-spine slices to merged PRs on main, proudest version.
**Authority (user-approved):** auto-merge per slice after green required CI + clean adversarial review. F-05 launch-mode = my GTM pick.
**Per-slice pipeline:** verify-open (live) -> brainstorming -> writing-plans -> fan-out plan grade (substantive slices) -> TDD execute -> adversarial review -> green CI -> squash-merge -> cleanup -> update this ledger + memory note. One worktree per slice off latest origin/main. Right-size ceremony: trivial config slices skip the 3-agent fan-out.
**Base:** origin/main @ dfe570c8 (F-02 merged). Re-fetch before each slice.

| #   | slice                         | status                  | PR    | merge SHA | notes                                                                                                                                                                                                                                                     |
| --- | ----------------------------- | ----------------------- | ----- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | F-20 client-flag              | CLOSED (already fixed)  | #1003 | cf91aa64  | static switch + eslint + no-dynamic-public-env CI guard, 2026-06-12; no slice                                                                                                                                                                             |
| 2   | F-09 trustHost                | MERGED                  | #1033 | b968f1da  | REFRAMED non-blocker (dashboard on Vercel, VERCEL=1 already trusts). Shipped trustHost:true as explicit-trust hardening, honest rationale. CI all green                                                                                                   |
| 4   | F-16 residual                 | CLOSED (non-triggering) | -     | -         | verified: signup lacks Riley deploy but no pre-GET-config action needs Riley; zero work                                                                                                                                                                   |
| 5   | F-05 launch-mode + OAuth gate | MERGED                  | #1036 | 10ec0132  | isSelfServeSignupOpen helper + signIn callback (deny net-new OAuth/email, allow existing -> NO lockout) + createUser backstop. GTM=waitlist. Review SHIP (source-verified vs @auth/core). FOLLOW-UP before prod waitlist flip: scripts/provision-pilot.ts |

## Loop complete (autonomous portion) 2026-06-14

Auto-merge slices done. Shipped this loop: F-09 #1033 (b968f1da, reframed hardening), F-05 #1036 (10ec0132). Pre-loop: F-02 #1030. Already-done discovered via verification: F-20 #1003, F-16 residual non-triggering. ALL 8 original blocks-pilot resolved/addressed.
GATED (awaiting user decision, NOT executed): escalation-reply (cross-pkg interface change, customer-facing, runtime-unverifiable locally). Brief: .claude/escalation-reply-brief.md.
Named follow-ups: scripts/provision-pilot.ts (before prod waitlist flip); WhatsApp live round-trip (top residual, needs sandbox).
| 3 | escalation-reply | MERGED | #1040 | c0193942 | USER RE-OPENED ("finish all slices"). Archaeology DONE: NO CState row exists on escalate path (skill-mode adapter update-only) -> write owner reply to ConversationMessage (GET reads it) + best-effort status flip; deliver via contact.phoneE164 (WA adapter passes `to` straight = proactive-sender.ts:154; gateway path's visitor-<digits> `to` is a SEPARATE pre-existing delivery bug, NOT my scope). Design: route resolves contactId via WorkTrace (F-19 mirror) BEFORE the released-flip; contact path -> ConversationMessage + phoneE164 delivery, no CState dependency; WorkTrace-miss -> legacy threadId path UNCHANGED (no regress); unresolvable=502 not 404; MUST NOT 404 escalate path. Plan: .claude/escalation-reply-plan.md. baseline 373cdc61. CONVERGE: auto-merge if CI+review green + gateway-path-unchanged proven (escalate path is 404 today so fix is strictly better, low-risk); surface the runtime-WA caveat in PR+report |

Excluded: WhatsApp live round-trip (no local sandbox; manual validation milestone, not a code slice).

## Log

- 2026-06-14: loop started after F-02 (#1030, dfe570c8) merged.
- 2026-06-14: parallel verification done. F-20 already fixed (#1003), F-16 residual non-triggering. 3 real slices left: F-09, escalation-reply, F-05. Starting F-09.
- 2026-06-14: F-09 #1033 + F-05 #1036 merged; autonomous portion marked complete; escalation-reply gated for user decision.
- 2026-06-14: USER RE-OPENED escalation-reply ("run autonomously end to end, finish all slices"). Worktree fix/escalation-reply-dual-path @ 373cdc61 (clean baseline; pnpm reset fixed a stale chat TS7006 false alarm). Archaeology (2 opus/sonnet passes) resolved all 4 OPEN UNCERTAINTIES + sendProactive addressing. Executing build-loop: FRAME done -> PLAN -> fan-out grade -> TDD execute -> verify -> converge.
- 2026-06-14: escalation-reply IMPLEMENTED (opus impl, 4 commits) + VERIFIED + reviewed. Fan-out plan-grade (3 opus) caught real fixes folded in: ConversationMessage real cols (orgId/content/direction:"outbound"/metadata.sender:"owner"), full ReleaseEscalationResult (conversationId+appendedReply), 502-only-for-ContactNotFound (keep CState-404), mock ripple. Gates GREEN (core 4078/db 1038/api 1963, typecheck post-build exit 0, build/lint/format/arch). Final /code-review: NO P0/P1; verified-clean = org-scope all 4 new queries + gateway byte-identical + error mapping + exact cols + full result. PR #1040 auto-merge enabled. cwd-drift hit the impl subagent again (caught pre-commit, reverted, no wrong-branch commit) = feedback_subagent_worktree_drift holds. DEFERRED as a focused follow-up (reviewer P2+nits, intertwined w/ pre-existing flip-before-resolve ordering; naive status-guard regresses re-reply-after-fail): reply-route hardening (idempotency + resolve-before-flip + null-phoneE164 fail-fast + 502 copy) + owner-vs-agent attribution (cross-app GET+dashboard). Post-merge: memory note + teardown + report.
- 2026-06-14: escalation-reply #1040 MERGED (c0193942), auto-merge on green CI. PILOT-SPINE WORKSTREAM FULLY COMPLETE: all blocks-pilot resolved (F-02 #1030, F-09 #1033, F-05 #1036, escalation-reply #1040; F-20 #1003 + F-16 already-done). Residual non-code milestones: live WhatsApp round-trip (escalation-reply + the gateway visitor-<digits> delivery bug), scripts/provision-pilot.ts (before prod waitlist flip), reply-route-hardening + owner-attribution follow-ups (see #1040 PR body). Worktree torn down.
