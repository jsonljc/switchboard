# 1B-1.5b executor — TDD plan (ephemeral scratch, uncommitted)

Slice: replace the fail-closed placeholder reallocate executor with the real read-modify-re-read
executor, per the locked 10-step order in `.claude/spec1b-act-leg-loop-state.md` (1B-1.5b FRAME+DESIGN).
Worktree: `.claude/worktrees/spec1b-act-loop` branch: `feat/spec1b-1b5b-executor` base: origin/main @ 861399f5.
Authority: act-leg auto-merge (WTP override) BUT the merge is a money-write executor → SURFACE-before-merge
(human merge call) per build-loop money/governance stop-floor; flags-off + mocked + no-real-money interlocks hold.

## Ground-truth anchors (verified 2026-06-14, zero drift)

- Template to clone: `apps/api/src/services/workflows/riley-pause-execution-workflow.ts` (`buildRileyPauseExecutionWorkflow(deps)`).
- Wrapper to clone: `apps/api/src/bootstrap/riley-pause-executor.ts` (`buildGetApprovalState`, `buildMarkRecommendationActed`, `buildRileyPauseExecutorHandler`).
- Placeholder to replace: `apps/api/src/bootstrap/riley-budget-executor.ts` (`buildRileyBudgetExecutorHandler()` → EXECUTOR_NOT_WIRED).
- Registration: `contained-workflows.ts:180` import; `:249` build (change to `await ...(prismaClient, workTraceStore)`, mirror pause `:243`); `:352` handlers map; `:458-471` workflowIntents (unchanged).
- Substrate (1B-1.5a, on main — VERIFIED file:line): `PrismaMetaMutationAttemptStore` {findByExecutionWorkUnitId(id)→row|null (`.status`: "pending"|"applied"|"recovery_required"), claimLeaseAndMark(input)→{claimed:true,row}|{claimed:false}, markApplied({executionWorkUnitId,organizationId})→{transitioned}, markRecoveryRequired({executionWorkUnitId,organizationId})→{transitioned} — NO reason param; reason lives only in the returned error.code} NS 920_002. claimLeaseAndMark input REQUIRES {organizationId, adAccountId, campaignId, executionWorkUnitId, observedPriorCents, requestedToCents, workTraceId?, now, leaseTtlMs?} (real field names, not org/adAccount/W).
- `ExecutionReceiptSchema` REQUIRED fields (VERIFIED): `kind`=literal "campaign_budget_reallocation", `organizationId`, `deploymentId`, `adAccountId`, `campaignId`, `workTraceId`, `executionWorkUnitId`, **`approvedLifecycleId`** (NOT approvalLifecycleId), `bindingHash`, `requestedFromCents`/`requestedToCents`/`observedPriorCents`/`appliedCents` = PositiveSafeCents (int.safe.positive), `deltaCentsSigned` = int.safe (may be negative), `executedAt` = z.string().datetime() (ISO STRING, use `.toISOString()`).
- `DEFAULT_BLAST_RADIUS_CONTRACT` ($50 delta / 0.25 share) + `assertWithinBlastRadius(contract, deltaCents, accountDailySpendCents)→{ok}|{ok:false,reason:DELTA_CAP|SHARE_CAP}` — uses Math.abs(deltaCents) (signed delta OK, magnitude-capped); NaN/non-finite delta→DELTA_CAP, non-finite/≤0 spend→SHARE_CAP (fail-closed).
- Meta client (1B-1.1): `getCampaign(id)→{...,dailyBudgetCents:number|null}` (cents verbatim); `updateCampaignBudget(id,cents)` (cents, safe-int+positive); `getAccountDailySpendCents()→number|null` (dollars×100→cents); pure `assessBudgetDrift(frozenFrom,live)→{ok}|{ok:false,reason:BUDGET_DRIFTED}`.
- Frozen params (riley-budget-submit-request.ts): {recommendationId, actionType:"scale", adAccountId, campaignId, fromCents, toCents, spendAmount, rationale, evidence}.
- Receipt sources off WorkTrace (getByWorkUnitId.trace): approvedLifecycleId←`approvalId` (set at park time, platform-ingress.ts:349), bindingHash←`contentHash` (persisted every trace create), workTraceId←`traceId` (always present), deploymentId←`workUnit.deployment.deploymentId`, kind=literal. approvalId/contentHash optional on the WorkTrace TYPE → FAIL CLOSED (REALLOCATE_NOT_APPROVED, missing binding) if absent on a reallocate (money path). buildGetApprovalState currently returns ONLY outcome/respondedBy → MUST be extended to also return approvalId/contentHash/traceId, else the fail-closed reads undefined and is vacuous.
- `markActedByExecution({id, organizationId, executableWorkUnitId, resolvedBy, executedAt})` stamps PendingActionRecord.executedAt (additive, from 1B-1.4).

## 10-step executor order (locked)

0 parse params (RileyBudgetExecutionInput) → INVALID_REALLOCATE_INPUT.
1 getApprovalContext(approved|patched, org-guard) else REALLOCATE_NOT_APPROVED. Also require approvedLifecycleId(=approvalId) + bindingHash(=contentHash) + workTraceId present else REALLOCATE_NOT_APPROVED (missing binding). 0 Meta calls before this passes. (Param-mutation-after-approval is caught UPSTREAM by WorkTrace contentHash verify at read/dispatch — verifyAndWrap; the executor only RECORDS bindingHash from the verified trace, so a mutated payload never reaches dispatch. No executor-side binding-mutation test needed; one-line note suffices.)
2 replay-first: attemptStore.findByExecutionWorkUnitId(W): if marker.status==="applied" → read receipt from executionOutputs, return completed{receipt,replayed} (0 Meta); if present but pending/recovery_required (or applied w/o receipt) → failed MUTATION_RECOVERY_REQUIRED (0 Meta).
3 creds: getDeploymentCredentials → org_mismatch→DEPLOYMENT_ORG_MISMATCH; none→NO_META_CONNECTION. Guard frozen adAccountId === creds.accountId else ACCOUNT_MISMATCH. (fresh client per call.)
4 getCampaign throw→CAMPAIGN_BUDGET_UNREADABLE; dailyBudgetCents null→UNSUPPORTED_BUDGET_TOPOLOGY. live=dailyBudgetCents.
5 assessBudgetDrift(frozenFrom, live).ok===false→BUDGET_DRIFTED.
6 getAccountDailySpendCents throw→ACCOUNT_SPEND_UNREADABLE (null is allowed → passes to cap which fails closed SHARE_CAP). deltaSigned=frozenTo-live; assertWithinBlastRadius(contract, deltaSigned, accountSpend).ok===false→reason (DELTA_CAP|SHARE_CAP). NO marker through here (clean pre-write fail leaves NO marker).
7 claimLeaseAndMark({organizationId,adAccountId,campaignId,executionWorkUnitId:W,observedPriorCents:live,requestedToCents:frozenTo,workTraceId,now})→{claimed:false}→LEASE_CONTENDED (no write, no hang).
8 updateCampaignBudget(campaign, frozenTo) throw→markRecoveryRequired({executionWorkUnitId:W,organizationId})+META_WRITE_ERROR.
9 getCampaign re-read; appliedCents !== frozenTo→markRecoveryRequired({executionWorkUnitId:W,organizationId})+POST_WRITE_MISMATCH.
10 build receipt (set kind + deploymentId + approvedLifecycleId + bindingHash + workTraceId; cents end-to-end; deltaCentsSigned=appliedCents-observedPriorCents; executedAt=now().toISOString()) → ExecutionReceiptSchema-validate → markApplied({executionWorkUnitId:W,organizationId}) → markActedByExecution (stamp PendingActionRecord.executedAt; id=source recommendationId; bookkeeping; not_found→warn-log, failure→error-log, NEVER fails the unit, mirror pause) → return completed{outputs:{receipt, replayed:false}}.

## Steps (each: write test RED first, then impl GREEN; type ALL vi.fn<...>() spy args — untyped reds the api BUILD while vitest greens)

- [ ] S1 SCHEMA (L1): NEW file `packages/schemas/src/riley-budget-execution.ts` with `RileyBudgetExecutionInput` zod (recommendationId,adAccountId,campaignId strings; fromCents,toCents = z.number().int().safe().positive()); add `export * from "./riley-budget-execution.js"` to `packages/schemas/src/index.ts`; co-locate `riley-budget-execution.test.ts` (src root, precedent execution-receipt.test.ts). RED: safeParse rejects float/negative/zero cents; accepts valid. (`pnpm --filter @switchboard/schemas test`)
- [ ] S2 WORKFLOW happy path (L5): new `apps/api/src/services/workflows/riley-budget-execution-workflow.ts` with `RileyBudgetExecutionDeps`, `RileyBudgetCredsResult`, `RILEY_REALLOCATE_EXECUTION_RESOLVED_BY="riley_reallocate_self_execution"`, reason-code consts, `buildRileyBudgetExecutionWorkflow(deps): WorkflowHandler`. RED: happy-path — approved+binding, no marker, live==frozenFrom, within cap, claim ok, write ok, re-read==frozenTo → completed + ExecutionReceiptSchema.safeParse(outputs.receipt).success (kind+deploymentId+approvedLifecycleId+bindingHash+workTraceId all set, executedAt ISO, deltaCentsSigned signed) + markApplied called + markActedByExecution called with id=recommendationId + Meta call count = getCampaign(pre)+accountSpend+update+getCampaign(post). (`pnpm --filter api test`)
- [ ] S3 approval+replay legs: REALLOCATE_NOT_APPROVED (no/rejected outcome → 0 Meta, creds NOT resolved); missing binding (approvalId/contentHash/traceId absent) → REALLOCATE_NOT_APPROVED; replay-receipt (marker applied + receipt in executionOutputs → completed replay, 0 Meta); replay-recovery (pending/recovery_required marker → MUTATION_RECOVERY_REQUIRED, 0 Meta). RED first.
- [ ] S4 creds+read legs: DEPLOYMENT_ORG_MISMATCH; NO_META_CONNECTION; ACCOUNT_MISMATCH (frozen adAccountId != creds.accountId); CAMPAIGN_BUDGET_UNREADABLE (getCampaign throws); UNSUPPORTED_BUDGET_TOPOLOGY (dailyBudgetCents null); BUDGET_DRIFTED (live != frozenFrom); ACCOUNT_SPEND_UNREADABLE (getAccountDailySpendCents throws). Each asserts claimLeaseAndMark NOT called (NO marker). RED first.
- [ ] S5 cap+claim+write legs: DELTA_CAP (no marker); SHARE_CAP (no marker); NaN/null account-spend→SHARE_CAP; LEASE_CONTENDED ({claimed:false} → no write, returns, no hang); META_WRITE_ERROR (update throws → markRecoveryRequired({executionWorkUnitId,organizationId}) called); SEQUENCED at-most-once (claim ok → update throws → marker recovery_required → re-dispatch SAME workUnit → replay-recovery returns MUTATION_RECOVERY_REQUIRED with ZERO further Meta calls — §13 finding #1); POST_WRITE_MISMATCH (re-read != frozenTo → markRecoveryRequired); markActedByExecution not_found is warn-logged + does NOT fail the unit (mirror pause). RED first.
- [ ] S6 WRAPPER (L5): replace placeholder in `apps/api/src/bootstrap/riley-budget-executor.ts` — make it `async buildRileyBudgetExecutorHandler(prismaClient, workTraceStore): Promise<{intent,handler}>`. Instantiate PrismaDeploymentConnectionStore/PrismaDeploymentStore/PrismaRecommendationStore/PrismaMetaMutationAttemptStore (+decryptCredentials, MetaAdsClient). Build deps: getApprovalContext (own closure — extend the buildGetApprovalState pattern to ALSO return approvalId/contentHash/traceId; org-scoped), getDeploymentCredentials (clone pause), createAdsClient=new MetaAdsClient, getExistingReceipt (ExecutionReceiptSchema.safeParse(trace.executionOutputs?.receipt)), markRecommendationActed (NEW builder with RILEY_REALLOCATE sentinel — do NOT reuse buildMarkRecommendationActed which hardcodes the pause sentinel), contract=DEFAULT_BLAST_RADIUS_CONTRACT. **RETIRE the existing placeholder test** `apps/api/src/bootstrap/__tests__/riley-budget-executor.test.ts` (it calls the 0-arg signature → build-red); rewrite to mirror `riley-pause-executor.test.ts` (`await ...(mockPrisma(), fakeWorkTraceStore())`). RED: wrapper test — getApprovalContext returns outcome+approvalId+contentHash+traceId, org-scoped (cross-org trace → empty).
- [ ] S7 REGISTRATION: `apps/api/src/bootstrap/contained-workflows.ts:249` → `const rileyBudgetExecutor = await buildRileyBudgetExecutorHandler(prismaClient, workTraceStore);` (prismaClient+workTraceStore both in scope; pause uses both at :243). grep confirms only 2 call sites (this + the now-rewritten test). typecheck.
- [ ] S8 VERIFY: `pnpm typecheck`; `pnpm --filter api test` AND `pnpm --filter api build` (catches untyped-vi.fn TS2493); `pnpm --filter @switchboard/schemas test`; `pnpm lint`; `pnpm format:check`; `pnpm arch:check`; `pnpm eval:riley` (api-side wiring, not the decision engine, but cheap defensive run); `CI=1 npx tsx scripts/local-verify-fast.ts`; `pnpm build`. NO schema migration / NO new env var / NO route-allowlist (executor is a workflow handler, not a Fastify route; flag+env land in 1B-1.6). Independent fresh-context review of `git diff origin/main...HEAD`.

## Reason-code taxonomy (all outcome:"failed" except the abstain/replay-receipt completed legs)

INVALID_REALLOCATE_INPUT, REALLOCATE_NOT_APPROVED, MUTATION_RECOVERY_REQUIRED, DEPLOYMENT_ORG_MISMATCH,
NO_META_CONNECTION, ACCOUNT_MISMATCH, CAMPAIGN_BUDGET_UNREADABLE, UNSUPPORTED_BUDGET_TOPOLOGY,
BUDGET_DRIFTED, ACCOUNT_SPEND_UNREADABLE, DELTA_CAP, SHARE_CAP, LEASE_CONTENDED, META_WRITE_ERROR,
POST_WRITE_MISMATCH.
