# S8a (DeploymentMemory provenance + valid-time + invalidate-not-delete) loop — externalized state (scratch, not committed)

Durable record: memory note project_ai_infra_uplift_state. Parent design: .claude/ai-infra-uplift-S8-design-decomposition.md. Plan: .claude/ai-infra-uplift-S8a-plan.md.

Goal: S8a — add nullable provenance (`source`) + bi-temporal valid-time (`validFrom`/`validTo`/`invalidatedAt`) columns to DeploymentMemory + switch decay/evict from hard-delete to invalidate (soft-remove, preserve history). FIRST slice of S8 (govern memory writes through PlatformIngress).
Authority: AUTONOMOUS-WITH-GUARDRAILS but S8a touches the **prisma stop-glob** -> SURFACE the PR for human merge (NEVER auto-merge). Per the loop firing = greenlight of the S8 design + A->B->C sequence.
Task-size: standard (one bounded PR).
Base: origin/main @ ea7a30cdd (re-fetched 2026-06-22; advanced past the user's verified 115c15d5f via dashboard rehaul/readiness #1242-#1245, none touch memory). baseline_sha: ea7a30cdd (worktree .claude/worktrees/ai-s8a, branch feat/ai-s8a-memory-provenance; worktree:init+install+reset+build all GREEN).
merge_safety: stop-glob touched = YES (packages/db/prisma/** + migrations/**) -> SURFACE. independent_review = PASS (opus fresh-context; no Critical; 2 Important triaged = deliberate decay-category-agnostic decision [flagged in PR] + S8b-scope owner-source). SURFACED -> PR #1247 (rebased onto origin/main d5dcbaa5e; MERGEABLE; CI running). Final SHAs a9d78abbf..ce42f0e01 (trailer-amended). Worktree .claude/worktrees/ai-s8a KEPT until #1247 merges.

## ORIENT brief (each claim tool-verified on ea7a30cdd)

- Premise CONFIRMED: memory writes bypass PlatformIngress (compounding-service.ts has zero `.submit(`); no `memory.write` intent (grep empty); all 4 columns absent from both the Prisma model and the Zod schema.
- Prisma model `DeploymentMemory` schema.prisma:769-787 (no @@map; @@unique[org,deployment,category,content]; @@index [org,deployment] / [org,deployment,category,canonicalKey] / [confidence]).
- Store `PrismaDeploymentMemoryStore` packages/db/src/stores/prisma-deployment-memory-store.ts: create(16-30), incrementConfidence(32-48, updateMany+count===0->StaleVersionError), 6 reads (listByDeployment/listHighConfidence/findByCategory/findByCategoryAndCanonicalKey/countByDeployment/findEvictionCandidate — NONE filter invalidatedAt), delete(91-96 = deleteMany+count===0->StaleVersionError; the EVICT hard-delete), decayStale(116-135 = updateMany decrement where confidence>floor). No row->domain mapper.
- Interface `CompoundingDeploymentMemoryStore` declared in CORE (compounding-service.ts:36-80; structurally implemented by the db store). `PatternDecayMemoryStore` (inngest-functions.ts:8-15) = decayStale only. db already imports StaleVersionError from core, so adding invalidate() crosses no layer.
- Writers (all DIRECT, no ingress) compounding-service.ts: create@386 (fact) + create@441 (FAQ) = source "conversation-compounding"; create@509 (pattern) = source "pattern-merge"; increments@356/412/481/540 (no create, no source); evict delete@372 (catches StaleVersionError -> drops fact).
- Decay cron: inngest.ts:716-740 -> executeDailyPatternDecay (inngest-functions.ts:38-62) -> store.decayStale. Config (inngest.ts:659-661): windowDays=180, decayAmount=0.1, floor=0.3. New facts created at computeConfidenceScore(1,false)=0.5 (> floor).
- Operator-forget = SEPARATE store PrismaOwnerMemoryStore.deleteMemory (deleteMany) — OUT of S8a scope; stays a true delete (erasure correctness).
- Zod `DeploymentMemorySchema` schemas/deployment-memory.ts:57-69 has ZERO runtime consumers (grep only finds the dist artifact) -> editing it is zero-risk forward-prep.
- Migration SQL types (from S6a + robin templates): nullable `TEXT` / `TIMESTAMP(3)`; hand-author (PG down) + CI db:check-drift. Latest migration ts = 20260621200000.

## FRAME pressure-test conclusions (focused re-validate of the greenlit design; NOT greenfield)

1. valid-time: keep BOTH validTo (valid-time end) + invalidatedAt (system-time soft-remove) = bi-temporal; liveness predicate = `invalidatedAt IS NULL`. In S8a's automatic paths both set = now (honest: no distinct valid-time). Naming nit: temporal-fact.ts (unrelated subsystem) uses `validUntil`; following the greenlit `validTo`, flag for reviewer.
2. invalidate-not-delete: ALL 6 reads + the count/eviction reads MUST filter invalidatedAt:null (cap correctness, not hygiene). evict@372 delete()->invalidate(). decay = 2-pass. operator-forget untouched.
3. **KEY CATCH (design didn't spell out): tombstone-collision.** soft-invalidate + content-unique + read-filter => a re-asserted evicted/decayed fact hits P2002 against the tombstone and can never re-learn (today's hard-delete lets re-create succeed; S8a would REGRESS this). Resolution = store-internal **resurrect-on-invalidated-collision** in create(): on P2002, findFirst by the unique tuple (incl. invalidated); if invalidatedAt!=null -> revive (clear invalidatedAt/validTo, set source/validFrom, confidence=input, sourceCount=1, lastSeenAt=now), return id; if LIVE -> rethrow P2002 (preserves today's app-level pattern recovery @529-543 unchanged). Deterministic; contained in db store; "ship clean" (latent: only fires at the 500-cap or 180d-stale, but fix now).
4. actor-principal: not exercised in S8a; S8c uses seeded {id:"system"} + trigger:"internal" (robin-recovery cron pattern). Deferred.

- None of the above is a genuinely-new product/consent/principal decision -> proceed autonomously (no gate-2 STOP). Gate-1 (SURFACE-for-merge) fires at PR open.

| step                                 | done-condition (test/cmd)               | RED proof                          | status  | evidence |
| ------------------------------------ | --------------------------------------- | ---------------------------------- | ------- | -------- |
| T1 schemas enum+fields               | --filter schemas test deployment-memory | enum undefined                     | pending |          |
| T2 prisma cols+migration             | prisma validate + db:generate           | n/a (schema)                       | pending |          |
| T3 store invalidate+reads+decay      | --filter db test store                  | invalidate not fn / where mismatch | pending |          |
| T4 store create provenance+resurrect | --filter db test store                  | source unset / no resurrect        | pending |          |
| T5 service evict->invalidate+source  | --filter core test compounding          | invalidate not called              | pending |          |

gate_results: typecheck=PASS lint=PASS format=PASS arch=PASS build=PASS verify-fast=PASS security=PASS schemas-test=PASS db-test=PASS(unit; 3 integration suites ENV/PG-down) core-test=PASS api-test=PASS db:check-drift=CI(PG-down) review=PASS(no Critical; 2 Important triaged below = doc-fix + S8b-scope, no code bug)

## Independent review (opus, fresh-context, origin/main...58c3ebd70) + triage

Verdict: "Ready to merge — With fixes." No Critical. Strengths confirmed: migration drift-faithful, read-filter total (cap correct), decay pass-2 staleness pinned by toEqual, invalidate count===0 guard, resurrection deterministic + dedup-axis-correct, producer-populated, layers clean, tests real (api 2396 pass).

- IMPORTANT-1 (decay invalidates taste/revenue_proven too; my "never invalidated" premise was WRONG): VERIFIED true (decayStale is category-agnostic by design). DECISION = KEEP category-agnostic (faithful to design "decay sets invalidatedAt at floor"; scoping pass-2 would invent scope + create a decrement-but-never-invalidate inconsistency). SAFE: creative crons' create() hits the resurrection on re-assert; <=0.3 rows are below the 0.66 surfacing threshold (invisible bar freeing cap). Cross-workstream behavior change (stale at-floor taste/revenue rows now soft-removed vs lingering) -> FLAG prominently in the PR for the human merge call. Existing pass-2 toEqual test pins "no category filter". NO code change.
- IMPORTANT-2 (owner route should pass source:"operator"; resurrection nulls a pattern's canonicalKey on that edge): PUSH BACK -> out of S8a scope. Owner route is apps/api; design RESERVES "operator"/"decay" sources for S8b/S8c governed writers. Resurrection-takes-new-key is dedup-correct + commented. Edge is extremely narrow (operator re-asserts a pattern whose content == a tombstone). S8b follow-up. NO code change.
- MINOR-3 (resurrection findFirst->update TOCTOU, last-writer-wins): benign, deterministic, non-financial. NO change.
- MINOR-4 (dead bypass stores PrismaAggregate/Customer/OwnerMemoryStore read DeploymentMemory unfiltered): ZERO call sites today (dead). S8b/S8c cleanup note. NO change.
  S8b FOLLOW-UPS (carry to S8b): (a) tag owner-memory-route writes source:"operator"; (b) DONE in S8a now (sibling-store filters added); (c) when decay routes through ingress (S8c), the category-agnostic invalidation persists.

## Review #2 (user-requested deeper pass: 2 opus, arch-alignment + works-as-intended) -> FIXES APPLIED

- **CRITICAL caught (my VERIFY MISSED IT):** the branch FAILED the CI-blocking `check-routes --mode=error` ("Route governance enforcement", ci.yml) store-mutation gate -> 2 NEW violations: resurrection `update`@:53 had no organizationId in WHERE (DOCTRINE §10/§12); decay pass-2 `updateMany`@:194 lacked the `store-mutation-global` directive. `local-verify-fast` does NOT cover packages/db store mutations -> the org-scope gate is SEPARATE. FIXED: resurrection -> `updateMany` org-scoped + `invalidatedAt:{not:null}` + count===0-rethrow guard (also closes the TOCTOU) + return {id}; pass-2 got the directive. Re-confirmed check-routes exit 0.
- **IMPORTANT (both reviews): 3 sibling stores read DeploymentMemory unfiltered** (customer/aggregate/owner) -> tombstone-leak risk when S8b/S8c wire them. Confirmed DEAD callers today. FIXED: added invalidatedAt:null to all 3 reads + updated 2 exact-where tests (prisma-scoped-stores.test.ts). Now the "all reads filtered" invariant is honest.
- MINOR: temporal-fact naming-divergence note added to deployment-memory.ts; isPrismaUniqueConstraintError hand-roll KEPT (matches db-internal precedent).
- Fix commits: f47a27153 (org-scope+pass2+temporal-note) + 93810692d (sibling filters), both trailered. FULL re-verify on 93810692d ALL-GREEN (check-routes 0 / typecheck 22pkgs / build / api-test 2399 / arch / verify-fast / security / format). Re-pushed (ff ce42f0e01..93810692d). PR #1247 head now 93810692d; three-dot diff 14 files +421/-63.

## MERGE (user /goal: merge cleanly local+remote, then session hygiene)

- User authorized the merge (the human-merge gate for the prisma stop-glob). origin/main moved again -> rebased onto 665cc1cd7 (A8 #1248 Alex reschedule guard, UNRELATED, clean). Local re-verify on the rebased base GREEN (check-routes exit 0 / db-store test / typecheck / build). Force-pushed -> HEAD 08aba6fd3 (final SHAs 55906e061..08aba6fd3). CI watching (background) until required checks (typecheck/lint/test/security; real-DB test validates the migration drift) pass -> then squash-merge (NO --delete-branch from a worktree; repo auto-deletes the remote head) -> ff primary main + remove worktree + delete local branch -> mark backlog/memory S8a MERGED. Next slice = S8b off merged main.
- DONE 2026-06-22: CI ALL-GREEN on 08aba6fd3 (typecheck / lint / test[11m20s real-DB, validates migration drift] / security / architecture / CodeQL / docker / all-evals). Pre-merge re-check: origin/main moved 665cc1cd7->2205fcccf (#1249 dashboard CSS, unrelated) -> MERGEABLE/CLEAN, no rebase needed. SQUASH-MERGED #1247 -> main `7d14402bc`. Teardown CLEAN: primary main ff'd to 7d14402bc (migration tracked), worktree removed+pruned, local+remote branch deleted, stale remote-tracking ref pruned, no stashes, no dangling. Scratch s8a-pr-body.md removed; ledger+plan+design kept for the S8b resume. **S8a COMPLETE + MERGED.** Next FRESH session: S8b (memory.write intent + governance) off main @ 7d14402bc, new worktree.
  EXECUTE commits (5, off ea7a30cdd): 62e9b3c26 schemas / eeaa74f50 prisma+migration / 38d970b25 store-invalidate+reads+decay / 4ae4e73a9 store-create-provenance+resurrection / 58c3ebd70 service-rewire. Diff 10 files +392/-60, scope-clean.
  carry_forward: PLAN + 2B plan-grade DONE. Plan-grade = REVISE (rev1/cap2): 2 reviewers flagged (a) decayStale pass-2 must pin staleness lastSeenAt<cutoff [FIXED: full toEqual where + omit lastDecayedAt OR-guard, documented], (b) riskiest bodies were deferred to non-existent ledger code [FIXED: inlined full decayStale+create bodies in plan]; +canonicalKey reset in resurrection; +scope-boundary corrected (crons share runtime Prisma instance but SAFE by construction). Pushed back on "add cron integration tests" (store unit test is the right layer). Worktree ready @ ea7a30cdd. EXECUTE = subagent dispatch per task (sonnet T1/T3/T5, opus T2/T5... T2+T4 risky=opus), RED proof hard gate, sequential (shared files). The resurrection P2002 branch (T4) is the riskiest unit.

## Log

- 2026-06-22: ORIENT + FRAME (focused design re-validation) complete on ea7a30cdd. Premise re-confirmed; tombstone-collision catch added (resurrect-on-invalidated-collision). -> PLAN.
- 2026-06-22: PLAN written (.claude/ai-infra-uplift-S8a-plan.md, 5 TDD tasks). 2B fan-out plan-grade (3 opus: CRITIC/COMPLETENESS/CODE-GROUNDED) = REVISE; fixes applied (rev1). Worktree created + init+install+reset+build GREEN. -> EXECUTE.
- 2026-06-22: EXECUTE complete (5 TDD subagents, RED proof each; T2 prisma + T4 resurrection on opus, T1/T3/T5 on sonnet). VERIFY all real gates GREEN (typecheck/lint/format/arch/build/verify-fast/security + schemas/db/core/api unit). Independent opus fresh-context review = no Critical, 2 Important triaged (no code bug; doc-fix + deliberate flagged decision + S8b-scope). Rebased onto origin/main d5dcbaa5e (A6 #1246 merged; clean, zero overlap, re-verified GREEN). Co-Authored-By trailers added (final SHAs a9d78abbf..ce42f0e01). PUSHED + SURFACED -> PR #1247. STOP for human merge (prisma stop-glob; never auto). NEXT: re-fire to build S8b off merged main.
