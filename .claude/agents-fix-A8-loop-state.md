# A8 — Alex booking-tool correctness loop — externalized state (orchestration scratch, not committed)

Durable record lives in memory note project_north_star_activation_gap + moc_alex_cockpit.

Goal: Fix the wrong-target reschedule/cancel defect (service-mismatch falls back to soonest of ALL bookings).
Authority: autonomous-with-guardrails BUT booking/cancel = merge-stop -> SURFACE-before-merge.
Task-size: standard (one bounded PR).
Base: origin/main @ d5dcbaa5e (rebased onto it mid-slice; was ea7a30cdd) baseline_sha: ea7a30cdd -> d5dcbaa5e
merge_safety: stop touched = appointment-cancel behavior change (JUDGMENT stop) -> SURFACED PR #1248, human merge call. independent_review = PASS (0 findings >= warn; trim nit fixed + tested).

## OUTCOME: MERGED. PR #1248 squash-merged to main as 665cc1cd7 (2026-06-22, user-authorized merge). CI 15/15 green. Remote branch auto-deleted; worktree removed + pruned; local branch deleted. 3 source commits collapsed: guard + trim + whitespace edge test.

## REVIEWS: (1) VERIFY independent fresh-context = 0 >= warn (trim nit fixed). (2) requesting-code-review senior (opus, user-requested arch+correctness) = READY TO MERGE YES, 0 Critical/0 Important; swept repo -> NO other wrong-target booking-selection site exists (reminders iterate all; failure-handler/deposit resolve by explicit bookingId); confirmed result reaches LLM via reinjection-filter PRESERVED_FIELDS; declined cosmetic availableServices case-dedupe (implausible w/ playbook services, both reviewers no-change).

## Ground-truth brief (ORIENT, each claim tool/file-backed)

- DEFECT (rank 17): `calendar-reschedule.ts` resolveTarget returned `(narrowed.length>0?narrowed:bookings)[0]` -> service-supplied-but-no-match fell back to soonest of ALL bookings. Shared by booking.reschedule + booking.cancel. NO_UPCOMING_BOOKING only fired on ZERO bookings.
- `fail(code,...)` code is a FREE STRING (tool-result.ts:45); NO code consumes booking error codes (grep empty) -> new NO_MATCHING_BOOKING breaks no seam.
- Guard is pre-mutation -> cannot weaken invariant; governance/idempotent/WorkTrace/PlatformIngress untouched.

## Scope decision (LOCKED)

- THIS PR = rank 17 wrong-target guard ONLY. Behavior on mismatch = new NO_MATCHING_BOOKING fail (held services in data.availableServices; mirrors NO_UPCOMING_BOOKING; NOT human-escalation). + trim/lowercase match (reviewer nit, mirrors booking-value.ts normalize).
- rank 20 (CAPI occurredAt=future slotStart, calendar-book.ts:460; seam meta-capi-dispatcher.ts:116; slotStart already in metadata) = SEPARATE PR (different file/concern; one-liner, ready).
- rank 18 (stalled pending_confirmation blocks slot; failure-handler throw leaves pending + no metric + no reaper; overlap predicate prisma-booking-store.ts:59 notIn[failed,cancelled]) = SEPARATE slice A8b (needs reaper/cron or db overlap-TTL).

| step               | done-condition (test/cmd)                               | RED proof | status | evidence                                                           |
| ------------------ | ------------------------------------------------------- | --------- | ------ | ------------------------------------------------------------------ |
| 1 RED wrong-target | two-service-mismatch cancel asserts NO_MATCHING_BOOKING | seen      | done   | "expected 'success' to be 'error'" calendar-reschedule.test.ts:227 |
| 2 GREEN guard      | discriminated resolveTarget + 2 call sites              | n/a       | done   | core test 13 pass; commit bece996c7                                |
| 3 broaden          | reschedule mismatch + positive regressions              | n/a       | done   | core test 17 pass                                                  |
| 4 RED trim         | " Botox " matches "botox"                               | seen      | done   | "expected 'error' to be 'success'"                                 |
| 5 GREEN trim       | trim+lowercase both sides                               | n/a       | done   | core test 18 pass; commit 8f38dca1c                                |

gate_results: typecheck=PASS(22/22) test=PASS(core 441,2skip; full suite green w/ 1 known chat-attrib flake reran) lint=PASS(0err) format=PASS arch=PASS(0err-level) verify-fast=PASS build=PASS eval=N/A(mock-tools + key-gated; blind to real tool; CI runs it green anyway) review=PASS(0>=warn)
carry_forward (<=150 words): A8 rank-17 SURFACED as PR #1248 (rebased onto d5dcbaa5e). Human merge call (appointment-cancel behavior change). REMAINING A8 work for future fresh sessions: A8b = rank 18 stalled-pending reaper (bigger, cron/TTL); rank 20 = CAPI occurredAt one-liner (calendar-book.ts:460 -> commit-time now, keep slotStart in metadata; verify meta-capi-dispatcher future-time rejection). LESSON: alex-conversation eval uses mock-tools.ts (its own canned booking.reschedule/cancel) + self-skips w/o ANTHROPIC_API_KEY -> it is BLIND to real booking-tool changes; "eval if booking tools touched" buys regression-cover ONLY for conversation, not tool logic. Worktree .claude/worktrees/agents-fix-a8 -> remove after #1248 merges.

## A8b-1 OUTCOME (rank-20 CAPI occurredAt): MERGED PR #1317 -> main 4ea4c3624 (2026-06-26, user-authorized)

- FIX: `calendar-book.ts:344` (NOT :460 — file refactored since the rank-17 note) `occurredAt: new Date(input.slotStart).toISOString()` -> `new Date().toISOString()` (commit-time). One prod line + clarifying comment.
- ROOT CAUSE: commit 4e5cf250a (Jun 6) bundled TWO changes: good deterministic eventId (`evt_booked_<id>`) + BAD flip of occurredAt from `new Date()` to slotStart under a "clock-game defense" rationale. My fix reverts ONLY the occurredAt half; keeps the eventId. The conflation: dedup determinism (solved by eventId; Meta dedup keyed on event_id NOT event_time) was misapplied to occurredAt, which must be the real conversion time. A booking slot is ALWAYS future -> Meta rejects future event_time -> every booked conversion was being dropped at Meta (total booked-stage attribution loss).
- TESTS: rewrote the pre-existing "clock-game defense" test (calendar-book.test.ts ~1256) which had codified the bug (`expect(occurredAt).toBe(slotStart)`) -> asserts commit-time window + `!== slotStart` + metadata.slotStart preserved. RED proven by reverting the 1-liner (git stash). Removed a redundant duplicate test I first added. Core suite 4586 pass / 0 fail; typecheck/lint/prettier clean (1 pre-existing complexity warn on execute, unrelated).
- REVIEW: 2 independent adversarial read-only reviewers (CAPI-correctness + blast-radius) = BOTH "Ready to merge: Yes", 0 Critical/0 Important. Bonus: same future-slot value also fed agent-home wins feed + Google Offline dispatcher (also future-time rejecting) -> both incidentally fixed. Reporting (prisma-conversion-record-store) now buckets booked conversions by commit-time not appointment-time = correct conversion semantics (old path was non-functional anyway). metadata.slotStart read by nobody but appointment time recoverable via booking.startsAt. Findings summarized in PR comment for the human merger.
- gate_results: typecheck=PASS lint=PASS arch=PASS integration-real-PG=PASS evals=PASS(incl Alex-conversation, blind to tool logic) security/secrets/codeql/docker=PASS test=PASS. ALL 16 CI checks green (#1317). review=PASS(0>=Important x2). Ship posture = SURFACE (human merge; revenue-attribution behavior change).
- POST-MERGE REVIEW (3rd, architecture+correctness, user-requested 2026-06-26): general-purpose reviewer RAN the tests (calendar-book 65 / dispatcher 29 / outbox-publisher 6 green) + traced producer->publisher->dispatcher->reporting. Verdict: doctrine-ALIGNED (producer is the correct authoring layer for occurredAt per conversion-record-store.ts:412 "producer-set event time"; matches sibling producer booking-failure-handler.ts:124 `new Date().toISOString()`; idempotency holds via unique deterministic eventId not event_time), WORKS as intended, 0 Critical / 0 defects. Bonus: now aligns in-app reporting window with Meta event_time -> ReconciliationRunner compares like-for-like for the first time. Minor (no action): booking-wins.ts:59 sub-second effective-time flip (cosmetic); no single e2e payload->event_time assertion (per-hop convention, each hop unit-tested).
- FOLLOW-UP SLICE (tracked, NOT done — defense-in-depth, separate package/concern; detail in [[feedback_booked_capi_occurredat_is_commit_time]]): add a symmetric FUTURE-event guard + warn + test at meta-capi-dispatcher.ts:45 (currently only rejects too-old). ~15 LOC; mirrors the event_time_too_old branch + test :218-225. This is the boundary that silently hid the bug. Candidate next-to-A8b-2 slice.
- REMAINING A8b: **A8b-2 = rank-18 stalled pending_confirmation reaper** (the LAST A8b sub-slice; STILL OPEN). Needs superpowers:brainstorming (genuine design choice: reaper cron vs DB overlap-TTL vs failure-handler hardening). Overlap predicate prisma-booking-store.ts:59 (status notIn [failed,cancelled]) treats pending as occupying; booking failure-handler can throw leaving a row stuck pending_confirmation w/ no terminal status + no metric + no reaper. Mirror lifecycle stalled-sweep.ts / stranded-claim-reaper.ts / dlq-retention-purge / Robin reaper. AUTO-MUTATES booking rows -> SURFACE for human merge. After A8b-2: backlog A23+/A24+ (re-check gh pr list + git worktree list FIRST).
- Worktree .claude/worktrees/agents-fix-a8b-capi-occurredat REMOVED + pruned; branch fix/a8b-capi-occurredat deleted local+remote (squash-merge, --delete-branch); local main fast-forwarded to origin/main 4ea4c3624.

## A8b-2 OUTCOME (rank-18 stalled-pending reaper): MERGED PR #1332 -> main 9a4664117 (2026-06-26, user-authorized via /goal)

- DESIGN (brainstormed): chose REAPER cron over DB overlap-TTL / failure-handler-hardening. Reaps stranded `pending_confirmation` (createdAt older than 30min TTL) to the existing terminal `failed` -> slot releases (failed already excluded from every overlap/active predicate) with NO new status, NO migration, NO cross-slice seam. Mirrors EV-2 stranded-claim-reaper end-to-end (core orchestrator over a narrow store iface + thin api Inngest cron + counter; a DIRECT row-mutation janitorial cron, NOT a governed ingress intent — same class as stranded-claim/lifecycle-stalled-sweep, so the "new auto-exec cron-intent" gov+PLATFORM_DIRECT wiring did NOT apply). Spec+plan: docs/superpowers/{specs,plans}/2026-06-26-stalled-booking-reaper\*.
- BUILD: db `PrismaBookingStore.findStalledPending` + `reapStalledPending` (status-guarded CAS, count===0=raced); core `reapStalledBookings` (TTL=30min, limit=500, per-row counter+log, ONE alert/run critical-iff-failed, CAPPED note) + `stalled_booking_reaped` errorType + barrel; api `stalled-booking-reaper` Inngest cron (hourly, retries:2, null-store no-op) wired in inngest.ts; `bookingStalledReaped` counter in ALL 3 registries. TDD RED->GREEN at every layer.
- REVIEW-DRIVEN HARDENING (both adversarial reviewers flagged; 1 ranked Important): freeing the slot introduced a NEW double-book — a confirm stalled past TTL could RESURRECT the reaped row (calendar-book confirm tx did an unguarded `update` by id) -> re-occupy slot another lead now holds + phantom booked conversion. FIXED: confirm write -> status-guarded `updateMany where {id,organizationId,status:"pending_confirmation"}`; count===0 -> throw -> existing orphan-compensation + failure path (failure-handler early-returns on already-`failed`, no double-escalation). Rippled the tx type (calendar-book-types.ts), api skill-mode.ts adapter, 6 calendar-book test tx mocks, + revenue-loop-substrate.ts `booking.updateMany`. Happy path byte-for-byte equal (65 existing calendar-book tests green + new RED->GREEN guard test). LESSON -> [[feedback_reaper_freeing_slot_needs_guarded_claimant]].
- GATES: tsc db/core/api/chat PASS; suites db 1243 / core 4595 / api 2632 (incl 4 revenue-proof e2e updated for the CAS); lint+prettier clean; NO em-dashes in new code. 2 adversarial reviews + 1 focused re-review of the confirm guard = ALL "Ready to merge: Yes", 0 Critical/0 Important after the guard. CI: **17/17 green** (#1332; incl Integration-Real-Postgres + Alex-conversation eval + test 12m).
- SHIP: SURFACED then user-authorized MERGE (squash 9a4664117, --delete-branch) after 17/17 CI green + main was MERGEABLE/CLEAN (origin/main had drifted ~10 concurrent merges; `git merge-tree`=0 conflicts + 0 file-overlap with my 10 touched files -> no semantic-conflict risk). Worktree .claude/worktrees/agents-fix-a8b-2-stalled-booking-reaper REMOVED + pruned; branch fix/a8b-2-stalled-booking-reaper deleted local+remote; local main ff'd to origin/main 9a4664117. **A8 FULLY CLOSED** (rank 17 #1248 + rank 20 #1317 + rank 18 #1332). NEXT = backlog A23+/A24+ (re-check gh pr list + git worktree list FIRST; concurrent sessions active on P2-3 #1331, EV-3 #1334, riley gate-2 #1333, alex-per-lead-jurisdiction #1330).

## Log

- 2026-06-22: ORIENT (collision clean) -> FRAME (scope=rank17 only; NO_MATCHING_BOOKING) -> PLAN (.claude scratch) -> EXECUTE (TDD RED->GREEN x2) -> VERIFY (all gates green; independent review 0>=warn, trim nit fixed) -> rebased onto d5dcbaa5e (A6 #1246 merged mid-slice) -> SURFACED PR #1248. DONE (human merge).
- 2026-06-26: A8b-1 (rank-20 CAPI occurredAt) ORIENT (collision clean; verified bug still live at calendar-book.ts:344) -> TDD (RED via existing clock-game test contradiction + git-stash proof -> GREEN) -> VERIFY (all gates) -> 2x adversarial review PASS -> SURFACED PR #1317 -> 16/16 CI green -> user-authorized MERGE (squash 4ea4c3624) -> worktree torn down + local main ff'd. A8b-2 (rank-18 reaper) remains = next slice.
- 2026-06-26: A8b-2 (rank-18 reaper) ORIENT (collision clean; verified defect live vs prisma-booking-store.ts:59 + booking-failure-handler + no reaper) -> BRAINSTORM (reaper-to-failed chosen) -> SPEC+PLAN -> TDD RED->GREEN x4 layers -> VERIFY (all gates green) -> 2x adversarial review (1 Important: confirm-resurrection double-book) -> ACTED: status-guarded confirm CAS + re-review CLEAN -> SURFACED PR #1332 (human merge pending CI). A8 fully closed once #1332 merges.
