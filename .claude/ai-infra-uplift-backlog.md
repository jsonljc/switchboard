# AI-infra uplift backlog (orchestration scratch, uncommitted)

Workstream: **ai-infra-uplift** (cross-cutting; not one agent).
Source of truth for scope/detail: `docs/research/2026-06-19-ai-engineering-landscape-for-switchboard.md`.
Memory hub: `reference_ai_landscape_research_2026_06.md` + the dev-gotchas map. Load the named `feedback_*.md` per slice.

Every item below is derived from a verified research finding. **Do not add items not traceable to that doc.** In-scope decomposition of an item (build-loop multi-slice rule) is allowed; scope expansion is not. Disposition labels are expectations only: the build-loop decides merge-vs-surface MECHANICALLY via its merge-stop globs at run time.

Status legend: `[ ]` todo, `[~]` in progress, `[x]` merged, `[S]` surfaced (PR open, awaiting your merge), `[B]` blocked, `[D]` decomposed (epic).

## Slices (priority order)

- [x] **S1 — Call-layer hardening** `[AUTO]` (findings f2, f3). Deps: none. **MERGED 2026-06-20** via PR #1179 (squash `fbee15839`): generation-aware sampling predicate + per-call prompt-cache effectiveness telemetry (core/api/chat) + deterministic tool ordering, across the 3 non-gov call sites + the pull-quote generator (4th site). TDD throughout; independent opus review = SHIP (0 >=warn); all CI gates green. Governance classifier deliberately excluded (= S2). Loop-state: `.claude/ai-infra-uplift-S1-loop-state.md`.
  - Emit prompt-cache effectiveness telemetry (`cache_read_input_tokens`, `cache_creation_input_tokens` per call) into `telemetry/metrics.ts`; log/alert on zero-read across identical prefixes.
  - Assert deterministic tool-manifest ordering (sort by name) before building the Anthropic tool array, so a reorder can never silently bust the cached prefix. Add a test.
  - Make the adapter model-generation-aware: omit `temperature`/`top_p`/`top_k` for 4.7+ model ids (no-op on today's 4.6 routes; removes the latent hard-400 gating any future bump).
  - Files: `packages/core/src/skill-runtime/adapters/anthropic-tool-adapter.ts`, `packages/core/src/agent-runtime/anthropic-adapter.ts`, `packages/core/src/model-router.ts`, telemetry. EXCLUDE the governance classifier adapter (that is S2; it trips the governance stop-glob).
  - Eval (non-regression): `pnpm eval:classifier`, `pnpm eval:alex-conversation`.

- [x] **S2 — Governance classifier adapter parity** `[SURFACE: governance glob]` (f2, f3). Deps: S1 (reuse the pattern). **SURFACED 2026-06-20 — PR #1182** (branch `feat/ai-gov-classifier-parity`, commit 872e9e333). Worktree `.claude/worktrees/ai-gov-classifier-parity`. Touches `governance/**` stop-glob -> human merge.
  - Apply the same cache-metrics + sampling-param forward-compat to `packages/core/src/governance/classifier/anthropic-classifier.ts`. Keep `strict:true` + forced `tool_choice` intact.
  - DONE: added `recordLlmCacheEffectiveness` after `messages.create` (the genuine gap). ORIENT finding: the classifier sends NO temperature/top_p/top_k (already 4.7+ forward-compatible) and has a SINGLE tool (ordering trivially deterministic), so the sampling-guard + tool-ordering halves of S1 would be dead code; pinned the no-sampling-params invariant with a regression-guard test instead. TDD RED->GREEN. Local gates GREEN (typecheck/core test 4311/lint 0-err/format/arch/verify-fast/build). Independent opus review = SHIP (0 >=warn). Eval = non-regression by construction (side-effect only; request + result byte-identical).

- [B] **S3 — Batch API for offline jobs** `[AUTO, verify glob at ORIENT]` (f18). Deps: none. **BLOCKED 2026-06-20 — premise invalidated at ORIENT (bounded REVISE signal). Full evidence: `.claude/ai-infra-uplift-S3-orient-revise.md`.** Neither named target fits the Batches API in the current code: (a) the "proof-quality report" (`compute-receipted-booking-quality.ts`) is pure DB aggregation (no LLM); the only reports LLM call (`pull-quote-generator.ts`) is a single on-demand dashboard call (weekly cron passes `llm:null`) — nothing to batch, and batching a latency-sensitive user call is wrong; (b) the classifier eval (92 sequential calls) IS the CI gate (`ci.yml`) — the async Batches API (minutes-to-24h SLA) is wrong for a latency-sensitive required gate, savings ~$4/wk, and it lives under `governance/` (stop-glob, so `[AUTO]` is wrong). No clean in-scope sub-slice is a net win. **Needs user decision: drop S3, or re-scope to a genuine future offline bulk LLM job (none exists today).**
  - Route the classifier eval and the proof-quality report generation through the Message Batches API (flat 50% off). Key results by `custom_id` (never array position); branch on `result.type`.
  - At ORIENT, confirm the report surface path does not sit under a money/receipt stop-glob; if it does, this slice surfaces.

- [B] **S4 — Adaptive thinking + effort dial** `[SURFACE: model behavior + eval]` (f3). Deps: S1. **BLOCKED 2026-06-20 — multi-slice + blocked on the 4.7+ model bump. Full state + decomposition: `.claude/ai-infra-uplift-S4-loop-state.md`.** ORIENT + design + an EXECUTE attempt were done (branch `feat/ai-adaptive-thinking` @ 6a7a2a933, local-only, worktree torn down); independent opus review = CRITICAL: the impl wired the agent-runtime adapter, which is a DEAD PATH — the critical tier actually flows through the skill-runtime tool loop (`resolveProfile` -> `chatWithTools`), which drops the new fields, so the flag was inert-forever. Correct tool-loop wiring with `display:"summarized"` needs the thinking-block lifecycle (extend `LLMContentBlock`, round-trip signatures across tool turns, response handling) = a core-contract change, AND adaptive thinking/effort are 4.7+ features while the critical slot is opus-4-6, so S4's own eval-validation acceptance criterion is unmeetable now (research scoped it "after the model bump"). Decomposed into 4a (infra; inert alone) + 4b (skill tool-loop integration; needs a display-mode decision + a live 4.7+ model). **Needs the model bump + a display:summarized-vs-omitted decision.**
  - Add `thinking:{type:"adaptive"}` + `output_config:{effort}` support to the call layer; wire on the critical tier behind a default-safe flag (per the flag-gated-control + producer-population lessons). `display:"summarized"` for operator-visible reasoning.
  - This is genuine design: run `superpowers:brainstorming` on the model-generation / cost / latency tradeoff. Validate against `eval:alex-conversation` + `eval:classifier`. Do NOT flip a default model without evidence.

- [x] **S5 — strict tool schemas at the skill-tool boundary** `[SURFACE: PlatformIngress glob]` (f4). Deps: none. **SURFACED 2026-06-20 — PR #1186** (branch `feat/ai-strict-tool-schemas`, commit fd2bbd154). Worktree `.claude/worktrees/ai-strict-tool-schemas`. State: `.claude/ai-infra-uplift-S5-loop-state.md`.
  - Adopt `strict:true` / `output_config json_schema` where safe at the PlatformIngress tool boundary; requires `additionalProperties:false` + all-required. Keep value-range / `Number.isFinite` checks in Zod (strict rejects min/max — see `feedback_anthropic_strict_tool_schema_no_minmax`). Coordinate with `input-schema-validator.ts`.
  - DONE: conservative `strictenToolSchema()` at the adapter boundary marks a tool strict:true + additionalProperties:false ONLY when its schema is a flat all-required scalar/enum object with no forbidden keyword (nested objects/arrays left loose); `output_config json_schema` is response-format (not tool-compatible) so not used. Range checks stay post-call (input-schema-validator/Zod). TDD; co-located sibling test file (600-line cap). Required gates GREEN; independent opus review = SHIP (enumerated every real schema, no false-positive). **CAVEAT: NOT live-API-validated (no valid key; CI key 401 -> evals red repo-wide incl. main); eligible set includes booking + deposit money tools -> the PR REQUIRES a real strict call / alex eval before merge (latent live-only failure class, #623 history).**

- [x] **S6 — Trajectory grading on WorkTrace** `[eval-gate; VERIFY-FIRST]` (f7). Deps: none. **FULLY MERGED 2026-06-20.** 6a = PR #1196 (squash `186856219`); 6b = PR #1202 (squash `c019796d1`). Both: all 4 required checks green incl. real-DB `test`; worktrees+branches removed. State: `.claude/ai-infra-uplift-S6a-loop-state.md` + `.claude/ai-infra-uplift-S6b-loop-state.md`.
  - **6a (query surface) — DONE/MERGED #1196.** Verify-first RE-confirmed NO on current main (WorkTrace has no tool-call steps; `ExecutionTrace.toolCalls` had no `workUnitId`/FK). Design fork RESOLVED to **option (i)** (FK column on ExecutionTrace) because `workUnitId` is already in scope at the executor->hook seam (`skill-mode.ts:95` passes `workUnitId: workUnit.id` for delegation lineage) -> cheap additive change, not the feared "wider change"; option (ii) embed-on-WorkTrace rejected (sealed/hashed canonical entity, builder lacks the sequence). Added nullable `ExecutionTrace.workUnitId` + index + hand-written migration; threaded workUnitId executor->hook->trace (afterSkill + onError); `findByWorkUnitId(orgId, workUnitId)` ordered chronological, tenant-scoped. TDD (executor->recorder seam pinned). Required gates GREEN (typecheck/core 4345/api 2258/lint/format/arch/verify-fast/build); independent opus review SHIP (0 >=warn). PRODUCER LIVE (not a dead column) -> real data on deploy; batch/sim runs NULL by design. Touches prisma stop-glob -> SURFACE (human merge). CAVEAT: migration hand-written (PG down) -> CI drift-validates as the first real-DB check.
  - **6b (the deterministic gate) — DONE/MERGED #1202 (`c019796d1`).** No-LLM/no-key/no-DB CI eval `evals/trajectory-grading/` (pure `gradeTrajectory`): Tool Correctness (`tool-sequence-mismatch`), Argument Correctness (`argument-invalid`, fail-closed), approval-bypass (`approval-bypassed`) oracled against the REAL gate (`getToolGovernanceDecision`+`mapDecisionToOutcome` from effectCategory+trustLevel facts — non-circular; catches "right action but bypassed approval" incl. executed-despite-gate; identity-matched so a bypass co-occurring with sequence drift still surfaces), `malformed-record` fail-closed for unknown outcomes, `"simulated"` correctly never a bypass. 12 golden fixtures + runner + `fixtures.test.ts` enforce exact verdict+kind set (no pass-by-construction). 3 drift guards. New `eval-trajectory-grading` CI job mirrors governance-decision. [AUTO] merged (no stop-glob); TDD; 2 independent opus reviews (1st found+fixed 1 important+2 warn, 2nd SHIP). Real-data adapter (findByWorkUnitId) = documented follow-up, deliberately not built (must not couple CI gate to DB/key). State: `.claude/ai-infra-uplift-S6b-loop-state.md`.

- [x] **S7 — Shadow-mode Riley money-path** `[SURFACE: money/Riley]` (f8). Deps: S6 helps (S6 blocked, but S7 does not hard-depend on it). **SURFACED 2026-06-20 — PR #1188** (branch `feat/ai-shadow-riley`, commit 65290e10b). Worktree `.claude/worktrees/ai-shadow-riley`. State: `.claude/ai-infra-uplift-S7-loop-state.md`.
  - Predicted-vs-actual shadow harness for Riley's flag-dark reallocation executor, with an LLM-judge diff, runnable before the real-money flip. Load `project_riley_capability_audit_2026_06_10.md`.
  - DONE: pure `buildShadowReallocationReport` (ad-optimizer) composing the existing planner (`buildRileyBudgetCandidate`) + blast-radius cap (`assertWithinBlastRadius`) + an INJECTED optional judge -> per-campaign report {predicted, deltaCentsSigned, blastRadius verdict, judge}. Provably cannot move money (no submit-sink/executor/MetaAdsClient import — reviewer traced the import closure). Deterministic without a key; judge injected so Layer-2 stays Anthropic-free. TDD; required gates GREEN; independent opus review = SHIP (0 >=warn). **Scope: this is the reusable harness CORE; the operational runner (audit shadow-log/CLI) + concrete LIVE judge are the documented follow-up (live judge needs the 401-blocked key). Report models the blast-radius cap only, not the executor's live re-read/drift (predict-time approximation, noted in JSDoc).**

- [~] **S8 — Govern memory writes through PlatformIngress** `[SURFACE: prisma/governance/ingress globs]` (f16). Deps: none. **S8a MERGED 2026-06-22 -> PR #1247 (squash `7d14402bc`)** (schema: 4 nullable provenance+valid-time cols + invalidate-not-delete for evict/decay + tombstone-resurrection + source tagging; TDD; 3 fresh-context reviews [the 2nd, user-requested, caught a CI-blocking `check-routes` store-mutation gate that local VERIFY missed -> fixed: org-scoped resurrection `updateMany` + decay-pass2 global directive + sibling-store read filters]; ALL CI green incl. the real-DB migration-drift validation; worktree+branch torn down; ledger `.claude/ai-infra-uplift-S8a-loop-state.md`). **S8b MERGED 2026-06-23 -> PR #1254 (squash `3a876f4c3`)** (registered the governed `memory.write` intent as operator_mutation + system_auto_approved + non-financial [resolved against the operator-mutation recipe, NOT skill-mode/executorBySlug: the gate short-circuits non-financial auto-approve so NO seeded policy, operator_mutation auto-resolves platform-direct so NO deployment_not_found, NO route-allowlist/eval-fixture/db change]; new MemoryWriteParametersSchema + buildMemoryWriteHandler + bootstrap wiring + app.ts producer; live-path test proves reachability through real bootstrap+gate+ingress with a seeded {id:"system"} submit [no policy -> executes, carve-out-off -> deployment_not_found load-bearing]; folded in the S8a follow-up tagging the operator memory-correction route source:"operator"; TDD, 2B plan-grade 3-opus PASS, independent review SHIP 0>=warn, all required CI green, autonomous merge). **S8c (route the compounding-service + decay-cron writes through PlatformIngress.submit as the seeded {id:"system"} actor, trigger internal/schedule) is the LAST remaining S8 slice**, gated on S8b's merge (now done). Was BLOCKED 2026-06-20 — multi-slice (research rates f16 Effort M-L); design + decomposition: `.claude/ai-infra-uplift-S8-design-decomposition.md`.** ORIENT verdict: NOT one bounded PR. Three drivers: (1) no memory-write governance intent exists (writes go DIRECT via `compounding-service.ts memoryStore.create/delete`, never PlatformIngress) -> needs a new IntentRegistration + governance seeding (GovernanceGate default-denies unmatched intents); (2) 4 net-new Prisma columns (source/validFrom/validTo/invalidatedAt) -> mandatory schema migration, unvalidatable locally (PG down) = migration-risk STOP; (3) caller (gateway-bridge callback) has no actor/principal to submit with. Touches the PlatformIngress core invariant. Brainstorming design model captured (provenance + bi-temporal valid-time + invalidate-not-delete + deterministic conflict resolution, reusing dedup-axis + idempotency-fingerprint; NOT a vector DB). Decomposed into S8a (schema + invalidate-not-delete) -> S8b (memory.write intent + governance) -> S8c (ingress integration). **Needs a human greenlight on the design + sequence before the schema/ingress changes land.\*\*
  - Route `compounding-service.ts` DeploymentMemory writes through PlatformIngress/GovernanceGate with `source` + `confidence` + `valid-time`; adopt invalidate-not-delete over the current decay/evict. Extend the existing dedup-axis + idempotency-fingerprint patterns. Do NOT build a vector DB (storage model is already correct).

- [x] **S9 — Long-loop context safety** `[SURFACE: governance glob likely]` (f9, f10). Deps: none. **SURFACED 2026-06-20 — PR #1189** (branch `feat/ai-long-loop-safety`, commit 755fffa9a). Worktree `.claude/worktrees/ai-long-loop-safety`. State: `.claude/ai-infra-uplift-S9-loop-state.md`.
  - For long multi-turn loops, re-state the hardest safety rules in a short reminder near the END of context (recency), and instrument context fill. Touches governance text assembly, so expect to surface.
  - DONE: `getSafetyRecencyReminder()` (condensed hardest rules) appended as a trailing text block to the tool-results turn each continuation (recency, end-of-context); `recordSkillContextFill` per-turn `skillContextFillRatio` histogram (billable/maxTotalTokens, NaN-safe). Broadened `LLMMessage.content` to allow the mixed tool_result+text turn (encoder already dispatches per type; no consumer changed). Additive/behavior-neutral. TDD; required gates GREEN (typecheck clean core/api/chat); independent opus review = SHIP (verified protocol correctness + behavior-neutrality). Efficacy eval-gated (401 key) -> non-regression by construction.

## Deferred epics (multi-slice — STOP & decompose when reached; mark `[D]`, write decomposition to `.claude/` scratch, await go)

**ALL DECOMPOSED 2026-06-20 — full slice breakdowns + blockers in `.claude/ai-infra-uplift-epics-E1-E5-decomposition.md`. None is a single bounded PR; each awaits a human go-ahead (L-effort / beta-API / cross-layer / dependency-gated).**

- [D] **E1 — Durable mid-loop approval parking** (f6). L-effort; core (Layer 3) cannot import Inngest (Layer 2), so this is a new architectural integration. See `feedback_skill_runtime_two_constraint_regimes`, `feedback_inngest_step_state_json_only`. Decomposed E1a-d (design + serializable executor state + apps/api Inngest park/resume wrapper). Needs design sign-off.
- [D] **E2 — Context editing (`clear_tool_uses`)** (f1). Beta; cache-invalidation tradeoff; today `maxTotalTokens` hard-throws. Decomposed E2a-c (clear policy driven by the new `skillContextFillRatio` signal; flag-gated tool-adapter wiring; clear-then-continue replacing the hard-throw). Blocked on the beta API + live eval (401 key).
- [D] **E3 — Tool search (`defer_loading`)** (f11). Cannot combine with `cache_control` on the same tool. Decomposed E3a-c (trigger analysis; deferred/non-deferred split reconciled with S1's cache breakpoint; cache-regression check). Likely premature until the tool surface is large.
- [~] **E4 — OTel GenAI spans derived one-directionally from WorkTrace** (f21). E4a satisfied by S6a (findByWorkUnitId). **E4b DONE/MERGED 2026-06-21 — PR #1213 (squash `a49f97cbe`):** pure core `projectWorkUnitSpans` (work-unit->execution->tool parented GenAI-span tree, read-only, one-directional, no db import) + a minimal `Tracer.startSpan(...,parent?)` extension honored in the OTel adapter (WeakMap+injected contextBridge) + a thin flag-gated apps/api exporter (reuses `OTEL_EXPORTER_OTLP_ENDPOINT`, NO new env var; reads nested `wt.trace.*` with tenant guard). TDD; full gates GREEN; independent opus review SHIP. State: `.claude/ai-infra-uplift-E4b-loop-state.md`. **E4c SLICE 1 DONE/MERGED 2026-06-21 — PR #1220 (squash `3e6031623`):** the timeline/quality fix — `Tracer.startSpan(...,options?{startTime,kind})` + `Span.end(endTime?)` honored in the OTel adapter (epoch-ms->HrTime); `projectWorkUnitSpans` synthesizes HONEST timing (work-unit = REAL requestedAt/completedAt; execution start = createdAt-durationMs DERIVED + tools sequential-packed, both marked `switchboard.timing.synthetic=true`; missing anchors degrade, never fabricate) + SpanKind (INTERNAL/CLIENT/INTERNAL as plain numbers, core stays OTel-free) + cache/cost attrs (`gen_ai.usage.cache_read_input_tokens`/`cache_creation_input_tokens`/`switchboard.cost_usd`) + provider + setIfString root-id guard + barrel re-export of OTelContextBridge. Telemetry-only, no stop-glob -> [AUTO]. TDD (4 tasks); all required CI GREEN; independent opus review SHIP. State: `.claude/ai-infra-uplift-E4c-loop-state.md`. **E4c SLICE 2 SURFACED (remains)** = the live trigger (call `exportWorkUnitSpans` after a work unit completes) + a real OTLP export + any dedicated enable-flag — touches the ingress submit path and/or env-allowlist (SURFACE). Design fork (route-side-effect vs central PlatformIngressConfig callback; reuse-flag vs dedicated-flag) + safety invariants in `.claude/ai-infra-uplift-E4c-slice2-plan.md`; needs a human decision before build. **E4c SLICE 1.1 hardening (post-merge review follow-ups) = PR #1225** (`feat/ai-e4c-hardening`, telemetry-only [AUTO]): inversion-safe + symmetric timing (`deriveSpanTiming` degrades inconsistent/skewed/clamp-inverted spans to flat), intrinsic throw-safety (Array.isArray + element-narrowing), geometry clamps, toEpochMs numeric, epochMsToHrTime defensive; independent review caught + fixed a real inversion bug the first clamps introduced; all gates GREEN + review SHIP. **Deferred (folded into the Slice 2 plan):** a real-SDK `InMemorySpanExporter` test pinning the adapter->SDK leg (needs an OTel dev-dep; do with the live OTLP export) + the `gen_ai.system`(switchboard)/`gen_ai.provider.name`(anthropic) semconv coherence (same concept, two keys; fix before dashboards depend on it) + (low) emitToolSpan complexity 17. **OBSERVABILITY-COMPLETION (post-#1236 review, 2026-06-22):** root-span-for-non-executing-legs MERGED #1239 (`c201ebda9`, [AUTO]); throw-path-fire MERGED #1241 (`115c15d5f`, user-authorized after 2 independent reviews); S9-histogram-ordering verified MOOT (no code). See the loop log + `.claude/ai-obs-rootspan-loop-state.md`.
- [D] **E5 — Native citations for provenance** (f5). Incompatible with structured outputs per call. Decomposed E5a-c (provenance-surface selection avoiding the structured/tool path; citations on report narrative; dashboard surfacing). Mutually exclusive with S5's strict outputs per call.

## Loop log

- 2026-06-19: backlog created from the verified research. Nothing started.
- 2026-06-19: S1 started (build-loop). C3 sampling-param predicate + C1 cache-effectiveness telemetry shipped GREEN (TDD); tool-adapter wiring tests RED-confirmed, impl pending. PAUSED for machine restart. Full resume state in `.claude/ai-infra-uplift-S1-loop-state.md`.
- 2026-06-20: `/loop` resumed (S1 confirmed merged via #1179 on origin/main). Running backlog in priority order from S2.
  - S2 SURFACED -> PR #1182 (governance stop-glob). Cache-effectiveness telemetry on the classifier; sampling-guard + tool-ordering were N/A (no sampling params, single tool). TDD, opus review SHIP.
  - S3 BLOCKED (premise invalidated at ORIENT): neither named target is a real batch job. Evidence in `.claude/ai-infra-uplift-S3-orient-revise.md`.
  - S4 BLOCKED + decomposed: independent review caught that the impl wired a dead path; the real critical-tier path is the skill tool loop, and correct `display:"summarized"` needs thinking-block lifecycle work; adaptive thinking is 4.7+ only so it is unvalidatable on the current 4.6 critical model. Blocked on the model bump. Branch `feat/ai-adaptive-thinking` @ 6a7a2a933 kept locally. State in `.claude/ai-infra-uplift-S4-loop-state.md`.
  - Next independent item: S5 (strict tool schemas at PlatformIngress boundary). Then S6, S7, S8, S9, then decompose epics E1-E5.
- 2026-06-20 (turn 3): S6 BLOCKED + decomposed (verify-first FAILED: WorkTrace has no ordered tool-call steps; the sequence lives in an unlinked `ExecutionTrace.toolCalls`; no golden trajectories; live capture blocked by the 401 key — did not force a canonical-persistence migration; `.claude/ai-infra-uplift-S6-decomposition.md`). S7 SURFACED -> PR #1188 (pure shadow-reallocation harness in ad-optimizer; provably cannot move money per the reviewer's import-closure trace; deterministic + injected judge; opus review SHIP). Next: S8 (govern memory writes through PlatformIngress — greenfield design, run brainstorming), then S9, then decompose E1-E5.
- 2026-06-20 (turn 4): S8 BLOCKED + decomposed (multi-slice; design model + A/B/C decomposition in `.claude/ai-infra-uplift-S8-design-decomposition.md`): no memory-write governance intent exists, 4 net-new Prisma columns needed (migration, PG-down unvalidatable), and the caller has no actor — touches the PlatformIngress invariant; needs a human greenlight on the design + sequence. S9 ORIENTed + design LOCKED (`.claude/ai-infra-uplift-S9-loop-state.md`) = a single bounded SURFACE slice (recency safety reminder + context-fill instrument); checkpointed to BUILD next turn. After S9: decompose epics E1-E5.
- 2026-06-20 (turn 5, LOOP COMPLETE): S9 SURFACED -> PR #1189 (recency safety reminder + context-fill metric; opus review SHIP). Epics E1-E5 all DECOMPOSED -> [D] (`.claude/ai-infra-uplift-epics-E1-E5-decomposition.md`). Every backlog item is now terminal. CI-KEY NOTE (CORRECTED post-merge — I earlier wrote "resolved", which was WRONG): the 401 ANTHROPIC_API_KEY is STILL INVALID (verified on main HEAD: the Claim Classifier eval 401s on the first fixture). The other evals only "pass" because their BLOCKING parts are deterministic (Governance Decision model-free; Alex/Riley deterministic oracle gates + continue-on-error live-scoring); the Claim Classifier eval is the only true live-API exerciser and hard-fails. So #1182's AND main's Claim Classifier failures are the same code-independent 401 (NOT an S2 regression; #1182 is behavior-neutral). The 4 REQUIRED checks (typecheck/lint/test/security) are GREEN on all 4 PRs + on main. KEY ROTATION is a user action; until then S5 strict-tools + all efficacy evals stay live-unvalidated (low residual risk; static analysis strong, classifier is a live proof of the same strict shape). LOOP STOPPED per the terminal-state rule.
- 2026-06-20 (POST-LOOP, user-authorized merge): cross-PR integration review = SHIP-ALL (combined build/typecheck/test of all 4 merged together = GREEN, zero conflicts; architecture/seam review zero >=warn). All 4 surfaced PRs SQUASH-MERGED to main: S2 #1182, S5 #1186, S7 #1188, S9 #1189 (main now @ d6b48abdf, all 4 features verified present). The 4 worktrees + branches removed; `feat/ai-adaptive-thinking` (S4 4a infra) kept local-only. Memory `project_ai_infra_uplift_state` + MEMORY.md index updated to COMPLETE. STILL OPEN FOR YOUR DECISION: S3 (drop/rescope), S4 (model bump + display decision), S6 (6a query-surface design fork), S8 (memory-gov design greenlight A->B->C), epics E1-E5 (go-ahead).
- 2026-06-20 (CI note, cross-cutting): the `Eval — Claim Classifier` check is RED on #1182 AND on `main`'s HEAD — root cause is a `401 invalid x-api-key` (the CI `ANTHROPIC_API_KEY` secret is invalid/expired), i.e. a global, code-independent infra failure, not an S2 regression (the 401 is upstream of the telemetry I added). The 4 REQUIRED checks (typecheck/lint/test/security) are GREEN on #1182, and the evals are NOT required, so #1182 is mergeable. Treat eval reds on any engine-touching surfaced PR as this same infra issue until the key is rotated; do not chase them as regressions.
- 2026-06-20 (turn 2): `/loop` resumed at S5. S5 SURFACED -> PR #1186 (strict tool schemas where safe; conservative detector; independent opus review SHIP after enumerating every real schema). Carries an explicit "must live-validate before merge" caveat (the 401 key blocks the live check; eligible set includes money/booking tools). Next: S6 (trajectory grading on WorkTrace, VERIFY-FIRST). Surfaced-PR worktrees (ai-gov-classifier-parity #1182, ai-strict-tool-schemas #1186) kept on disk until merge.
- 2026-06-20 (S6a slice): drove S6 via build-loop. Verify-first RE-confirmed on current main (origin/main had advanced to 559628a5b via concurrent sessions: #1193, #1194, and the 8c54d56d2 proof-paid commit). Resolved the design fork to option (i) after verifying `workUnitId` is in scope at `skill-executor.ts:295` (cheap, not a wider change). Built 6a (nullable ExecutionTrace.workUnitId + index + hand-written migration + executor->hook->trace threading + findByWorkUnitId), TDD with the executor->recorder seam pinned, producer-population verified LIVE at skill-mode.ts:95. Gates GREEN, independent review SHIP. S6a SURFACED -> PR #1196 (prisma stop-glob; worktree ai-s6a-trace-query kept on disk until merge). S6b (the deterministic gate) is the clear next slice. STOPPED per SURFACE authority (no auto-merge on prisma glob).
- 2026-06-20 (S6a deep review + MERGE, user-authorized): user requested a thorough architecture+correctness review, then authorized the merge. Two fresh-context opus reviewers (architecture-alignment + correctness/works-as-intended) = BOTH ship, zero Critical/Important; delegation attribution verified correct; migration validated drift-faithful via no-DB `prisma migrate diff`. Applied one hardening (pinned findByWorkUnitId returns governanceDecisions for 6b). All 4 required checks GREEN on CI (typecheck/lint/security + real-DB `test` 11m). SQUASH-MERGED #1196 -> main `186856219` (MERGEABLE CLEAN, required_reviews=0, no conflict vs the re-moved main). Teardown: local main ff'd, worktree removed, local+remote branch deleted, no dangling stash. S6 now = 6a DONE, **6b is the next slice**.
- 2026-06-20 (S6b slice, build-loop, [AUTO]): drove S6b via build-loop. ORIENT verified ground truth on current main (0121d39a0): ToolCallRecord.governanceDecision is `GovernanceOutcome`; the executor ALSO emits a 4th value `"simulated"` (skill-executor.ts:563, force-cast); `getToolGovernanceDecision`+`mapDecisionToOutcome` exported; findByWorkUnitId returns `toolCalls: unknown[]`. Design (brainstorming): grader = eval-local PURE fn oracling the recorded outcome against the REAL gate (non-circular), permissive recorded side + fail-closed, "simulated" never a bypass, 3 drift guards. TDD (RED proofs captured). 1st opus review CHANGES (1 important: bypass masked by sequence drift; 2 warn: ci-filter coverage, simulated trust-boundary; 1 nit) -> triaged via receiving-code-review (pushed back on the false-positive-prone "flag simulated+success as malformed", documented instead), ALL fixed (identity-match arg/approval; +types.ts/skill-executor.ts to filter; non-circular outcome-vocab test). 2nd FRESH opus review = SHIP (0 >=warn, empirically verified drift guard + fail-closed/empty/reorder/unexpected-call paths). origin/main advanced 0121d39a0->06b929c18 (#1200, zero overlap) -> rebased clean. All 4 required CI checks GREEN (typecheck/lint/security + real-DB test); Eval — Trajectory Grading job GREEN; Eval — Claim Classifier fail = known 401 (non-required). SQUASH-MERGED #1202 -> main `c019796d1`. Teardown done. **S6 FULLY COMPLETE.** (Also unblocks E4 = OTel spans depend on S6a.) Remaining AI-infra open items unchanged: S3/S4/S8 decisions + epics E1-E5.
- 2026-06-21 (E4b slice, build-loop, /loop, [AUTO]): drove E4b via build-loop. VERIFY-FIRST reconfirmed on main: OTel infra exists (tracing.ts Tracer+adapter, otel-init.ts SDK bootstrap, @opentelemetry deps) — the gap was only the WorkTrace/ExecutionTrace -> GenAI-span projection. Design (brainstorming): 3-level tree work-unit(WorkTrace)->execution(per ExecutionTrace, aggregate model/token attrs)->tool(per ToolCallRecord); per-individual-LLM-call spans DEFERRED (no data source persisted, only aggregate tokenUsage+turnCount); the flat `Tracer` needed a minimal `parent?: Span` extension honored end-to-end in the adapter (else inert-flat-in-prod trap). FAN-OUT plan grade (2 opus): BOTH caught 1 CRITICAL (getByWorkUnitId returns nested `{trace,integrity}`, flat read = silent-inert enrichment) -> fixed + seam tests strengthened. EXECUTE 4 TDD tasks (T1 opus iface, T2-4 sonnet). Gates ALL GREEN + independent opus review SHIP. main advanced twice during the loop (41fc94bac->d6bb02ba6->103237a5a, zero telemetry overlap) -> rebased clean. SQUASH-MERGED #1213 -> main `a49f97cbe`. Teardown done. **E4 now: 4a(=S6a) + 4b DONE; E4c (live trigger + OTLP export + any new env var) REMAINS = the next E4 slice (will SURFACE on the env var).** Other AI-infra open items unchanged: S3/S4/S8 decisions + E1/E2/E3/E5.
- 2026-06-21 (E4-COMPLETION /loop, build-loop, 3 slices): drove E4 to FULL completion. **A (gen_ai semconv coherence) #1230 MERGED** [AUTO] (dropped the deprecated `gen_ai.system`, derived `gen_ai.provider.name` on the root invoke_agent span from its executions; omit-not-fabricate). **B (real-SDK InMemorySpanExporter pin) #1235 MERGED** [AUTO] (apps/api test wiring `createOTelTracer` -> a REAL `BasicTracerProvider`+`InMemorySpanExporter`, asserts HrTime-exact timing/SpanKind/single-traceId parenting/coherent semconv; added `@opentelemetry/api`+`sdk-trace-base` exact-pinned devDeps; core stays OTel-free). **C (the LIVE TRIGGER) #1236 MERGED 2026-06-22** (squash `a4b7eff26`; user-authorized after a clean pre-merge architectural review — touches PlatformIngress stop-glob, all required CI green): an optional `onWorkUnitComplete` hook on `PlatformIngressConfig` fired at the 4 processed ok:true legs (gov-error/deny/require_approval/success), wired once in app.ts to `exportWorkUnitSpans` gated on `OTEL_EXPORTER_OTLP_ENDPOINT` (REUSE-flag, NO new env var); void/not-awaited + error-SWALLOWING + read-only + gated + tenant-safe + fire-once; NOT on idempotency replays / ok:false / the failed-execution throw-path (v1 limitation). All 3 TDD, full gates GREEN, independent fresh-context review SHIP (0 >=warn; C's review traced all 14 submit() exits + verified every safety invariant + producer-population in prod). **E4 COMPLETE (fully merged-or-surfaced).** Worktree `.claude/worktrees/ai-e4c-trigger` kept until #1236 merges. Remaining ai-infra-uplift items still decision/dependency-blocked: S3 drop/rescope, S4 4.7+ model bump, S8 memory-gov greenlight, E1/E2/E3/E5.
- 2026-06-22 (OBSERVABILITY-COMPLETION /loop): drove the 3 documented post-E4/#1236-review follow-ups to terminal. **(1) root-span for non-executing legs MERGED #1239** (squash `c201ebda9`, [AUTO] telemetry-only): the apps/api exporter no longer early-returns on zero ExecutionTrace rows; it fetches the tenant-guarded WorkTrace and projects a root-only span for deny/approval/gov-error legs (no-op preserved gated-off/both-absent/cross-tenant). TDD RED->GREEN, full gates green, independent review SHIP, all required CI green on final head. **(2) fire onWorkUnitComplete on the failed-execution throw path SURFACED #1241** (commit `62f9238251`, PlatformIngress stop-glob -> human merge): added the fire after the trace seal + before the rethrow; fires-once / original-error-preserved / throwing-hook-swallowed / no-double-fire; TDD, all gates green + independent review SHIP, all required CI green (test 10m52s real-DB), worktree `ai-obs-throwpath` kept until merge. **(3) S9 histogram ordering VERIFIED MOOT** (recordSkillContextFill already runs before the token throw on main; the hook/runtime breaches are pre-call guards) -> SKIPPED, no code (corrected the stale memory note). Ledger: `.claude/ai-obs-rootspan-loop-state.md`. **ai-infra-uplift autonomously-buildable work now FULLY EXHAUSTED**; remaining = S3/S4/S8/E1/E2/E3/E5 (all need a human decision).
- 2026-06-22 (session close): user requested a 2nd fresh-context review (codebase/arch alignment + works-as-intended) on both slices -> BOTH reviewers Ready-to-merge YES, 0 Critical/0 Important (4 cosmetic Minors, all accept-as-is; works-as-intended reviewer empirically re-confirmed all 3 key tests RED against old source). User then authorized the merge. **#1241 MERGED clean (squash `115c15d5f`)** -- mergeState CLEAN/MERGEABLE, zero overlap with the 4 intervening main commits, no rebase needed. Both worktrees + branches torn down, local main ff'd `115c15d5f`, no drift. **OBSERVABILITY-COMPLETION cluster CLOSED: both follow-ups merged (#1239 + #1241), S9 moot.**
