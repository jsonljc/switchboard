# S8b (memory.write governance intent) loop — externalized state (scratch, not committed)

Durable record: memory note project_ai_infra_uplift_state. Parent design: .claude/ai-infra-uplift-S8-design-decomposition.md. S8a ledger: .claude/ai-infra-uplift-S8a-loop-state.md.

Goal: S8b — register a governed `memory.write` intent so DeploymentMemory writes can flow through PlatformIngress (audited via WorkTrace, idempotent, governed). REGISTER the path only; does NOT reroute the writers (that is S8c). Fold in the S8a follow-up: tag the operator-memory route create `source:"operator"`.
Authority: AUTONOMOUS-WITH-GUARDRAILS (loop firing = greenlight of the S8b design). Merge ONLY if EVERY: all required CI green + independent review 0>=warn + NO genuinely-new product/consent/principal decision + high confidence. Any miss -> SURFACE.
Task-size: standard (one bounded PR).
Base: origin/main @ a2de9564f (re-fetched 2026-06-22; S8a #1247 squash 7d14402bc present; #1250 A12 + #1252 audit-docs on top, none touch memory/operator-intents). baseline_sha: a2de9564fe4d706cf9088b3726017078fac6f4e9 (worktree .claude/worktrees/ai-s8b, branch feat/ai-s8b-memory-write-intent; worktree:init [DB down, skipped migrate/seed] + install --frozen-lockfile + reset + build all GREEN).
merge_safety: stop-glob MECHANICAL filename check = NONE touched (no prisma/migrations [S8b is schemas+apps only, NO db store change, NO migration], no _governance_ filename, no _credential_, no external-send, no allowlist). SUBSTANTIVELY governance-adjacent (registers a new system_auto_approved mutating intent through GovernanceGate) -> design tagged [SURFACE: governance/ingress]; per the prompt's explicit AUTONOMOUS authority, merge-when-clean is permitted IF all gate conditions hold. Hold a HIGH review bar. independent_review = TBD.

## ORIENT brief (each claim tool-verified on a2de9564f / 7d14402bc)

- Premise CONFIRMED: NO `memory.write` intent anywhere (clean grep empty); compounding-service.ts has zero `.submit(` (writes still bypass PlatformIngress); S8a's `DeploymentMemorySourceSchema` + 4 cols present on main.
- operator_mutation is the EXACT pattern: `registerOperatorIntent` (apps/api/src/bootstrap/operator-intents.ts:165-185) registers defaultMode/allowedModes/executor="operator_mutation", parameterSchema:{} (validation is in the handler, NOT the registration), mutationClass:"write", budgetClass:"cheap", approvalPolicy:"none", approvalMode:"system_auto_approved", idempotent:true, spendBearing OMITTED (=false), allowedTriggers param (default ["api"]).
- Handler pattern: `buildXHandler(writer): OperatorMutationHandler` with `execute(workUnit)` -> parse `workUnit.parameters` via a Zod schema, read orgId from `workUnit.organizationId` + actor from `workUnit.actor.id`, call the store, return {outcome,summary,outputs,error} (reconcile-booking.ts:27-80 is the template). Conditional wiring in `bootstrapOperatorIntents` (operator-intents.ts:187-336): `if (dep) handlers.set(INTENT, buildHandler(dep))` + `if (dep) registerOperatorIntent(...)` + intentCount.
- Gate short-circuit CONFIRMED (governance-gate.ts:162-193): a `system_auto_approved` intent that is NOT financial (`isFinancialIntent`=false: not in FINANCIAL_AUTO_APPROVE_DENYLIST + carries no OUTBOUND_SPEND_KEYS) returns `{outcome:"execute"}` BEFORE the policy engine -> NO anchored allow policy needed. F4 guard (intent-registration.ts:106-110) only throws for spendBearing:true+auto-approve -> spendBearing:false is safe.
- Deployment resolution CONFIRMED (platform-deployment-resolver.ts:67-115): `buildPlatformDirectIntentPredicate` returns true for ANY `defaultMode==="operator_mutation"` intent -> resolves "platform-direct" BEFORE the slug lookup -> NO deployment_not_found (the prod-inert trap is auto-avoided; NO seed / NO PLATFORM_DIRECT_WORKFLOW_INTENTS entry needed). app.ts wires this predicate into the prod ingress (app.ts ~746-797).
- Entitlement (platform-ingress.ts:206-221): org-level at submit IF entitlementResolver configured; operator_mutation NOT exempt. Existing operator_mutation through-submit tests omit entitlementResolver. Org-level, same as all intents -> NOT S8b's concern (S8c caller wiring).
- Triggers: `Trigger = "chat"|"api"|"schedule"|"internal"` (core/platform/types.ts:8). S8c callers = conversation-compounding (internal) + decay cron (schedule) -> register `allowedTriggers:["internal","schedule"]`.
- Param-schema precedent: ReconcileBookingParametersSchema lives in @switchboard/schemas (receipted-booking-reconcile.ts). schemas barrel = `export * from "./deployment-memory.js"` -> add `MemoryWriteParametersSchema` there. `DeploymentMemoryCategorySchema` (enum) + `DeploymentMemorySourceSchema` exist for reuse.
- Store: `PrismaDeploymentMemoryStore.create` (db barrel:99) ALREADY accepts `{organizationId,deploymentId,category,content,confidence?,canonicalKey?,source?}` (S8a) -> S8b needs NO db change. validFrom is store-set to now (CreateDeploymentMemoryInput has NO validFrom).
- Fold-in target: deployment-memory.ts:60-66 route create (operator forget/correction route, DIRECT store write, NOT ingress) lacks `source` -> add `source:"operator"`. Test harness = deployment-memory-cross-tenant.test.ts buildApp+buildMockPrisma (deploymentMemory.create is a vi.fn spy).
- Reachability test template: recommendation-handoff-cron-live-path.test.ts (real IntentRegistry+ModeRegistry+GovernanceGate+PlatformIngress, submit through real ingress, seeded {id:"system"} principal). For S8b: empty policies (proves short-circuit), platform-direct via buildPlatformDirectIntentPredicate, assert outcome="completed" + store received the write + NOT approvalRequired.
- No concurrent S8b: no ai-s8b worktree, no PR matching memory.write/S8b. Other worktrees (a7-proof-chain, agents-fix-a15, alex-north-star-spec, audit-dashboard, readme, work-trace-bypass-guard #782) are DISJOINT workstreams -> do NOT touch.

## FRAME pressure-test (design-against-named-files; mechanical pattern, per prompt skipped greenfield brainstorming)

1. **Execution mode = operator_mutation, NOT skill-mode/executorBySlug.** RESOLVED against the code per [[feedback_operator_mutation_owner_action_recipe]]: a non-conversation, non-financial, system-internal mutation uses operator_mutation+system_auto_approved. The prompt scope's "anchored default-allow Policy + executorBySlug" is the SKILL-MODE recipe ([[feedback_new_skill_intent_governance_recipe]]); the operator_mutation recipe EXPLICITLY says that is WRONG for an owner/system-direct mutation and to "resolve against the code, not the memo." Consequence: NO anchored allow policy (gate short-circuits), NO deployment seed (operator_mutation carve-out), NO route-allowlist (flows through submit), NO eval:governance fixture (orthogonal grid), NO SKILL.md. This is an ARCHITECTURE decision settled by code+recipe, NOT a new product/consent/principal decision -> proceed autonomously, document prominently in the PR.
2. **approvalMode = system_auto_approved** (settled by the design). Correct for a high-volume internal learned-fact write (human-approving every memory write is nonsensical); the governance VALUE is the WorkTrace audit + idempotency + ingress routing, not an operator approval dial. spendBearing:false (no money) -> passes F4.
3. **validFrom OMITTED from the param schema (deviation from the prompt's illustrative list, documented).** Rationale: S8a's store sets validFrom=now and CreateDeploymentMemoryInput does not accept it; no current/planned S8c writer asserts a custom valid-time (compounding writes at conversation-end ~= now; decay does not create). Adding it = speculative generality (YAGNI / "no premature abstractions") OR a dead/decorative param (violates ship-clean). Param schema carries EXACTLY what the store accepts: deploymentId, category, content, source (REQUIRED — provenance is the point of S8), confidence?, canonicalKey?. orgId from workUnit. Writer-asserted valid-time = a deliberate future extension. (Plan-grade CODE-GROUNDED lens to sanity-check; flip to threading validFrom only if it flags this.)
4. **Handler error handling = minimal/faithful** (mirror reconcile): Zod parse throws on bad params (ingress -> failed trace); store errors propagate (a LIVE-collision P2002 rethrow = honest failed trace; S8a resurrects invalidated tombstones). S8c owns dedup/idempotency-key wiring + any graceful-duplicate mapping. Keep S8b focused on "register + reachable."
5. **actor/principal**: handler reads `workUnit.actor.id` (authenticated/seeded). S8c callers = seeded {id:"system"}. S8b test submits as seeded {id:"system",type:"system"}. SETTLED by the design (S8a FRAME pt 4). NOT new.
6. **No genuinely-new product/consent/principal decision** -> the prompt's ONE hard-stop does NOT fire. memory.write is internal (no outbound/consent surface); entitlement is org-level standard; principal settled; system_auto_approved settled by design. Proceed.

| step                                      | done-condition (test/cmd)                        | RED proof                                         | status | evidence                                                                                                                                                        |
| ----------------------------------------- | ------------------------------------------------ | ------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T1 schemas MemoryWriteParametersSchema    | --filter schemas test                            | "Cannot read properties of undefined (safeParse)" | DONE   | 9b36e098b; schemas 21/21 GREEN; dist rebuilt                                                                                                                    |
| T2 intent const + handler + unit test     | --filter api test memory-write                   | "Failed to load url ./memory-write.js"            | DONE   | b7514cbb1; handler 3/3 GREEN; tsc clean                                                                                                                         |
| T3 wiring + app.ts producer + live-path   | --filter api test memory-write-live-path         | "intent_not_found" (pre-wiring, submit not ok)    | DONE   | fa0b19a98; live-path 3/3 + handler GREEN; carve-out-off->deployment_not_found verified load-bearing; app.ts producer wired; tsc clean; 4 em-dashes caught+fixed |
| T4 fold-in source:"operator" route + test | --filter api test deployment-memory-cross-tenant | "expected null to be 'operator'"                  | DONE   | dba53c37a; 8/8 GREEN (7 pre-existing + 1 new); tsc clean                                                                                                        |

2B plan-grade (3 opus CRITIC/COMPLETENESS/CODE-GROUNDED) = ALL PASS (no >=2-same-issue, no critical/important; CODE-GROUNDED verified every sketch vs real code: gate short-circuit fires [memory.write non-financial, confidence NOT a spend key], deployment_not_found assertion EXACT [platform-ingress.ts:242-252], producer-population real, all barrels/types/test-helpers confirmed). REFINEMENTS to fold into EXECUTE: (a) PR-note that creative-taste-sweep.ts cron writes category:"taste" memory with NO source — genuinely out of S8b scope (cron=S8c reroute target; enum has no taste/creative member) so deferred, not missed; (b) comment in the live-path harness that omitting entitlementResolver is load-bearing (a future editor adding one would short-circuit at entitlement_required before the auto-approve path); (c) copy the FULL 3-method throwingResolver (resolveByOrgAndSlug throws; resolveByDeploymentId/resolveByChannelToken throw "not used") from revenue-proof-digest-delivery-e2e.test.ts; note store.create return is a union DeploymentMemory|{id} that structurally satisfies {id:string} (PR-note so a reviewer does not flag it). route-allowlist already covers deployment-memory.ts (no new entry).

gate_results (local, on rebased base): typecheck=PASS(22) test=PASS(api 2420 incl new memory-write/live-path/cross-tenant; schemas 917) lint=PASS(0-err) format=PASS arch=PASS(0-err) verify-fast=PASS(7 checks) check-routes=PASS(exit0, no new store-mutation) build=PASS(10) security=PASS(audit exit0, GHSAs in ignore list) review=SHIP(independent opus fresh-context, 0 findings >=warn; 2 nits: 1 fixed [entitlementResolver comment reworded], 1 no-action [S8c seam unpinned = correct for register-only]).

VERIFY+CONVERGE: ALL local gates GREEN + independent review SHIP(0>=warn). Diff scoped schemas+apps/api only (10 files, +415/-1; NO db/prisma/migration). Rebased onto fresh origin/main 089318ce6 (A15 per-org WA creds #1253; A15 ALSO touched app.ts but DISJOINT region -> git auto-merged clean, re-verified GREEN: typecheck 22 + build 10 + targeted tests 14+21). Final rebased SHAs dc9f43ff4..bbaebb35c (5 commits). PUSHED + PR #1254 OPENED (body documents all decisions + the taste-sweep deferral). CI watching (background poller bppwo8hhq) for required checks typecheck/lint/test/security. merge-decision: all 4 autonomous-merge conditions HOLD (CI[pending]/review-0>=warn/no-new-decision/high-confidence) + NO stop-glob filename touched -> MERGE when required CI green (squash with Co-Authored-By trailer -> ff main -> teardown).

gate_results: typecheck=PASS test=PASS lint=PASS format=PASS arch=PASS verify-fast=PASS check-routes=PASS security=PASS build=PASS review=SHIP(0>=warn) | CI(PR#1254) ALL REQUIRED GREEN: typecheck+lint+test(11m53s real-DB)+security (branch-protection required set = exactly those 4; required_reviews=0; mergeStateStatus CLEAN). | post-merge integration check on merged main (A7#1251 + A16#1255 + S8b) = typecheck 22/22 after pnpm reset (the db-typecheck red was the stale-dist false-alarm, clean in isolation) + memory.write tests 6/6.

**S8b MERGED -> PR #1254 squash `3a876f4c3` (2026-06-23).** Autonomous merge (all 4 conditions held: CI green / independent review 0>=warn / no new product-consent-principal decision / high confidence; no merge-stop glob filename touched). GitHub squashed cleanly on top of concurrently-merged A7#1251 (touched attendance.ts, sibling file, no conflict) + A16#1255 (no overlap). Teardown CLEAN: worktree force-removed (build artifacts), local+remote branch deleted, primary main ff'd 3a876f4c3. **S8c (route compounding-service + decay cron writes through PlatformIngress as the seeded {id:"system"} actor, trigger internal/schedule) is the LAST remaining S8 slice.**

carry_forward (<=150 words): ORIENT+FRAME done on a2de9564f. Design = operator_mutation recipe (NO policy/seed/allowlist; gate short-circuits non-financial system_auto_approved; carve-out auto-resolves platform-direct). S8b = schemas (param schema) + apps/api (intent const + memory-write handler + bootstrap wiring + app.ts store dep + reachability live-path test) + route source:"operator" fold-in. NO db/prisma/migration change (store already accepts source). Next: write plan -> 2B fan-out plan-grade (3 opus) -> worktree .claude/worktrees/ai-s8b off fresh main -> EXECUTE TDD -> VERIFY (incl. check-routes --mode=error + local-verify-fast) -> independent review -> merge-if-all-clean-else-surface.

## Log

- 2026-06-22: ORIENT (ground truth via Explore agent + direct reads; premise + full operator_mutation machinery confirmed) + FRAME (mechanical, design-against-named-files; 6 pressure-test conclusions; operator_mutation resolved over skill-mode; validFrom omitted documented; no new decision) complete. -> PLAN.
