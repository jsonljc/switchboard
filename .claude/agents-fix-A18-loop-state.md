# A18 — Consent: CTWA window-not-permanent-optin (P1-5) + dedup-reuse no-regreeting (P1-7) — loop state (orchestration scratch, not committed)

Durable record lives in memory note project_second_wave_gap_eval_2026_06_22 + moc_governance / moc_launch_readiness.

Goal: (P1-5) a CTWA ad-click is NOT a permanent messaging opt-in — only an Instant Form submit is; a genuine CTWA inbound rides the 24h lastWhatsAppInboundAt window. (P1-7) a same-person reuse (2nd leadgenId, A4 identity match) must NOT re-greet / re-record an inquiry — only a freshly CREATED contact does.
Authority: user said "build A18". A18 is plan-marked SURFACE (consent / multi-tenant correctness) -> STOP at SURFACE-before-merge (human merge call). Consent stop-glob also forces surface.
Task-size: standard (one cohesive PR). 4 prod files + 1 shared schema type + ~4 test files across schemas/core/ad-optimizer/api. NO Prisma/DB migration.
Base: origin/main @ e01d78383 (includes A17 #1256). worktree .claude/worktrees/a18-consent-optin-regreeting, branch fix/a18-consent-optin-regreeting. baseline_sha: e01d78383a206a31677f317ced05e04ddfdb3235
merge_safety: stop-glob touched = YES (consent path: lead-intake opt-in basis + greeting gate inputs). SURFACE regardless of gates. independent_review=<pending>

## Ground truth (tool-verified on e01d78383)

- P1-5 leak: lead-intake-handler.ts:102-109 sets optInSource="ctwa" (=> upsertContact messagingOptIn=true, messagingOptInSource="ctwa", a DURABLE opt-in) for any whatsapp-channel ctwa lead. canSendWhatsAppTemplate (whatsapp-window.ts:21-32) returns allowed when messagingOptIn===true REGARDLESS of the 24h window -> a CTWA ad-click becomes a permanent send basis. evaluateProactiveSendEligibility (proactive-eligibility.ts:80-87) -> no_optin only when NEITHER window NOR messagingOptIn. So removing the durable opt-in for ctwa makes a CTWA contact greetable ONLY inside the lastWhatsAppInboundAt 24h window, blocked no_optin after — exactly the acceptance.
- CTWA producer = CtwaAdapter (ad-optimizer/lead-intake/ctwa-adapter.ts:69 source:"ctwa") wired from apps/chat managed-webhook.ts:158 on a genuine WhatsApp INBOUND -> the window basis is populated for real CTWA leads. CtwaAdapter.ingest returns void and IGNORES the lead.intake result -> unaffected by the LeadIntakeResult change.
- P1-7 gap: lead-intake-handler.ts returns duplicate:true ONLY for the idempotency hit (findContactByIdempotency, line 70-71). The A4 identity-REUSE path (decideContactMatch kind "reuse", line 93-98) falls through to createActivity (line 129) and returns duplicate:FALSE (line 137). The Instant Form orchestrator meta-lead-intake-workflow.ts:139 skips greeting+inquiry only on ingestResult.duplicate -> a 2nd leadgenId for one corroborated person (reuse) => duplicate:false => greeted + inquiry-recorded TWICE.
- Seam the plan did NOT name: the orchestrator reads ingestResult.duplicate from InstantFormAdapter (ad-optimizer), NOT directly from LeadIntakeResult. instant-form-adapter.ts:84-87 InstantFormIngestResult{contactId,duplicate}; ingest() reads outputs.duplicate (line 112). lead-intake-workflow.ts:32 (core) is the result->outputs map. So P1-7 threads through 4 files: handler -> lead-intake-workflow wrapper (outputs) -> instant-form-adapter (InstantFormIngestResult) -> orchestrator.
- Shared type home: @switchboard/schemas/lead-intake.ts (Layer 1; core + ad-optimizer + api all import it; ad-optimizer is Layer 2 and CANNOT import core). messagingOptInSource enum (lifecycle.ts:99) keeps "ctwa" (historical value; removing = migration risk, out of scope; harmless now-unused-by-this-handler).
- Existing tests that ENCODE the bug (will be inverted = the RED): lead-intake-handler.test.ts:79-89 (ctwa => messagingOptInSource:"ctwa") and :114-126/:121 (reuse toEqual {duplicate:false}); meta-lead-intake-workflow.test.ts mocks return {duplicate} w/o outcome and branch on duplicate.
- createActivity-on-reuse: KEEP. A reuse is a distinct 2nd ad-touch event for an existing contact (attribution) and the idempotency hit already skips createActivity (early return). Only the orchestrator's greeting + meta.lead.inquiry.record child are gated. No store change.

## Design (determined; fix against named files; no brainstorm)

- ADDITIVE, back-compat. Keep LeadIntakeResult.duplicate (= idempotency hit only, unchanged) and ADD outcome. Keep InstantFormIngestResult.duplicate, ADD outcome. Orchestrator switches to gating on outcome==="created".
- P1-5: lead-intake-handler.ts optInSource = (isWhatsAppLead && intake.source==="instant_form") ? "web_form" : null. Drop the ctwa->"ctwa" branch. Reword the comment (ctwa rides the 24h window, not a durable opt-in).
- Counters: orchestrator increments `duplicates` for anything not "created" (idempotent_duplicate OR reused) — preserves the {received,created,duplicates} output shape.

| step | file                                     | done-condition (test/cmd)                                                                                                                                                                                                                                                                                                                                 | RED proof                                         | status | evidence |
| ---- | ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ------ | -------- |
| 1    | schemas/lead-intake.ts                   | add LeadIntakeOutcomeSchema=z.enum(["created","reused","idempotent_duplicate"]) + type; lead-intake.test.ts parses 3 values, rejects junk                                                                                                                                                                                                                 | enum import missing                               | todo   |          |
| 2    | core/intents/lead-intake-handler.ts      | LeadIntakeResult+outcome; set idempotency->idempotent_duplicate, reuse->reused, create->created; handler.test asserts outcome at all 3 paths                                                                                                                                                                                                              | outcome undefined at each return                  | todo   |          |
| 3    | core/intents/lead-intake-handler.ts      | only instant_form sets durable opt-in; ctwa->no messagingOptIn; INVERT existing :79-89 test + ADD ctwa-no-optin assertion; keep instant_form web_form                                                                                                                                                                                                     | current impl sets ctwa optIn (inverted test reds) | todo   |          |
| 4    | core/intents/lead-intake-workflow.ts     | outputs include outcome; lead-intake-workflow.test asserts outputs.outcome for created+idempotent+reused                                                                                                                                                                                                                                                  | outputs.outcome undefined                         | todo   |          |
| 5    | ad-optimizer/.../instant-form-adapter.ts | InstantFormIngestResult + `outcome?:LeadIntakeOutcome` (OPTIONAL); ingest STRICT-parses outputs.outcome via LeadIntakeOutcomeSchema.safeParse -> undefined on miss/invalid (NO `duplicate?..:created` fail-open — CRITIC blocker). adapter.test: surfaced created/reused/idempotent + MISSING-outcome -> outcome undefined; update toEqual :171/:181/:238 | result has no outcome                             | todo   |          |
| 6    | api/.../meta-lead-intake-workflow.ts     | gate `if(ingestResult.outcome!=="created"){duplicates++;continue;}` (keep `if(!ingestResult)continue` FIRST) => reuse AND missing-outcome BOTH skip greeting (fail closed). orchestrator.test: reuse->0 children/duplicates=1, MISSING-outcome->0 children, created->2 children; update mocks :33/:201 outcome:"created", :97 "idempotent_duplicate"      | reuse currently greets; mocks lack outcome        | todo   |          |

gate_results: typecheck=· test=· lint=· format=· arch=· verify-fast=· build=· eval=n/a(no decision-engine change; greeting gate logic unchanged) · security=· review=·
carry_forward (<=170 words): consent slice; SURFACE before merge (plan + stop-glob). Additive `outcome?` (created|reused|idempotent_duplicate) shared via schemas (ad-optimizer can't import core). Adapter FAIL-CLOSED: strict safeParse, missing outcome => undefined => orchestrator skips greeting (NOT created). ctwa durable opt-in removed -> rides 24h window (the CTWA inbound itself sets ConversationThread.lastWhatsAppInboundAt via gateway.handleIncoming, NOT CtwaAdapter which returns void). Acceptance: (P1-5) ctwa upsert has NO messagingOptIn; a ctwa contact greetable in-window, no_optin after (canSendWhatsAppTemplate). (P1-7) 2 leadgenIds one person -> exactly 1 greeting + 1 inquiry (reuse skips both). createActivity-on-reuse KEPT (distinct attribution event). Build order: rebuild core dist before ad-optimizer+api tests (else stale-dist masks the seam). HUMAN-VERIFY at surface: does the Meta Instant Form actually carry a WhatsApp opt-in checkbox? (instant_form durable-optIn basis; real-world form config, not code-checkable.)

## FAN-OUT verdict (3 opus graders) + applied REVISE

CRITIC=REVISE (1 real fail-open blocker, applied). COMPLETENESS=PASS. CODE-GROUNDED=PASS. Cap-1 REVISE, no second full grade.

- [applied] adapter must FAIL CLOSED: drop the `duplicate?idempotent_duplicate:created` fallback (reuse keeps duplicate:false -> would map to created -> re-greet when outcome absent / stale dist). Strict safeParse; undefined on miss; only explicit "created" greets. New tests pin missing-outcome -> no greet (steps 5+6).
- [noted, out of scope] organic_inbound durable opt-ins (alex.ts:60, resolve-contact-identity.ts:38) NOT touched (genuine 2-way inbound = legit durable basis).
- [noted] lead-retry cron is a 2nd InstantFormAdapter.ingest() consumer; verified SAFE (checks only `!ingestResult`, never greets/reads outcome) -> no code change.
- [noted] orchestrator `duplicates` now also counts reuse (no behavioral reader; harmless conflation).
- managed-webhook ctwa wiring is ~L188-200 (my ground-truth cited a comment line); conclusion unchanged.

## Log

- 2026-06-24: ORIENT done on e01d78383 (post-A17). Gap tool-confirmed both ends + the unnamed adapter seam + the eligibility/window basis. Design determined (additive 3-way outcome + drop ctwa durable opt-in). Worktree created. -> PLAN/TASKS.
- 2026-06-24: FAN-OUT = 2 PASS + 1 REVISE. Applied the CRITIC fail-open fix (adapter fail-closed). -> EXECUTE TDD.
