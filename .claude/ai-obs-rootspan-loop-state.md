# ai-obs (observability-completion follow-ups) loop — externalized state (scratch, uncommitted)

Durable record: `project_ai_infra_uplift_state` + `.claude/ai-infra-uplift-backlog.md` (E4 line) + `.claude/ai-infra-uplift-E4c-loop-state.md` (#1236 review findings). E4 FULLY MERGED (#1236 `a4b7eff26`).

Cluster = 3 documented post-E4 / #1236-review follow-ups (NOT new features; the ai-infra FEATURE backlog is exhausted — S3/S4/S8/E1/E2/E3/E5 are decision/dependency-blocked, DO NOT attempt).
Authority: AUTONOMOUS-WITH-GUARDRAILS. Slice 1 (telemetry-only, no stop-glob) -> MERGE CLEAN after independent review 0>=warn + all required CI green on FINAL head + high confidence. Slice 2 (PlatformIngress stop-glob) -> SURFACE. Slice 3 -> verify-then-likely-skip.

## ORIENT (verified on main a4b7eff26, all tool/file-backed)

- **S1 [AUTO] root-span for non-executing legs — REAL.** `exportWorkUnitSpans` (apps/api/src/telemetry/work-unit-span-export.ts:134-136): `if (!enabled) return; const traces=...; if (traces.length===0) return;` returns BEFORE reading the WorkTrace -> deny / require_approval / governance-error work units (a WorkTrace but ZERO ExecutionTrace rows) render NO span. VERIFY-FIRST CONFIRMED: `projectWorkUnitSpans` (packages/core/src/telemetry/work-unit-spans.ts:112-204) creates the root span at :128 BEFORE the executions loop at :148; with `executions:[]` the loop is skipped, `deriveWorkUnitProvider([])`=undefined (no provider attr — correct on a no-model root), `root.end()` :203 closes exactly one root. Fix = fetch wt ALWAYS (tenant-guarded), project root-only when zero-traces-but-wt, keep no-op when (gated off) OR (zero traces AND no tenant-matched wt). Files: apps/api work-unit-span-export.ts + its test ONLY. NO stop-glob -> [AUTO].
- **S2 [SURFACE] throw-path export — REAL.** platform-ingress.ts:464-496 catch: seals the trace (finalizeTrace :477 keyed / persistTrace :479 non-keyed) + recordInfrastructureFailure :489, then `throw executionErr` :495 with NO `fireWorkUnitComplete`. The 4 ok:true legs fire (:297/:312/:336/:518); the throw path does not. Fix = `this.fireWorkUnitComplete(workUnit);` before :495 (trace already sealed). Touches PlatformIngress core invariant stop-glob -> SURFACE.
- **S3 [SKIP] S9 histogram ordering — MOOT.** skill-executor.ts:390 `recordSkillContextFill` runs BEFORE the token throw :396 (comment :386 documents the intent — over-budget turns ARE observed). The other breaches :355 (hook abort) + :366 (runtime/time) are PRE-CALL guards (fire before `callAdapterWithDeadline` :369 -> no new token usage that turn -> nothing fill-relevant to record; recording there would re-emit the prior turn's already-recorded ratio). No breach path skips a fill-relevant record. Memory note (`project_ai_infra_uplift_state` "S9 KNOWN MINOR") is STALE. Ship NOTHING. Optional emitToolSpan helper fold-in: SKIP (slice 1 does not edit work-unit-spans.ts).

## Slice 1 — render a root-only span for non-executing legs [AUTO]

Goal: a deny / require_approval / governance-error work unit (WorkTrace present, zero ExecutionTrace rows) renders exactly ONE root span (governance outcome / status / org / intent / risk / duration) instead of early-returning. Preserve: gated-off no-op, zero-traces-AND-no-tenant-wt no-op, cross-tenant no-op (no bare root, no leak), has-traces full waterfall unchanged.
Base: origin/main @ a4b7eff26. Worktree `.claude/worktrees/ai-obs-rootspan` (branch `feat/ai-obs-root-span`). Task-size: standard-small (1 src file + tests, telemetry only). merge_safety: stop-glob touched=NO -> [AUTO]. Plan: `.claude/ai-obs-rootspan-plan.md`.

| step | done-condition (test/cmd)                                                                                                                 | RED proof                                                                                                                                           | status                          | evidence                                                                                                                                                                                                          |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1    | zero-traces+tenant-wt -> exactly 1 root span (no children); no-op preserved for null-wt + cross-tenant + gated-off; full gate suite green | RED proven: "renders exactly one root span" failed `expected [] to have a length of 1 but got +0` (test:378); only the 1st new test failed pre-impl | MERGED #1239 (squash c201ebda9) | commit bcf4fed4b; GREEN 16/16; +82/-2. All gates green + review SHIP. ALL CI green on final head bcf4fed4b (test 11m28s real-DB). [AUTO] squash-merged; worktree+branch torn down, main ff'd c201ebda9, no drift. |

gate_results (commit bcf4fed4b, local): typecheck=PASS(22/22) test=PASS(api 2344 / core 4433; full pnpm test deferred to CI real-DB, PG down) lint=PASS(0 err) format=PASS arch=PASS verify-fast=PASS(no route/env debt) build=PASS(10/10) security=PASS(only pre-existing ignored GHSAs) review=SHIP(0>=warn; 2 cosmetic nits)
merge_safety: stop-glob touched=NO (apps/api telemetry only; core untouched; no env var) -> [AUTO]. independent_review=SHIP. NO SURFACE condition fired. MERGED #1239 (c201ebda9); all required CI green on final head bcf4fed4b (test 11m28s real-DB).

## Slice 2 — fire onWorkUnitComplete on the failed-execution throw path [SURFACE]

Goal: the failed-execution rethrow path (platform-ingress.ts:495) fires `fireWorkUnitComplete(workUnit)` ONCE (trace already sealed at :477 keyed / :479 non-keyed) so failed work units render a span (composes with slice 1: zero-row failures -> root-only). Closes the #1236-documented v1 limitation. The 4 ok:true legs already fire (:297/:312/:336/:518); the throw path is mutually exclusive with the :518 success fire (no double-fire).
Base: origin/main @ c201ebda9 (includes slice 1). Worktree `.claude/worktrees/ai-obs-throwpath` (branch `feat/ai-obs-throw-path-fire`). Task-size: standard-small (1 src line + harness/2 tests). merge_safety: stop-glob touched=YES (PlatformIngress core invariant) -> SURFACE. Plan: `.claude/ai-obs-throwpath-plan.md`.

| step | done-condition (test/cmd)                                                                                                                                 | RED proof                                                                                                                                                                                      | status         | evidence                                                                                                                                                            |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2    | throwing dispatch fires hook ONCE w/ {orgId,workUnitId} AND still rethrows boom AND a throwing hook still surfaces boom; no double-fire; full gates green | RED proven: "fires ... on the failed-execution throw path" failed `expected spy called 1 times, got 0` (test:225); .rejects.toBe(boom) passed pre-impl (contract guard, not the fire detector) | SURFACED #1241 | commit 62f9238251; GREEN 6/6 (+ core 4435); +46/-0 (platform-ingress.ts + test). All gates green + review SHIP (0>=warn). PlatformIngress stop-glob -> human merge. |

gate_results (slice 2, commit 62f9238251): typecheck=PASS(22/22, consumers clean vs changed core) core-test=PASS(4435+2 new) lint=PASS(0 err) format=PASS arch=PASS verify-fast=PASS(no route/env debt) build=PASS(10/10) security=PASS(pre-existing ignored GHSAs) review=SHIP(0>=warn; 2 cosmetic nits). merge_safety: stop-glob=YES (PlatformIngress core invariant) -> SURFACED #1241, AWAIT human merge.

## Log

- 2026-06-22: ORIENT done — all 3 scope items verified on a4b7eff26. S3 confirmed moot (the predicted "already moot" one). Slice 1 worktree created off origin/main. -> PLAN -> EXECUTE.
- 2026-06-22: Slice 1 PLAN + EXECUTE + VERIFY done. TDD RED->GREEN (sonnet impl subagent, commit bcf4fed4b), full gate-runner ALL GREEN (sonnet), independent fresh-context review SHIP (opus, 0>=warn). PR #1239 opened (telemetry-only, no stop-glob -> [AUTO]). CI queued (evals already green incl. Claim Classifier; required test/typecheck/lint/security pending). AWAIT CI green on bcf4fed4b -> squash-merge -> teardown -> Slice 2 (throw-path fire, SURFACE) off updated main. /loop wake ~20min.
- 2026-06-22 (wake): Slice 1 ALL CI green on bcf4fed4b (4 required: typecheck/lint/test[11m28s real-DB]/security; + arch/CodeQL/docker/6 evals). Pre-merge divergence re-check clean (origin/main unchanged a4b7eff26; three-dot = exactly the 2 telemetry files; no concurrent overlap on apps/api/src/telemetry; #1238 touches core/telemetry/metrics.ts = different file). [AUTO] SQUASH-MERGED #1239 -> main c201ebda9. Teardown done (worktree+branch removed, main ff'd, no drift). -> Slice 2 (throw-path fire) worktree created off c201ebda9; PLAN written; EXECUTE dispatched.
- 2026-06-22 (wake, cont.): Slice 2 EXECUTE + VERIFY done. TDD RED->GREEN (sonnet, commit 62f9238251; RED = spy called 0 not 1, .rejects.toBe(boom) held pre-impl as a contract guard). Full gate-runner ALL GREEN (typecheck 22/22 consumers-clean, core 4435+2, lint/format/arch/verify-fast/build/security) + independent fresh-context review SHIP (opus, 0>=warn; traced all submit exits: fire-once / contract-unchanged / throwing-hook-safe / no-double-fire / replay-unaffected; RED non-vacuous by revert). Touches PlatformIngress stop-glob -> SURFACED PR #1241 (NOT merged; human merge call). CI queued (head 62f9238251). Slice 3 = MOOT (skip, documented). NEXT (final wake): confirm #1241 CI green -> mark backlog + memory -> ONE summary -> STOP. Cluster terminal (1 merged, 2 surfaced, 3 moot).
- 2026-06-22 (final wake): #1241 ALL CI GREEN on head 62f9238251 (4 required: typecheck/lint/test[10m52s real-DB]/security + arch/CodeQL/docker/6 evals; mergeState CLEAN, mergeable). Left OPEN for the human merge call (PlatformIngress stop-glob). Backlog + memory (project_ai_infra_uplift_state, incl. the stale-S9 note CORRECTED to verified-moot) marked. Main worktree: local==origin/main 7b593bdf4 (advanced under concurrent sessions; slice 1 c201ebda9 IS in history; slice-1 change present; NO tracked-file drift; no next-env.d.ts). slice-2 worktree `ai-obs-throwpath` KEPT until #1241 merges. **CLUSTER DONE. LOOP STOPPED.** ai-infra-uplift autonomously-buildable work FULLY EXHAUSTED; open items all need a human decision (S3 drop/rescope, S4 4.7+ model bump, S8 memory-gov greenlight, E1/E2/E3/E5).
- 2026-06-22 (session close, /goal merge+hygiene): user requested a 2nd fresh-context 2-reviewer pass (arch-alignment + works-as-intended) -> BOTH Ready-to-merge YES, 0 Critical/0 Important (4 cosmetic Minors accept-as-is; works-as-intended reviewer empirically re-confirmed all 3 key tests RED vs old source + full suites green). User authorized the merge. **Slice 2 #1241 MERGED clean (squash `115c15d5f`)**: re-fetched, polled mergeState -> CLEAN/MERGEABLE, verified zero overlap with the 4 intervening main commits (no rebase needed), squash-merged. Teardown: worktree `ai-obs-throwpath` + branch removed, local main ff'd `115c15d5f`, slice-2 change confirmed present, NO tracked-file drift, no next-env.d.ts. pr-body scratch removed. BOTH observability follow-ups now on main (#1239 `c201ebda9` + #1241 `115c15d5f`); S9 moot. **CLUSTER FULLY CLOSED.**
