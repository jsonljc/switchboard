# E4c (OTel work-unit projection LIVE + timeline) — build-loop state (scratch, uncommitted)

Durable record: `project_ai_infra_uplift_state` + backlog `.claude/ai-infra-uplift-backlog.md` (E4 line) + `.claude/ai-infra-uplift-epics-E1-E5-decomposition.md` (E4c section). Predecessor: `.claude/ai-infra-uplift-E4b-loop-state.md` (the merged projection + the deep-review follow-ups carried here).

Goal: make the E4b OTel GenAI-span projection LIVE + the trace TIMELINE usable. E4b shipped a structurally-correct but temporally-flat (~0ms spans) un-triggered projection. E4c closes both gaps.
Authority: AUTONOMOUS-WITH-GUARDRAILS. The pure quality/timeline fix (Tracer timing+kind, synth offsets, cache attrs, setIfString guard, barrel export — NO env var, NO ingress seam) is NOT a stop-glob -> MERGE CLEAN after independent review (0 sev>=warn, all required CI green). The live trigger (PlatformIngress/route/skill-executor seam) OR a new env var -> SURFACE.

## Two-slice split (build-loop multi-slice rule; prompt-authorized)

- **Slice 1 [AUTO] — timeline/quality fix (THIS session, merge clean):** Tracer `startTime/endTime/kind` extension honored in the OTel adapter; `projectWorkUnitSpans` synthesizes honest start/end from persisted timestamps + SpanKind; cache/cost attrs; setIfString root guard; barrel-export OTelContextBridge. Telemetry-only, no stop-glob.
- **Slice 2 [SURFACE] — live trigger wiring:** the call to `exportWorkUnitSpans` after a work unit completes (+ any dedicated enable-flag). Touches the ingress submit path and/or env-allowlist -> SURFACE with the design fork for a human decision.

Task-size: Slice 1 = standard (one bounded PR, ~7 files, cohesive). Base: origin/main @ cc6fd4645 (advanced from 147a93da7 mid-ORIENT — concurrent #1214 test-only revenue-proof merge on top; worktree is exactly on the new tip, three-dot 0/0; zero telemetry overlap). Worktree: `.claude/worktrees/ai-e4c-otel-live` (branch `feat/ai-e4c-otel-timeline`). Three-dot diffs.

## ORIENT brief (VERIFY-FIRST, all tool/file-backed on current main 147a93da7)

- E4b MERGED a49f97cbe present on main; all 6 files present. NO concurrent E4c/otel/telemetry session (no PRs/branches/worktrees). Projection chain fully UN-TRIGGERED in prod (callers = tests only). [V3]
- **Timing data [V1]:** WorkTrace (`platform/work-trace.ts:32,38-41`): `requestedAt:string`(REQ), `governanceCompletedAt:string`(REQ), `executionStartedAt?:string`(OPT), `completedAt?:string`(OPT), `durationMs:number`(REQ); all ISO strings. `getByWorkUnitId` returns them on `.trace` un-stripped (`prisma-work-trace-store.ts:471-475` mapRowToTrace). ExecutionTrace row (findByWorkUnitId, ordered createdAt asc): ONLY `createdAt:Date`(@default now, = persist≈exec END) + `durationMs:number` — NO per-exec start/end ts. ToolCallRecord (`skill-runtime/types.ts:138`): ONLY `durationMs` — zero absolute time. `tokenUsage`: `{input,output,cacheRead?,cacheCreation?,costUsd?,model?}` all present on persisted row.
- **OTel API [V2]:** `@opentelemetry/api@1.9.1`. `SpanOptions.startTime?:TimeInput` + `kind?:SpanKind` (SpanOptions.d.ts:14,20); `Tracer.startSpan(name,options?,context?)`; `Span.end(endTime?:TimeInput)`; `TimeInput = HrTime|number|Date` (number=epoch-millis OK). SpanKind {INTERNAL=0,SERVER=1,CLIENT=2,PRODUCER=3,CONSUMER=4} (numeric enum). Adapter (`tracing.ts:101`) passes `undefined` in options slot today; passthrough purely additive. Core can pass `kind` as a NUMBER (mirror the stable OTel constants) -> NO otel import in core; otel-init unchanged for Slice 1.
- **Completion seam [V3]:** `PlatformIngress.submit` (platform-ingress.ts:93-502) SYNC, returns `{ok,result,workUnit}`; `workUnit.id`+`.organizationId` in scope at return (work-unit.ts:11-14). Finalize is INSIDE submit (tracePersister.finalizeTrace, platform-ingress.ts:488) = CORE (Layer 3, CANNOT call apps exporter). ~20+ apps submit sites; NO central post-completion callback on PlatformIngressConfig. `app.prisma`+`app.workTraceStore` decorated; precedent `new PrismaExecutionTraceStore(app.prisma)` (marketplace.ts:758). -> Slice 2 trigger = existing-route-side-effect OR a new `onWorkUnitComplete?` callback in PlatformIngressConfig (SURFACE either way).
- **Env [V3]:** only `OTEL_EXPORTER_OTLP_ENDPOINT`+`OTEL_SERVICE_NAME` allowlisted (required_in_env_example). Exporter+SDK both gate on the SAME `OTEL_EXPORTER_OTLP_ENDPOINT` (one switch). A dedicated work-unit flag = NEW env var -> env-allowlist stop -> Slice 2/SURFACE.

## Honest timing model (LOCKED at ORIENT, evidence-backed)

- **work_unit span:** start = `requestedAt` (REAL); end = `completedAt` if present else `requestedAt + durationMs` (REAL/real-anchored). kind=INTERNAL. NOT marked synthetic.
- **execution span:** end = `createdAt` (REAL persist≈end); start = `createdAt − durationMs` (DERIVED from real duration). Ordered createdAt asc -> sequence honest. kind=CLIENT (GenAI inference span). MARK `switchboard.timing.synthetic=true` (start derived).
- **tool span:** NO absolute time -> packed sequentially within parent exec window by array order (= LLM turn order), accumulating `durationMs`. kind=INTERNAL. MARK `switchboard.timing.synthetic=true` (fully reconstructed; ignores inter-tool thinking gaps). Do NOT fabricate precision — the mark is the honesty signal.
- All times as epoch-millis numbers parsed from the persisted ISO strings / Dates. NO wall-clock (`Date.now()`) anywhere.

## FRAME — LOCKED (brainstorming, autonomous; doctrine-checked, designed against named existing files)

**Tracer extension (the genuine interface change, honored end-to-end):** add `options?: SpanStartOptions = { startTime?: number; kind?: number }` as a 4th param to `Tracer.startSpan(name, attributes?, parent?, options?)` (back-compat: the 2 existing 2-arg callers — execution-manager.ts:92, propose-pipeline.ts:81 — unaffected; NoopTracer ignores) + `endTime?: number` on `Span.end`. The OTel adapter converts epoch-ms -> an UNAMBIGUOUS HrTime tuple `[s, ns]` (sidesteps the bare-number TimeInput ambiguity across OTel versions) and passes `{startTime, kind}` in the options slot + `endTime` to `raw.end`. RawOtelSpan.end widened to accept the arg. A fake-otelTracer adapter test PINS that timing+kind actually reach the raw OTel span (same "honored end-to-end or inert-in-prod-while-fakes-green" trap E4b guarded for `parent`). SpanKind passed as a NUMBER mirroring the stable OTel numeric enum (INTERNAL=0, CLIENT=2) -> core stays OTel-import-free; otel-init UNCHANGED for Slice 1.
**Slice 1 files (telemetry only, NO stop-glob):** core `tracing.ts`+`tracing.test.ts`, `work-unit-spans.ts`+`work-unit-spans.test.ts`, `telemetry/index.ts` (barrel re-export `OTelContextBridge` type — the minor follow-up; it's exported from tracing.ts:53 but absent from the barrel), apps/api `work-unit-span-export.ts`+`__tests__/work-unit-span-export.test.ts` (widen ExecutionTraceRow.tokenUsage + add createdAt; lift WorkTrace requestedAt/executionStartedAt/completedAt; toMs ISO/Date->epoch-ms). NO otel-init, NO prisma/auth/governance/send/route/env-allowlist.
**Cache/cost + provider:** exec span gains `gen_ai.usage.cache_read_input_tokens`/`gen_ai.usage.cache_creation_input_tokens` (from tokenUsage.cacheRead/cacheCreation, setIfFinite) + `switchboard.cost_usd` (tokenUsage.costUsd, setIfFinite) + `gen_ai.provider.name="anthropic"` when model contains "claude" (honest, codebase is Claude-only). setIfString guard added to the root `switchboard.work_unit.id` (the one unguarded always-set attr).

## Slice 2 (SURFACE, NEXT) — live-trigger design fork for the user

- (i) **route-side-effect:** `void exportWorkUnitSpans(...).catch(warn)` after `submit()` await in apps routes (actions.ts/execute.ts...). Partial coverage (misses cron/contained-workflow paths); per-site; touches ingress submit path -> SURFACE.
- (ii) **[RECOMMENDED] central `onWorkUnitComplete?` callback on `PlatformIngressConfig`**, fired once post-finalize with `{orgId, workUnitId}`, wired ONCE in app.ts to call the db-backed exporter (RecommendationEmitter-style callback injection: core stays db-free, apps provides the effect). Full coverage; touches core PlatformIngress (stop-glob) -> SURFACE.
- (iii) cron-sweep: REJECTED (new cron + 2nd source-of-truth + latency).
- **Flag fork:** reuse `OTEL_EXPORTER_OTLP_ENDPOINT` (no new var) vs a dedicated `*_WORK_UNIT_SPANS_ENABLED` flag (decouples per-work-unit DB-read projection from general OTLP; NEW env var -> env-allowlist SURFACE).
- **Safety invariants (non-negotiable):** void/not-awaited (request returns regardless); error-SWALLOWING (`.catch` -> console.warn, never propagates); read-only (no WorkTrace write, no 2nd source of truth); gated (no-op unless configured). Telemetry must NEVER block/slow/break a request or read path.

Plan: `.claude/ai-infra-uplift-E4c-plan.md` (4 TDD tasks). baseline_sha: cc6fd4645 (worktree HEAD == origin/main).

| step | done-condition (test/cmd)                                                                                                                                          | RED proof                                                        | status | evidence                                                                                                                                                                                                 |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T1   | Tracer `options?{startTime,kind}` 4th param + `Span.end(endTime?)`; adapter epoch-ms->HrTime forward; `--filter core test -- tracing.test.ts` + core/api typecheck | adapter doesn't forward startTime/kind/endTime to raw otelTracer | DONE   | commit 8947ab708; RED=`started[0].options` undefined (2 new tests); GREEN=7/7 tracing (4 new + 3 E4b); core+api typecheck clean (api needed dep build = stale-dist artifact). No deviation.              |
| T2   | `projectWorkUnitSpans` real/derived/synthetic timing + SpanKind + setIfString root id; `--filter core test -- work-unit-spans.test.ts`                             | spans have no startTime/endTime/kind; root id set on empty       | DONE   | commit 84b167afe; RED=5 fail (startTimeMs undefined; root id set on empty); GREEN=15/15 work-unit-spans + full core 4376 pass 0 fail; typecheck clean. No deviation.                                     |
| T3   | cache/cost+provider exec attrs + barrel re-export OTelContextBridge/SpanStartOptions; core test+typecheck                                                          | cache attrs absent; barrel type import unresolved                | DONE   | commit 9cda2b1b8; RED=cache attr undefined (runtime) + barrel type gap (typecheck); GREEN=18/18 + typecheck clean. Minor: barrel import aliased SpanStartOptionsFromBarrel to avoid clash. No deviation. |
| T4   | apps mapper lifts createdAt+WorkTrace ts+cache (toEpochMs) + seam timing test; `--filter api test`                                                                 | createdAtMs/requestedAtMs/cache undefined; seam timing flat      | DONE   | commit 10fd76061; RED=3 new fail (createdAtMs/cache/requestedAtMs undefined); core rebuilt first (CRITICAL fix worked); GREEN=8/8 apps; api typecheck clean. No deviation.                               |

gate_results (local, 4 commits 8947ab708/84b167afe/9cda2b1b8/10fd76061): typecheck=PASS(22 tasks) core-test=PASS(4379) api-test=PASS(2312) lint=PASS(0 err) format=PASS arch=PASS(no err; touched files <400ln) verify-fast=PASS(no route/env debt) build=PASS(10 incl dashboard) security=PASS(exit0; high/crit = pre-existing ignored GHSAs, 0 new deps) review=SHIP(0 sev>=warn, 2 unreachable/cosmetic nits)
diff-stat: +460/-30, 7 files (telemetry only).
merge_safety: stop-glob touched=NO (7 files all telemetry: tracing.ts/.test, work-unit-spans.ts/.test, telemetry/index.ts, apps work-unit-span-export.ts/.test; no prisma/auth/governance/send/route/env-allowlist; reuses OTEL_EXPORTER_OTLP_ENDPOINT) -> [AUTO] eligible. independent_review=SHIP (fresh-context opus: all 8 criteria PASS — non-throwing/one-directional/honest-timing/GenAI+SpanKind-correct/layer-clean/no-env-var/back-compat/privacy; seam confirmed driven from REAL producer types; HrTime math verified). NONE of the SURFACE conditions fired.

## DONE — Slice 1 MERGED to main `3e6031623` (PR #1220, squash; [AUTO]). 7 files +460/-30.

[AUTO]-merged per authority (MERGE CLEAN; no SURFACE condition fired, no merge-stop glob — telemetry only). ALL GitHub checks GREEN incl. the 4 required (typecheck/lint/test[real-DB]/security) + architecture + CodeQL + docker + 6 evals. Independent fresh-context opus review = SHIP (0 sev>=warn; 2 unreachable/cosmetic nits). Rebased clean onto d27102655 mid-loop (concurrent dashboard-only merges, zero telemetry overlap, three-dot 0/4). Teardown done: local main ff'd, worktree + local branch removed, remote auto-deleted.

**E4c status now: Slice 1 (timeline/quality fix) DONE/MERGED #1220.** The projection now emits honest start/end times (REAL work-unit requestedAt/completedAt; DERIVED+synthetic-marked execution/tool timing) + SpanKind (INTERNAL/CLIENT/INTERNAL) + cache/cost attrs + provider, with OTelContextBridge barrel-exported and the root id setIfString-guarded. **Slice 2 (live trigger) REMAINS = SURFACE** — design fork in `.claude/ai-infra-uplift-E4c-slice2-plan.md` (A route-side-effect vs B central PlatformIngressConfig callback [recommended]; i reuse-flag vs ii dedicated-flag). Touches the ingress submit path and/or env-allowlist -> needs a human decision before build.

## Post-merge fan-out review (user-requested, 2026-06-21): 3 fresh-context opus lenses (arch-alignment / works-as-intended / adversarial). NO Critical. WORKS-AS-INTENDED + ARCHITECTURALLY-ALIGNED = YES.

Reviewers 2+3 EMPIRICALLY ran the merged code against the REAL OTel SDK (@opentelemetry/sdk-trace-base 2.7.1 + api 1.9.1): timing exact, HrTime tuple honored unambiguously (bypasses the bare-number perf-timestamp misread), parent+timing compose into a real single-traceId nested tree (root window contains exec contains tool, kinds INTERNAL/CLIENT/INTERNAL), producer->mapper->projection seam sound vs real store rows. Layer-clean (self-asserting no-db test), one-directional, back-compat, privacy intact, attrs finite-guarded. Sound as merged.
ADJUDICATED disagreement: the "root span -> multi-month wall-clock duration when end(undefined)" finding (R3 called live-reachable, R2 not) -> R2 CORRECT: `requestedAtMs`+`durationMs` are lifted together from the same `wt.trace` summary and `WorkTrace.durationMs` is Int NOT NULL, so `rootStartMs` defined ⟹ `rootEndMs = completedAtMs ?? rootStartMs+durationMs` always defined. NOT live-reachable; latent defense-in-depth gap + a test blind spot (work-unit-spans.test.ts:~348 sets root start-only but never asserts root end).

## Slice 1.1 hardening — MERGED to main `91e609cb7` (PR #1225, squash, [AUTO] auto-merge). 7 files +399/-36. (built this session per /goal "ensure code is clean")

Branch `feat/ai-e4c-hardening` (2 commits 48d4cbe0d + 93ba6c02b, rebased to 856ab4c98), off origin/main 464d82e4f. All required GitHub CI GREEN (typecheck/lint/test[real-DB]/security) + architecture/CodeQL/docker/6 evals. Auto-merge armed behind required checks (no babysitting); repo auto-deleted the head. Teardown: worktree removed, local+remote branch deleted, local main ff'd to 91e609cb7. HYGIENE GOTCHA fixed: a hardening subagent cwd-DRIFTED and left uncommitted edits to 3 telemetry TEST files in the MAIN worktree ([[feedback_subagent_worktree_drift]]); 2 of 3 were byte-identical to the merged canonical, the 3rd a stale partial; discarded all 3 + ff'd to the reviewed canonical on origin/main (the merged hardening was made + reviewed + CI'd on the hardening branch, unaffected).
Fixes A-E (throw-safety Array.isArray + element-narrowing; symmetric+inversion-safe timing via `deriveSpanTiming` helper [both-or-neither AND start<=end, else degrade flat]; geometry clamps child-within-parent; toEpochMs numeric; epochMsToHrTime negative-clamp+nanos-carry). Independent review on the FIRST commit caught a REAL inversion bug my clamps introduced (exec start clamped above its own end; root clock-skew; negative tool dur) + incomplete null-element guard -> REVISE round 1 (`deriveSpanTiming` + dur floor + emitMalformed DRY + null-element narrow; tests split into `work-unit-spans-inversion.test.ts` to stay under the 600-line cap). Re-VERIFY: all 11 gates GREEN (core 4401/api 2317/typecheck/lint 0-err/format/arch[no >600]/verify-fast/build/security); independent fresh-context opus review = SHIP (0 sev>=warn; proved no span can invert across all 3 levels + malformed, intrinsically never-throw, no regression, mutation-verified tests). Telemetry-only, no stop-glob -> [AUTO]. emitToolSpan complexity 17 (warn-only, non-blocker). DEFERRED (planned): the real-SDK InMemorySpanExporter test + gen_ai.system/provider semconv coherence.

RECOMMENDED follow-up (DONE this session as Slice 1.1) = a focused [AUTO] hardening PR BEFORE Slice 2 (telemetry-only, no stop-glob), folding the agreed cheap fixes: (1) `Array.isArray` guards on executions+toolCalls in projectWorkUnitSpans (intrinsic throw-safety vs the never-throw contract; toolCalls is a Json column that can hold a non-array, which the mapper's `?? []` does NOT catch); (2) symmetric timing guard (only set startTime when endTime also derivable -> partial anchor degrades fully flat; closes the wall-clock gap + the test blind spot); (3) clamp tool end to execEndMs (+ exec start >= root start) -> no child-exceeds/precedes-parent geometry; (4) toEpochMs handle a numeric epoch (createdAt-as-number quiet data loss); (5) epochMsToHrTime normalize negative/fractional (defensive); (6) consider gen_ai.system(switchboard, E4b) vs gen_ai.provider.name(anthropic, E4c) semconv coherence; (7) a real InMemorySpanExporter test pinning the adapter->SDK leg (would catch 1-3). All cheap, ship-clean, harden BEFORE the live trigger fires.

## Log

- 2026-06-21: ORIENT done. VERIFY-FIRST via 3 parallel subagents (timing/otel-api/completion-seam), all evidence-backed. Two-slice split confirmed; honest timing model locked. -> FRAME (brainstorming the timing + live-trigger design) -> PLAN.
- 2026-06-21: FRAME + PLAN done (4 TDD tasks, `.claude/ai-infra-uplift-E4c-plan.md`). FAN-OUT PLAN GRADE (2 opus): B(code-grounded)=PASS (every sketch compiles vs real WorkTrace/ExecutionTraceInput/OTel@1.9.1 types; 2 legacy startSpan callers + all 3 Tracer impls safe; Date.parse/HrTime math exact); A(critic)=REVISE, 1 CRITICAL = T4 seam `import("@switchboard/core")` resolves to STALE dist (`pnpm --filter api test` skips turbo ^build) -> new core timing code absent until rebuilt. REVISE round 1 applied: (1) T4 rebuilds core before the apps seam test, (2) RecordingTracer-extension made an explicit MANDATORY step in T2+T4, (3) dropped unused executionStartedAt (YAGNI), (4) epochMsToHrTime positive-epoch comment. Both confirm: timing honest, no stop-glob, [AUTO] eligible. -> EXECUTE.
- 2026-06-21: EXECUTE done (4 TDD tasks, each RED-proven then GREEN: 8947ab708/84b167afe/9cda2b1b8/10fd76061). T1 opus (Tracer iface), T2 opus (timing correctness), T3/T4 sonnet. The CRITICAL stale-dist fix worked (T4 rebuilt core before the seam test went green). VERIFY: full gate suite ALL GREEN (typecheck/core 4379/api 2312/lint/format/arch/verify-fast/build/security) + independent fresh-context opus review = SHIP (0 sev>=warn; 2 unreachable/cosmetic nits, no fix needed). origin/main advanced cc6fd4645->d27102655 (concurrent dashboard #1217, zero telemetry overlap) -> rebased clean (three-dot 0/4); fast re-verify green. PR #1220 -> all GitHub required checks GREEN -> [AUTO] SQUASH-MERGED #1220 -> main `3e6031623`. Teardown done. **Slice 1 DONE. Slice 2 (live trigger) SURFACED for a human decision** (`.claude/ai-infra-uplift-E4c-slice2-plan.md`).

---

## E4-completion `/loop` (2026-06-21, this session) — closing E4 in 3 PR-sized slices

Goal: drive E4 to FULL completion, then STOP. Authority: AUTONOMOUS-WITH-GUARDRAILS (slices A+B telemetry-only [AUTO] -> merge clean after independent review at 0 >=warn; slice C live trigger -> SURFACE). VERIFY-FIRST reconfirmed on current main (135613b5b): slice-1 `3e6031623` + 1.1 `91e609cb7` present; exporter still un-triggered (callers = tests only); `submit` returns `{ok,result,workUnit}` on success/deny/require_approval legs (NOT the pre-normalize ok:false early returns: intent-not-found/entitlement-denied/bad-trigger/deployment-resolve-error); `approvalNotifier?` is the fire-and-forget precedent on PlatformIngressConfig (invoked best-effort with `.catch`); PrismaExecutionTraceStore(`findByWorkUnitId`)/PrismaWorkTraceStore(`getByWorkUnitId`) assignable to WorkUnitSpanExportDeps (re-confirmed); env-allowlist has ONLY OTEL_EXPORTER_OTLP_ENDPOINT + OTEL_SERVICE_NAME (both required_in_env_example); `isWorkUnitTracingEnabled()` reads ONLY OTEL_EXPORTER_OTLP_ENDPOINT; apps/api has NO @opentelemetry/\* deps declared (otel-init.ts loads them via runtime require); exporter uses module-level `getTracer()` (set by otel-init), NOT createOTelTracer directly. OTel GenAI semconv (web-confirmed): `gen_ai.system` DEPRECATED -> renamed `gen_ai.provider.name` (same concept); provider.name is Required on BOTH inference AND agent (invoke_agent) spans. app.ts: `new PlatformIngress({...})` ~:776-794; `app.prisma` decorated :399; `app.workTraceStore` decorated :508 (NULLABLE, `?? null`); precedent `new PrismaExecutionTraceStore(app.prisma)` in routes/marketplace.ts:758.

Slice split (combined A+B was considered, then SPLIT — different layers/concerns; B naturally pins A's corrected semconv):

- **Slice A — gen_ai semconv coherence [AUTO]: PR #1230 (auto-merge ARMED, awaiting required CI).** Branch `fix/ai-e4c-semconv-coherence` @ `4a4d0f743` (rebased onto 135613b5b). Dropped `gen_ai.system` from root+exec (deprecated; "switchboard" not a provider); derive `gen_ai.provider.name` on root invoke_agent from executions (Claude-only->anthropic; omit when no model ran) via new `deriveWorkUnitProvider`; kept exec provider.name. New `work-unit-spans-semconv.test.ts` (124 ln). TDD RED (4 fail: system-present + no-root-provider) -> GREEN (34/34). Gate-runner ALL GREEN (typecheck 22/22, core 4424, api 2331, lint 0-err, format, arch<600, verify-fast no route/env debt, audit no-new-dep). Independent fresh-context review = SHIP (0 >=warn; proved omit-not-fabricate + never-throw vs hostile inputs; core OTel+db-free; tests genuinely pin). Telemetry-only, no stop-glob. Worktree `.claude/worktrees/ai-e4c-semconv` (remove at teardown after merge).
- **Slice B — real-SDK InMemorySpanExporter pinning test [AUTO]: NEXT (off updated main AFTER #1230 merges).** apps/api test wiring `createOTelTracer(realOtelTracer, contextBridge)` to a real `BasicTracerProvider` + `InMemorySpanExporter`, project a small tree, assert FINISHED spans carry right name/kind/start/end/parenting + the corrected semconv. ADD devDeps `@opentelemetry/api` + `@opentelemetry/sdk-trace-base` to apps/api (match the lockfile-resolved version — sdk-node override floor 0.217.0; post-merge review ran sdk-trace-base@2.7.1 + api@1.9.1; check pnpm-lock for the transitive resolved version + pin to it). createOTelTracer is structurally typed (`otelTracer: {startSpan}` + optional contextBridge `{active(),with()}`); reuse otel-init.ts:64-69 contextBridge literal (`active: () => api.context.active(); with: (c,s) => api.trace.setSpan(c,s)`) built from a DIRECT import (not the runtime require). Build core dist before the apps test. The test that would have caught the slice-1.1 inversion bug at unit level.
- **Slice C — the LIVE TRIGGER [SURFACE]: after B.** superpowers:brainstorming the GENUINE design: placement fork (A per-route side-effect vs B [recommended] central `onWorkUnitComplete?` callback on PlatformIngressConfig wired ONCE in app.ts, RecommendationEmitter-style so core stays db-free; invoke post-finalize at every ok:true return wrapped in try/catch, never awaited) + flag fork (i reuse OTEL_EXPORTER_OTLP_ENDPOINT vs ii dedicated flag -> env-allowlist stop). Safety NON-NEGOTIABLE: void/not-awaited + error-SWALLOWING (throwing exporter can't reach submit caller; TEST it) + read-only (no WorkTrace write, no 2nd source of truth) + gated (no-op when off) + tenant-safe + fire-once-per-submit incl. deny/approval paths. Touches ingress submit (stop-glob) and/or env-allowlist -> SURFACE for human merge regardless of fork. Design fork detail in `.claude/ai-infra-uplift-E4c-slice2-plan.md`.

Cadence: one slice per `/loop` iteration. Wakeup polls #1230 merge, then teardown + Slice B.

## Log

- 2026-06-21 (E4-completion loop): VERIFY-FIRST done (1 ground-truth subagent + direct greps + web semconv check). Slice A (semconv coherence) built TDD RED->GREEN, gate-runner ALL GREEN + independent review SHIP, PR #1230 opened, auto-merge armed. -> wakeup to confirm merge -> Slice B.
- 2026-06-21 (E4 loop, iter 2): Slice A **#1230 MERGED** (f98f9bbb7, all checks green) + torn down. **Slice B (real-SDK pinning test) DONE -> PR #1235, auto-merge armed.** Built off fresh main 2dac4ef82: apps/api `__tests__/work-unit-spans-realsdk.test.ts` wiring `createOTelTracer` -> REAL `BasicTracerProvider`+`InMemorySpanExporter` (sdk-trace-base@2.7.1 + api@1.9.1 EXACT-pinned devDeps, core stays OTel-free), asserts 3-span tree name/kind(real `SpanKind` enum)/HrTime-exact-timing/single-traceId-parenting/coherent-semconv + a no-anchor degrade case. GOTCHAS: had to `pnpm reset` (stale schemas dist -> `first-touch-greeting` union TS error) THEN full `pnpm build` (reset skips ad-optimizer -> `@switchboard/ad-optimizer` module-not-found cascade in api typecheck); `ToolCallRecord` REQUIRES `governanceDecision` (tsc-over-tests caught it, vitest's esbuild didn't -> api BUILD failed); the statically-typed real `Tracer` needs a thin pass-through wrapper for `createOTelTracer`'s loose `options?:unknown` param (otel-init gets this free via untyped require). TDD RED-proven non-vacuous (drop the contextBridge -> spans get different traceIds, parenting asserts fail). Gate-runner ALL GREEN (build 10/10, typecheck 22/22, api 2334, core 4424, lint/format/arch/verify-fast; **audit NO new advisory** - OTel already in the tree, lockfile delta = within-range 2.7.0->2.7.1 patch re-resolution). Independent review SHIP (0>=warn; re-derived all 6 timing assertions + proved HrTime float-exactness; wrapper sound; no prod change). Rebased onto c5b1ef6f5 (#1234 test-only, zero overlap; cleaned a `next-env.d.ts` build artifact the verifier's build left). PR #1235 (8d155aa98) auto-merge armed. **NEXT = Slice C (the LIVE TRIGGER, SURFACE capstone)**: brainstorm the placement fork (A per-route side-effect vs B [recommended] central `onWorkUnitComplete?` on PlatformIngressConfig wired once in app.ts) + flag fork (reuse OTEL_EXPORTER_OTLP_ENDPOINT vs dedicated flag); safety = void/not-awaited + error-SWALLOWING + read-only + gated + tenant-safe + fire-once. Touches ingress submit (stop-glob) and/or env-allowlist -> SURFACE for human merge. After C surfaced, E4 COMPLETE -> final report + STOP.
- 2026-06-21 (E4 loop, iter 3 — FINAL): Slice B **#1235 MERGED** (2e7b6da71) + torn down. **Slice C (the LIVE TRIGGER) DONE -> #1236 SURFACED** (NOT auto-merged; touches PlatformIngress stop-glob). Brainstormed (B central callback + reuse-flag), pressure-tested vs the real submit() (refined: do NOT fire on idempotency replays 178/180 [double-export] nor ok:false legs; the failed-execution throw-path 479 finalizes-then-throws = v1 limitation). Design+plan `.claude/ai-infra-uplift-E4c-sliceC-plan.md`. EXECUTE (opus subagent, TDD): `onWorkUnitComplete?` field on PlatformIngressConfig + private `fireWorkUnitComplete` (try/catch swallow) + 4 fire calls (after each persistTrace/finalizeTrace: gov-error/deny/require_approval[covers both returns]/success); apps `buildWorkUnitSpanExportHook(deps)` (void+`.catch`); app.ts wires it gated on `isWorkUnitTracingEnabled() && app.prisma` (the `&& app.prisma` is a real type-guard — PrismaExecutionTraceStore needs non-null prisma), deps built ONCE at boot. platform-ingress.ts 516->551 (<600). TDD RED-proven (6 core+4 api fails -> 9 core+13 api GREEN; 68/68 platform-ingress regression). Gate-runner ALL GREEN (build 10/10, typecheck 22/22, core 4433, api 2341, lint/format/arch/verify-fast[no new route/env]/audit[no deps]). Independent review SHIP (0>=warn; traced all 14 submit() exits; verified void-not-awaited [only sync work = 1 process.env read]/swallow-both-legs/read-only/gated/tenant-safe/fire-once + producer-populated-in-prod + layer-clean; 2 no-action nits: optional throw-path no-fire test, success-leg can export a `running` trace if finalize blipped [read-only best-effort, fine]). origin/main unchanged (2e7b6da71) at SURFACE; no rebase. **E4 COMPLETE: A #1230 + B #1235 MERGED, C #1236 SURFACED for human merge.** Worktree `.claude/worktrees/ai-e4c-trigger` KEPT until #1236 merges (teardown then). LOOP STOPPED (terminal: E4 fully merged-or-surfaced). Remaining ai-infra items decision/dependency-blocked (S3/S4/S8/E1/E2/E3/E5).
- 2026-06-22 (user-requested /requesting-code-review on #1236, pre-merge): fresh-context senior review (lens = codebase/wider-arch alignment + works-as-intended; ran tests + both typechecks) = **Ready to merge YES, 0 Critical/0 Important, 2 Minor.** Traced every submit() exit (fire-once on the 4 processed ok:true legs, zero on the 9 ok:false/replay/throw exits), confirmed layer purity (core db/otel-free), the approvalNotifier-precedent injection, store-built-once-at-boot, tenant-safety, producer-populated-in-prod, both plan deviations (`&& app.prisma` guard + dynamic import) correct. **Minor 2 FIXED (commit 773bbc35b, test-only):** strengthened the seam test to pin the REAL `PlatformIngressConfig["onWorkUnitComplete"]` type (from `@switchboard/core/platform`) + a REAL `PrismaExecutionTraceStore`/`PrismaWorkTraceStore` -> `WorkUnitSpanExportDeps` type-level assertion (api typecheck passes => seam proven). **Minor 1 documented (PR comment), NOT code-changed (out of scope):** the exporter zero-trace early-return means only the success-leg-with-executions renders a full waterfall; deny/gov-error/require_approval fires emit nothing today (kept for future root-span emission + invariant simplicity; changing it = editing the merged E4b exporter = separate slice). **PR #1236 head now 773bbc35b** (was 4eee93f9e). Still SURFACED awaiting human merge; worktree kept. origin/main advanced to 43da31ea3 (#1237 dashboard-only, zero overlap with the 5 files).
- 2026-06-22 (user authorized the merge via /goal, post-review): all 4 required checks GREEN on head 773bbc35b (test 10m43s real-DB, typecheck, lint, security) + architecture/CodeQL/docker/6 evals; mergeable CLEAN. **SQUASH-MERGED #1236 -> main `a4b7eff26`.** Teardown: local main ff'd a4b7eff26, worktree `.claude/worktrees/ai-e4c-trigger` removed, branch `feat/ai-e4c-live-trigger` deleted, remote head auto-deleted, pruned, `next-env.d.ts` artifact restored. Main worktree clean, no drift. **E4 FULLY MERGED & CLOSED: A #1230 + B #1235 + C #1236 all on main.** All 3 slice worktrees torn down. Remaining ai-infra items decision/dependency-blocked: S3 drop/rescope, S4 4.7+ model bump, S8 memory-gov greenlight, E1/E2/E3/E5.
