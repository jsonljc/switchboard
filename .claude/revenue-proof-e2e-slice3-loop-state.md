# revenue-proof e2e SLICE 3 (digest + delivery) loop — externalized state (orchestration scratch, not committed)

Durable record lives in memory note project_north_star_activation_gap (P3.3 de-risk lane; "slice 3 (LAST)").

Goal: Prove the weekly-digest ASSEMBLE-AND-DELIVER content seam e2e. Reuse the slice-1/2 substrate + cohort (book -> attendance -> payment over InMemoryRevenueDb via REAL stores). (1) Assert buildWeeklyDigest renders the owner numbers (count, expected, proven-paid, needs-attention) from the REAL substrate-backed report path (assembleWeeklyReport -> createPeriodRollup). (2) Drive ledger.deliver_weekly_report through REAL PlatformIngress.submit + REAL platform-direct carve-out resolver + REAL deliver handler + REAL delivery service, with an INJECTED EmailSender capturing the payload; assert the SAME digest figures reach the email UNALTERED. Mock ONLY: Resend (injected EmailSender), Prisma (substrate), recipient resolution (DB edge).
Authority: autonomous-with-guardrails (auto-merge ONLY if all gates green + independent review 0 findings>=warn + divergence clean + high confidence; else SURFACE). Task-size: standard (test-only, ONE new file).
Base: origin/main @ bdb2c2117 (re-fetched) baseline_sha: bdb2c2117
merge_safety: stop-glob touched = NO (single new file apps/api/src/**tests**/revenue-proof-digest-delivery-e2e.test.ts; name has no payment/auth/governance/prisma; diff is 1 test file, no prod change). Exercises MONEY + OUTBOUND EMAIL content -> keep bar HIGH: independent review MUST re-derive the digest figures from the seeded journey AND confirm they reach the EmailSender payload unaltered. independent_review=<pending>

## Ground truth (tool-verified vs origin/main bdb2c2117)

- Chain: deliver-weekly-report.ts handler -> WeeklyReportDeliveryWriter.deliverReport({orgId,actorId}) -> createWeeklyReportDeliveryService({resolveRecipients, assembleReport, sendEmail, dashboardUrl, now}) -> assembleWeeklyReport(deps,orgId,now) [createPeriodRollup over ReportStores] -> buildWeeklyDigest(report,{periodLabel,dashboardUrl,maxAttentionItems}) -> sendEmail({to,subject,html,text}).
- DETERMINISM (load-bearing): assembleWeeklyReport uses completedWeekRange(now) = PRIOR fully-elapsed UTC Mon..Sun week (NOT slice 1/2 route "THIS WEEK"). So book at T_book and DELIVER with injected now=T_deliver where completedWeekRange(T_deliver) CONTAINS T_book. Pinned: T_book=2026-06-17T12:00Z (Wed), T_deliver=2026-06-24T12:00Z (Wed next wk). completedWeekRange(T_deliver)=[2026-06-15,2026-06-22) ∋ T_book (verified by node). Receipt.createdAt = system-time at book (substrate stamps new Date(); buildCalendarReceiptData sets none) -> freeze system time at T_book; inject now=T_deliver into the delivery service only.
- NUMBERS (re-derived, not fixtured): cohort=1; revenueCents=45000 (expectedValueAtIssue snapshot, opp estimatedValue); paidRevenueCents=30000 (PSP fetch-back, != expected by design); paidBookings=1; bookingsWithValue=1; bookingsNeedingAttention=0 (clean lead). view.currency="SGD" (issueReceiptedBookingInTx persists deps.defaultCurrency; substrate wires defaultCurrency:"SGD"). formatMoneyFromCents(45000,"SGD")="SGD 450.00"; (30000,"SGD")="SGD 300.00" (Intl en-US, node-verified). weekPeriodLabel(T_deliver)="Jun 15 to Jun 21".
- DIGEST metrics (buildWeeklyDigest): receipted_bookings value "1"; paid_revenue value "SGD 300.00" detail "1 of 1 bookings paid"; receipted_revenue value "SGD 450.00" detail "1 of 1 carried a value"; needs_attention value "0". subject "Your week: 1 receipted booking, SGD 450.00 booked". headline/periodLabel "...Jun 15 to Jun 21". attention [], attentionNote null.
- WIRING boundary: buildTestServer does NOT register deliver_weekly_report (no weeklyReportDeliveryWriter option) AND uses resolveAuthoritativeDeployment(null) (NO carve-out). So buildTestServer drives ONLY the cohort seeding (book via buildCalendarBookTool; attendance + payment via app.platformIngress, slice-2 proven). The delivery seam needs a SEPARATE manually-built ingress = the ledger-weekly-report-cron-live-path.test.ts pattern (real GovernanceGate+seeded system spec, real OperatorMutationMode+buildDeliverWeeklyReportHandler, deploymentResolver=resolveAuthoritativeDeployment(throwingResolver,{isPlatformDirectIntent:buildPlatformDirectIntentPredicate(registry)}), inMemory idempotency-aware traceStore) BUT with the REAL delivery service writer (not the fake), over the SAME substrate.
- assembleWeeklyReport deps: { stores: app.reportStores (after wireReportStores: stub rest + REAL receipts.countReceiptedBookingsInWindow + receiptedBookings.listForCohort over substrate), reportCache: app.reportCacheStore (createInMemoryReportCacheStore), baselineStore: createInMemoryBaselineStore() }. report.receiptedBookings.count <- receipts.countReceiptedBookingsInWindow; revenue+quality <- receiptedBookings.listForCohort. No NEW substrate query (same store calls as slice-2 route) -> NO substrate change, NO prod change.
- Recipient resolution is a DB (Prisma) edge -> inject resolveRecipients: async () => ["owner@org-1.test"] (faithful to the injected boundary).

## Plan (TDD-shaped, ephemeral) — ONE new file: apps/api/src/**tests**/revenue-proof-digest-delivery-e2e.test.ts

Shared harness: db=InMemoryRevenueDb; seed contact ct-1 (leadgenId, pdpa null) + opp-1 (45000, qualified); buildTestServer(real stores over db) -> book (buildCalendarBookTool) -> submitAttendance -> submitPayment (PSP 30000 SGD). Seed OUT-OF-WINDOW decoy: booking bk-decoy + booked calendar receipt createdAt 2026-06-24 (current week, value-less) -> must be EXCLUDED by completed-week window (load-bearing: real booking row so a broken window would over-count, not orphan-drop). Build report deps (app.reportStores after wireReportStores + caches). Build delivery ingress (carveOut param) with REAL delivery service { resolveRecipients->[email], assembleReport->assembleWeeklyReport(deps), sendEmail->capturingSender, dashboardUrl, now->T_deliver }.

| step                               | done-condition (test/cmd)                                                                                                                                                                                                                                                                                           | RED proof                                                                             | status | evidence |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ------ | -------- |
| 1 PART1 digest figures             | assembleWeeklyReport+buildWeeklyDigest -> count "1", expected "SGD 450.00", paid "SGD 300.00", needs-attn "0", subject/label; decoy excluded                                                                                                                                                                        | falsify: skip payment -> paid "SGD 0.00" (red); deliver-now=T_book -> count "0" (red) | todo   |          |
| 2 PART2 figures->email unaltered   | submit deliver via real ingress+carve-out -> outcome completed, recipientCount 1; captured email subject==digest.subject, text/html CONTAIN "SGD 450.00"/"SGD 300.00"/"Receipted bookings: 1"/"Bookings needing attention: 0"; text===renderWeeklyDigestText(re-derived), html===renderWeeklyDigestHtml(re-derived) | falsify: assert wrong figure (red)                                                    | todo   |          |
| 3 PART2-neg carve-out load-bearing | carveOut=false -> submit returns deployment_not_found, NO email captured                                                                                                                                                                                                                                            | inherent                                                                              | todo   |          |

gate_results: typecheck=PASS test(api 2334/0, +3 new)=PASS lint=PASS format=PASS arch=PASS(581 lines<600) verify-fast=PASS security=PASS build=PASS review=PASS(APPROVE 0>=warn)
merge_safety: stop-glob touched=NO (1 test file). independent_review=APPROVE — opus re-derived all 4 figures from PROD CODE (count 1 / paid "SGD 300.00" / expected "SGD 450.00" / needs-attn 0 / subject / label) = MATCH on every line; confirmed figures reach EmailSender unaltered (byte-exact, not circular); real-vs-mock boundary correct; decoy + carve-out negative test load-bearing; determinism sound. 2 INFO nits (weak att.ok/pay.ok seeding checks; listForCohort drops optional now=prod-faithful). HARDENED the att.ok/pay.ok nit -> assert outcome "completed" (strictly additive, re-verified green). confidence=HIGH.
commit: f2eacca1e -> REBASED onto origin/main 135613b5b (concurrent advance #1228 docs-only, ZERO overlap) -> 1d31b712d. PR #1234 OPEN.
carry_forward (<=150 words): VERIFY done, ALL gates green + review APPROVE. RED proofs captured (drop payment -> paidRevenueCents 0; wrong window T_BOOK -> count 0). Authority=autonomous-with-guardrails -> ALL conditions MET (gates green, review 0>=warn, no stop-glob, divergence clean post-rebase, high confidence) -> CONVERGE: watch PR #1234 CI; on all-required-green + final divergence re-check clean -> squash-merge (disable any --auto before late pushes). Then cleanup worktree + branch, ff-sync main, update memory note (P3.3 slice 3 = LAST DONE -> whole-loop e2e complete), drop slice-3 plan-doc checkbox at next ORIENT (don't burn CI docs PR). Worktree .claude/worktrees/revenue-proof-slice3.

## Log

- 2026-06-21: ORIENT done. Collision scan clean. Read full chain (handler/service/assembler/digest/send-email/producer/cron-live-path/substrate/test-server/app.ts wiring/getView/issuance). Verified numbers+dates+currency via node. Worktree created off origin/main bdb2c2117. -> EXECUTE (TDD).
- 2026-06-21: EXECUTE done. Wrote 1 test file (3 tests). GREEN after fixing Intl-currency NBSP (U+00A0, node-confirmed) via normalizeSpace; removed 3 accidental em-dashes. RED proofs captured: drop payment -> paidRevenueCents 0; assemble w/ wrong window T_BOOK -> count 0. GOTCHA: fresh worktree FULL-TURBO build skipped `prisma generate` -> `.prisma/client/default` missing -> ran `pnpm db:generate`. -> VERIFY.
- 2026-06-21: VERIFY done. Gate-runner: ALL green (typecheck/api-test 2334-0/lint/format/arch 581<600/verify-fast/build/security). Independent opus review #1: APPROVE 0>=warn, re-derived all 4 figures from PROD CODE = MATCH, confirmed unaltered+real-vs-mock+determinism+load-bearing decoy/carve-out. Hardened the 1 INFO nit (weak att.ok/pay.ok -> assert outcome "completed"), re-verified green. -> CONVERGE.
- 2026-06-22: SESSION CLOSE-OUT (post 3rd user-requested review). 3 independent fresh-context reviews ALL passed (figure-derivation APPROVE 0>=warn; architecture-alignment Ready-YES; 3rd review Ready-YES, all 6 figures independently re-derived = MATCH). Confirmed main HEALTHY (the reviewer's flagged tsc errors were LOCAL stale dist+node_modules from concurrent A3/A4/A5/E4c merges; `pnpm reset`+`pnpm build`+`pnpm install --frozen-lockfile` -> api tsc EXIT 0). HYGIENE: restored generated apps/dashboard/next-env.d.ts (my `pnpm build` flipped .next/dev/types->.next/types); ff-synced local main = origin/main = a4b7eff26 (slice c5b1ef6f5 is ancestor; clean ff, no divergence); NO tracked working-tree changes; my branch+worktree gone; left concurrent worktrees/.claude-scratch/MEMORY.md (A5b-owned) untouched. DEFERRED (judgment, to keep session close clean + per slice-1/2/A3 precedent): plan-doc #1211 "Status: slice 1 in flight" line is stale (all 3 slices merged: #1214/#1224/#1234) -> a future session can finalize it in a 1-line docs PR; authoritative completion record = git history + topic note project_north_star_activation_gap (updated). NEXT SLICE = different workstream (A5b Robin retry, MEMORY.md RESUME, worktree agents-fix-a5b-retry = concurrent session) -> FRESH session per build-loop doctrine; NOT started here. WORKSTREAM COMPLETE.
- 2026-06-21: CONVERGE done. Rebased onto origin/main 135613b5b (docs-only advance #1228, zero overlap) -> pushed -> PR #1234. User-requested 2nd review (superpowers:requesting-code-review, architecture-alignment lens, opus): Ready-to-merge YES, 0 Critical/Important, 2 minor style nits (acceptable harness dup w/ cron-live-path; harmless trace-store shape). CI ALL 15 checks pass (test 10m59s + typecheck/lint/security/architecture/docker/CodeQL/analyze/secrets/setup/5 evals). Final divergence re-check: origin/main advanced 135613b5b->2dac4ef82 (A5a 8bbe997bf + E4c #1230 + dashboard #1232) = ZERO overlap w/ slice-3 dep chain (verified by precise grep); mergeable=MERGEABLE/CLEAN, 15/15 SUCCESS. **SQUASH-MERGED PR #1234 -> origin/main c5b1ef6f5** (581 insertions, test-only). Remote branch auto-deleted; worktree removed+pruned; local main ff-synced to c5b1ef6f5. **SLICE 3 COMPLETE = the LAST slice; whole-loop revenue-proof e2e (slices 1+2+3) DONE.** Plan-doc #1211 slice-3 checkbox left for next ORIENT (no CI docs PR burned, per A3/slice-2 precedent).
