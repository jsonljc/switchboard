// Thin re-export: /reports Funnel now delegates to the shared implementation.
// Props are identical; the shared component supplies the mercury-voiced markup.
export { Funnel } from "@/components/reports-shared/funnel";
