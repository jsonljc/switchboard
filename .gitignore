node_modules
dist
.turbo
.next
*.tsbuildinfo
.env
.env.local
.env.*.local

# Personal Claude context bridge (second brain → Switchboard)
CLAUDE.local.md

coverage
.DS_Store
cartridge-audit-prompt.md
GRANT_PITCH_DECK.md
PLAN-V1.md

# Agent operating layer — ephemeral working memory
.agent/memory/working/

# Git worktrees — each session works in an isolated branch checkout
.worktrees/

# Claude Code local artifacts
.claude/scheduled_tasks.lock
.claude/worktrees/

# Obsidian editor state (workspace layout, view state, etc.)
.obsidian/
