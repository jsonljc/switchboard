# Claim Classifier Eval PR-2 Completion — Fixtures-First, Baseline Deferred

**Date:** 2026-05-22
**Status:** Spec — pending user review
**Source plan:** `docs/superpowers/plans/2026-05-16-claim-classifier-eval-ci.md` §PR-2 (lines 1047–1335)
**Prerequisite:** PR #611 (PR-1 harness + smoke fixtures) — MERGED 2026-05-16

---

## Goal

Close out the claim-classifier eval golden set (plan §PR-2) by opening one focused PR that lands all hand-authored dataset artifacts, then commit the machine-generated `baseline.json` as a follow-up after dataset reviewers sign off. Unblocks PR-3 (CI gate + response cache) once the baseline lands.

This is a sequencing decision, not a feature decision. The plan as written bundles fixture authoring and baseline generation into a single PR; this spec splits them.

## Constraints

- **Plan invariants** (carry over from `docs/superpowers/plans/2026-05-16-claim-classifier-eval-ci.md`):
  - File layout under `evals/claim-classifier/fixtures/` is locked.
  - Schema (`evals/claim-classifier/schema.ts`) and harness (`run-eval.ts`, `invoke-classifier.ts`, `score.ts`, `load-fixtures.ts`) ship in PR-1 — out of scope here.
  - Baseline file lives at `evals/claim-classifier/baseline.json` and is the regression floor compared in PR-3 CI.
- **Doctrine — specs land on main.** This spec ships as a focused PR to `main` per CLAUDE.md branch doctrine. The fixture implementation lives on the existing `feat-claim-classifier-eval-golden-set` branch.
- **No new harness behavior.** Schema already accepts `"none"` as `expectedClaimType` (adversarial fixtures use it). The shape test iterates `*.jsonl` so a new neutral file is picked up automatically.

## Code-grounded current state (verified 2026-05-22)

**Worktree:** `.claude/worktrees/classifier-eval-ci` on branch `feat-claim-classifier-eval-golden-set`, 6 commits ahead of `origin/main`, no PR opened, working tree clean.

**Commits present:**

- `38a8a362` — 30 SG English positive fixtures
- `3175a5b0` — 15 SG English adversarial fixtures
- `9fd4496f` — 30 MY English positive fixtures
- `93754cf6` — 15 MY English adversarial fixtures
- `a46b8aac` — fixtures-directory shape test
- `220901b8` — code-review fixes

**Files on disk (fixture line counts):**

- `evals/claim-classifier/fixtures/sg-positive.jsonl` — 30
- `evals/claim-classifier/fixtures/sg-adversarial.jsonl` — 15
- `evals/claim-classifier/fixtures/my-positive.jsonl` — 30
- `evals/claim-classifier/fixtures/my-adversarial.jsonl` — 15
- `evals/claim-classifier/fixtures/smoke.jsonl` — 10 (PR-1)

**Plan §PR-2 calls for, NOT yet present:**

- `evals/claim-classifier/fixtures/neutral.jsonl` — 5 `none`-class fixtures (plan lines 10 + 61).
- `evals/claim-classifier/baseline.json` — generated by `run-eval --write-baseline` against the locked classifier prompt (plan line 1253 / Task 14). Requires `ANTHROPIC_API_KEY` and runs Haiku 4.5 against all ~95 fixtures.

## Design

### Scope of PR-2 (this PR)

Single PR off `feat-claim-classifier-eval-golden-set`, opened against `main`:

1. **Keep the 6 existing commits as-is.** No squash, no rebase. They are already coherent per-language, per-jurisdiction units.
2. **Add one new commit:** `evals/claim-classifier/fixtures/neutral.jsonl` with 5 `none`-class fixtures. Subject — unambiguously non-claim operational copy (hours, parking, location, contact). No marketing verbs, no superlatives, no time pressure.
   - Distribution: 3 SG, 2 MY (matches the broader SG-leaning bias of the dataset).
   - IDs: `neutral-sg-001` … `neutral-sg-003`, `neutral-my-001` … `neutral-my-002`.
   - Each row sets `expectedClaimType: "none"` and `language: "en"`.
3. **No baseline.json in this PR.** Explicitly called out in the PR body.
4. **PR body** uses the plan's template at line 1315, modified to declare baseline as out of scope and reference this spec for the rationale.

### Scope of follow-up commit (PR-2.5, same branch)

After dataset reviewers approve fixtures on the open PR:

1. Run `ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY pnpm eval:classifier --write-baseline` locally (per plan §Task 14 step 1). ~95 Haiku calls, est. ~$0.10.
2. Verify baseline per plan §Task 14 step 2: `classifierPromptHash` matches `CLASSIFIER_PROMPT_HASH` in `packages/core/src/governance/classifier/prompt.ts`, `overallAccuracy >= 0.80`, per-class accuracy ≥ 0.70 for any category with ≥3 samples.
3. Commit `evals/claim-classifier/baseline.json` directly on the same branch with the canonical message from plan §Task 14 step 4: `feat(eval-classifier): lock baseline.json (v1) against current classifier prompt`.
4. Re-request review (light pass — baseline is mechanical output).
5. Merge as one squash.

### Why this sequencing

The baseline is a _regression floor_. Whatever the classifier scores against these fixtures on the day baseline is generated becomes the threshold that all future prompt edits must beat. Two failure modes if we bundle baseline with fixtures:

1. **Reviewer flags a bad fixture.** We have to regenerate the baseline (more API calls, more diff churn) and rebase. The baseline diff and the fixture diff become entangled.
2. **A fixture has a wrong label that nobody catches.** The classifier "gets it wrong" against the wrong label, that wrong-vs-wrong outcome bakes into the floor, and PR-3 CI will silently protect a bad expectation.

Splitting puts the human-judgment step (fixture review) strictly before the irreversible machine step (baseline generation). The trade-off is one extra commit on the same branch — not even an extra PR.

### Anti-scope (deliberately not in this spec)

- ZH / MS / Chinese / Malay fixtures — out of scope for plan §PR-2 entirely.
- PR-3 (CI gate + cache) — separate plan section, separate PR.
- Domain-expert recruitment / review checklist — process question, not architecture.
- Any change to the harness, schema, or scoring logic.

## Risks

- **Neutral fixtures accidentally read as claims.** Mitigation: copy review against the claim-type enum before commit. If a reviewer flags one, swap it; cheap.
- **Branch goes stale during review.** Mitigation: rebase on `main` before the baseline commit; baseline depends only on fixture content + classifier prompt, not on unrelated main changes.
- **Domain-expert review delays baseline.** Acceptable. PR-3 is gated on baseline either way; delay shifts to the cheaper artifact.

## Acceptance criteria

- [ ] `evals/claim-classifier/fixtures/neutral.jsonl` exists, 5 rows, all `expectedClaimType: "none"`, all parse via the existing shape test.
- [ ] PR-2 opened against `main` with the 7 commits described above.
- [ ] PR body explicitly states baseline.json is deferred and links this spec.
- [ ] No `baseline.json` in the PR-2 diff.
- [ ] After approval, baseline commit lands on the same branch and PR merges as one squash.
