# <Client Name>: Commitments

<!-- APPEND-ONLY ledger, both directions. Overdue items escalate to the cockpit top (doc §6.4): -->
<!-- a broken promise is a relationship cost; an honored one is a free CI. -->
<!-- Never edit past entries; close or correct with a new entry. -->

```yaml
open:
  - by: AM | client
    what:
    promised_on: <date + where (call/email/thread ref)>
    due:
    status: open | overdue
closed:
  - by:
    what:
    closed_on:
    outcome:
```
