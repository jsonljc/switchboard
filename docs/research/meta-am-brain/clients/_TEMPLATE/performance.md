# <Client Name>: Performance Snapshot

<!-- GENERATED class: regenerable from dashboards; overwrite freely. -->
<!-- RULES (doc §7.1, §8): every number carries source link + fetched_at. Shelf life 7 days: -->
<!-- expired numbers are EXCLUDED from evidence and flagged, never silently reused. -->
<!-- Anything quoted to the client as current is re-fetched fresh, not read from this file. -->

```yaml
as_of: <YYYY-MM-DD>
source: <internal dashboard link / export ref>

snapshot:
  spend_last_30d:
  primary_kpi: # e.g. CPA, ROAS, cost per lead: THEIR kpi
  kpi_trend_mom:
  emq_purchase: # or per key event
  server_event_coverage:
  creative: { active_ads, oldest_active_days, frequency_7d }
  delivery_flags: [] # learning limited, rejected ads, etc.

anomalies:
  - metric:
    observation:
    window:
    possible_cause: # hypothesis, labeled as such
```
